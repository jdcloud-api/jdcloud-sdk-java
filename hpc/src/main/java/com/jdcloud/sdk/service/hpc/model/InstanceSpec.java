/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.hpc.model;

import java.util.List;
import java.util.ArrayList;

/**
 * 实例配置
 */
public class InstanceSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 可用区
     */
    private String az;

    /**
     * 实例规格
     */
    private String instanceType;

    /**
     * 安装包ID
     */
    private String packageId;

    /**
     * 实例名称。长度为2~128个字符，只允许中文、数字、大小写字母、英文下划线（_）、连字符（-）
     */
    private String name;

    /**
     * 实例hostname。若不指定hostname，则默认以实例名称 name 作为hostname，但是会以RFC 952和RFC 1123命名规范做一定转义
     */
    private String hostName;

    /**
     * 实例密码。可用于SSH登录和VNC登录，长度为8-30个字符，必须同时包含大、小写英文字母、数字和特殊符号中的三类字符。特殊符号包括：\(\)\&#x60;~!@#$%^&amp;\*\_-+&#x3D;\|{}\[ ]:&quot;;&#39;&lt;&gt;,.?/

     */
    private String password;

    /**
     * 主网卡配置。

     */
    private InstanceNetworkInterfaceAttachmentSpec primaryNetworkInterface;

    /**
     * 计费配置。

     */
    private InstanceChargeSpec charge;

    /**
     * 自定义脚本。目前仅支持启动脚本，即 &#x60;launch-script&#x60;，须 &#x60;base64&#x60; 编码且编码前数据长度不能超过16KB。
**linux系统**：支持 &#x60;bash&#x60; 和 &#x60;python&#x60;，编码前须分别以 &#x60;#!/bin/bash&#x60; 和 &#x60;#!/usr/bin/env python&#x60; 作为内容首行。

     */
    
    private List<Userdata> userdata;
    /**
     * 用户自定义元数据。以key-value键值对形式指定，可在实例系统内通过元数据服务查询获取。最多支持40对键值对，且key不超过256字符，value不超过16KB，不区分大小写。
注意：key不要以连字符(-)结尾，否则此key不生效。

     */
    
    private List<Metadata> metadata;
    /**
     * 指定是否在创建计算实例时跳过GPU驱动的自动安装流程。当该参数设置为：&#x60;true&#x60;（默认值）：系统将自动为实例安装适配的GPU驱动程序；&#x60;false&#x60;：系统将不执行GPU驱动安装操作，用户需自行完成驱动的安装配置。
注意事项：该参数仅对配备GPU的计算实例规格生效，若实例未配置GPU硬件则此参数不产生实际效果。建议在需要定制化驱动版本或使用特殊驱动场景时开启此参数

     */
    private Boolean installGpuDriver;

    /**
     * 传入指定调度的su列表：如果参数中的su不在用户配置的数据库su列表中，则禁止调度；传入的参数没有指定su列表时：1.用户数据库中配置了su，则走配置的su调度；2.用户未配置su则进行默认调度策略
     */
    
    private List<String> su;
    /**
     * 实例网络集群Id
     */
    private String hpcClusterId;

    /**
     * 实例网络集群名称
     */
    private String hpcClusterName;

    /**
     * 资源组ID
     */
    private String resourceGroupId;

    /**
     * 自定义实例标签。以key-value键值对形式指定，最多支持10个标签。key不能以 &quot;jrn:&quot; 或“jdc-”开头，仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;。

     */
    
    private List<Tag> userTags;


    /**
     * get 可用区
     *
     * @return
     */
    public String getAz() {
        return az;
    }

    /**
     * set 可用区
     *
     * @param az
     */
    public void setAz(String az) {
        this.az = az;
    }


    /**
     * get 实例规格
     *
     * @return
     */
    public String getInstanceType() {
        return instanceType;
    }

    /**
     * set 实例规格
     *
     * @param instanceType
     */
    public void setInstanceType(String instanceType) {
        this.instanceType = instanceType;
    }


    /**
     * get 安装包ID
     *
     * @return
     */
    public String getPackageId() {
        return packageId;
    }

    /**
     * set 安装包ID
     *
     * @param packageId
     */
    public void setPackageId(String packageId) {
        this.packageId = packageId;
    }


    /**
     * get 实例名称。长度为2~128个字符，只允许中文、数字、大小写字母、英文下划线（_）、连字符（-）
     *
     * @return
     */
    public String getName() {
        return name;
    }

    /**
     * set 实例名称。长度为2~128个字符，只允许中文、数字、大小写字母、英文下划线（_）、连字符（-）
     *
     * @param name
     */
    public void setName(String name) {
        this.name = name;
    }


    /**
     * get 实例hostname。若不指定hostname，则默认以实例名称 name 作为hostname，但是会以RFC 952和RFC 1123命名规范做一定转义
     *
     * @return
     */
    public String getHostName() {
        return hostName;
    }

    /**
     * set 实例hostname。若不指定hostname，则默认以实例名称 name 作为hostname，但是会以RFC 952和RFC 1123命名规范做一定转义
     *
     * @param hostName
     */
    public void setHostName(String hostName) {
        this.hostName = hostName;
    }


    /**
     * get 实例密码。可用于SSH登录和VNC登录，长度为8-30个字符，必须同时包含大、小写英文字母、数字和特殊符号中的三类字符。特殊符号包括：\(\)\&#x60;~!@#$%^&amp;\*\_-+&#x3D;\|{}\[ ]:&quot;;&#39;&lt;&gt;,.?/

     *
     * @return
     */
    public String getPassword() {
        return password;
    }

    /**
     * set 实例密码。可用于SSH登录和VNC登录，长度为8-30个字符，必须同时包含大、小写英文字母、数字和特殊符号中的三类字符。特殊符号包括：\(\)\&#x60;~!@#$%^&amp;\*\_-+&#x3D;\|{}\[ ]:&quot;;&#39;&lt;&gt;,.?/

     *
     * @param password
     */
    public void setPassword(String password) {
        this.password = password;
    }


    /**
     * get 主网卡配置。

     *
     * @return
     */
    public InstanceNetworkInterfaceAttachmentSpec getPrimaryNetworkInterface() {
        return primaryNetworkInterface;
    }

    /**
     * set 主网卡配置。

     *
     * @param primaryNetworkInterface
     */
    public void setPrimaryNetworkInterface(InstanceNetworkInterfaceAttachmentSpec primaryNetworkInterface) {
        this.primaryNetworkInterface = primaryNetworkInterface;
    }


    /**
     * get 计费配置。

     *
     * @return
     */
    public InstanceChargeSpec getCharge() {
        return charge;
    }

    /**
     * set 计费配置。

     *
     * @param charge
     */
    public void setCharge(InstanceChargeSpec charge) {
        this.charge = charge;
    }


    /**
    * get 自定义脚本。目前仅支持启动脚本，即 &#x60;launch-script&#x60;，须 &#x60;base64&#x60; 编码且编码前数据长度不能超过16KB。
**linux系统**：支持 &#x60;bash&#x60; 和 &#x60;python&#x60;，编码前须分别以 &#x60;#!/bin/bash&#x60; 和 &#x60;#!/usr/bin/env python&#x60; 作为内容首行。

    *
    * @return
    */
    public List<Userdata> getUserdata() {
        return userdata;
    }

    /**
    * set 自定义脚本。目前仅支持启动脚本，即 &#x60;launch-script&#x60;，须 &#x60;base64&#x60; 编码且编码前数据长度不能超过16KB。
**linux系统**：支持 &#x60;bash&#x60; 和 &#x60;python&#x60;，编码前须分别以 &#x60;#!/bin/bash&#x60; 和 &#x60;#!/usr/bin/env python&#x60; 作为内容首行。

    *
    * @param userdata
    */
    public void setUserdata(List<Userdata> userdata) {
        this.userdata = userdata;
    }


    /**
    * get 用户自定义元数据。以key-value键值对形式指定，可在实例系统内通过元数据服务查询获取。最多支持40对键值对，且key不超过256字符，value不超过16KB，不区分大小写。
注意：key不要以连字符(-)结尾，否则此key不生效。

    *
    * @return
    */
    public List<Metadata> getMetadata() {
        return metadata;
    }

    /**
    * set 用户自定义元数据。以key-value键值对形式指定，可在实例系统内通过元数据服务查询获取。最多支持40对键值对，且key不超过256字符，value不超过16KB，不区分大小写。
注意：key不要以连字符(-)结尾，否则此key不生效。

    *
    * @param metadata
    */
    public void setMetadata(List<Metadata> metadata) {
        this.metadata = metadata;
    }


    /**
     * get 指定是否在创建计算实例时跳过GPU驱动的自动安装流程。当该参数设置为：&#x60;true&#x60;（默认值）：系统将自动为实例安装适配的GPU驱动程序；&#x60;false&#x60;：系统将不执行GPU驱动安装操作，用户需自行完成驱动的安装配置。
注意事项：该参数仅对配备GPU的计算实例规格生效，若实例未配置GPU硬件则此参数不产生实际效果。建议在需要定制化驱动版本或使用特殊驱动场景时开启此参数

     *
     * @return
     */
    public Boolean getInstallGpuDriver() {
        return installGpuDriver;
    }

    /**
     * set 指定是否在创建计算实例时跳过GPU驱动的自动安装流程。当该参数设置为：&#x60;true&#x60;（默认值）：系统将自动为实例安装适配的GPU驱动程序；&#x60;false&#x60;：系统将不执行GPU驱动安装操作，用户需自行完成驱动的安装配置。
注意事项：该参数仅对配备GPU的计算实例规格生效，若实例未配置GPU硬件则此参数不产生实际效果。建议在需要定制化驱动版本或使用特殊驱动场景时开启此参数

     *
     * @param installGpuDriver
     */
    public void setInstallGpuDriver(Boolean installGpuDriver) {
        this.installGpuDriver = installGpuDriver;
    }


    /**
    * get 传入指定调度的su列表：如果参数中的su不在用户配置的数据库su列表中，则禁止调度；传入的参数没有指定su列表时：1.用户数据库中配置了su，则走配置的su调度；2.用户未配置su则进行默认调度策略
    *
    * @return
    */
    public List<String> getSu() {
        return su;
    }

    /**
    * set 传入指定调度的su列表：如果参数中的su不在用户配置的数据库su列表中，则禁止调度；传入的参数没有指定su列表时：1.用户数据库中配置了su，则走配置的su调度；2.用户未配置su则进行默认调度策略
    *
    * @param su
    */
    public void setSu(List<String> su) {
        this.su = su;
    }


    /**
     * get 实例网络集群Id
     *
     * @return
     */
    public String getHpcClusterId() {
        return hpcClusterId;
    }

    /**
     * set 实例网络集群Id
     *
     * @param hpcClusterId
     */
    public void setHpcClusterId(String hpcClusterId) {
        this.hpcClusterId = hpcClusterId;
    }


    /**
     * get 实例网络集群名称
     *
     * @return
     */
    public String getHpcClusterName() {
        return hpcClusterName;
    }

    /**
     * set 实例网络集群名称
     *
     * @param hpcClusterName
     */
    public void setHpcClusterName(String hpcClusterName) {
        this.hpcClusterName = hpcClusterName;
    }


    /**
     * get 资源组ID
     *
     * @return
     */
    public String getResourceGroupId() {
        return resourceGroupId;
    }

    /**
     * set 资源组ID
     *
     * @param resourceGroupId
     */
    public void setResourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
    }


    /**
    * get 自定义实例标签。以key-value键值对形式指定，最多支持10个标签。key不能以 &quot;jrn:&quot; 或“jdc-”开头，仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;。

    *
    * @return
    */
    public List<Tag> getUserTags() {
        return userTags;
    }

    /**
    * set 自定义实例标签。以key-value键值对形式指定，最多支持10个标签。key不能以 &quot;jrn:&quot; 或“jdc-”开头，仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;。

    *
    * @param userTags
    */
    public void setUserTags(List<Tag> userTags) {
        this.userTags = userTags;
    }



    /**
     * set 可用区
     *
     * @param az
     */
    public InstanceSpec az(String az) {
        this.az = az;
        return this;
    }


    /**
     * set 实例规格
     *
     * @param instanceType
     */
    public InstanceSpec instanceType(String instanceType) {
        this.instanceType = instanceType;
        return this;
    }


    /**
     * set 安装包ID
     *
     * @param packageId
     */
    public InstanceSpec packageId(String packageId) {
        this.packageId = packageId;
        return this;
    }


    /**
     * set 实例名称。长度为2~128个字符，只允许中文、数字、大小写字母、英文下划线（_）、连字符（-）
     *
     * @param name
     */
    public InstanceSpec name(String name) {
        this.name = name;
        return this;
    }


    /**
     * set 实例hostname。若不指定hostname，则默认以实例名称 name 作为hostname，但是会以RFC 952和RFC 1123命名规范做一定转义
     *
     * @param hostName
     */
    public InstanceSpec hostName(String hostName) {
        this.hostName = hostName;
        return this;
    }


    /**
     * set 实例密码。可用于SSH登录和VNC登录，长度为8-30个字符，必须同时包含大、小写英文字母、数字和特殊符号中的三类字符。特殊符号包括：\(\)\&#x60;~!@#$%^&amp;\*\_-+&#x3D;\|{}\[ ]:&quot;;&#39;&lt;&gt;,.?/

     *
     * @param password
     */
    public InstanceSpec password(String password) {
        this.password = password;
        return this;
    }


    /**
     * set 主网卡配置。

     *
     * @param primaryNetworkInterface
     */
    public InstanceSpec primaryNetworkInterface(InstanceNetworkInterfaceAttachmentSpec primaryNetworkInterface) {
        this.primaryNetworkInterface = primaryNetworkInterface;
        return this;
    }


    /**
     * set 计费配置。

     *
     * @param charge
     */
    public InstanceSpec charge(InstanceChargeSpec charge) {
        this.charge = charge;
        return this;
    }


    /**
    * set 自定义脚本。目前仅支持启动脚本，即 &#x60;launch-script&#x60;，须 &#x60;base64&#x60; 编码且编码前数据长度不能超过16KB。
**linux系统**：支持 &#x60;bash&#x60; 和 &#x60;python&#x60;，编码前须分别以 &#x60;#!/bin/bash&#x60; 和 &#x60;#!/usr/bin/env python&#x60; 作为内容首行。

    *
    * @param userdata
    */
    public InstanceSpec userdata(List<Userdata> userdata) {
        this.userdata = userdata;
        return this;
    }


    /**
    * set 用户自定义元数据。以key-value键值对形式指定，可在实例系统内通过元数据服务查询获取。最多支持40对键值对，且key不超过256字符，value不超过16KB，不区分大小写。
注意：key不要以连字符(-)结尾，否则此key不生效。

    *
    * @param metadata
    */
    public InstanceSpec metadata(List<Metadata> metadata) {
        this.metadata = metadata;
        return this;
    }


    /**
     * set 指定是否在创建计算实例时跳过GPU驱动的自动安装流程。当该参数设置为：&#x60;true&#x60;（默认值）：系统将自动为实例安装适配的GPU驱动程序；&#x60;false&#x60;：系统将不执行GPU驱动安装操作，用户需自行完成驱动的安装配置。
注意事项：该参数仅对配备GPU的计算实例规格生效，若实例未配置GPU硬件则此参数不产生实际效果。建议在需要定制化驱动版本或使用特殊驱动场景时开启此参数

     *
     * @param installGpuDriver
     */
    public InstanceSpec installGpuDriver(Boolean installGpuDriver) {
        this.installGpuDriver = installGpuDriver;
        return this;
    }


    /**
    * set 传入指定调度的su列表：如果参数中的su不在用户配置的数据库su列表中，则禁止调度；传入的参数没有指定su列表时：1.用户数据库中配置了su，则走配置的su调度；2.用户未配置su则进行默认调度策略
    *
    * @param su
    */
    public InstanceSpec su(List<String> su) {
        this.su = su;
        return this;
    }


    /**
     * set 实例网络集群Id
     *
     * @param hpcClusterId
     */
    public InstanceSpec hpcClusterId(String hpcClusterId) {
        this.hpcClusterId = hpcClusterId;
        return this;
    }


    /**
     * set 实例网络集群名称
     *
     * @param hpcClusterName
     */
    public InstanceSpec hpcClusterName(String hpcClusterName) {
        this.hpcClusterName = hpcClusterName;
        return this;
    }


    /**
     * set 资源组ID
     *
     * @param resourceGroupId
     */
    public InstanceSpec resourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
        return this;
    }


    /**
    * set 自定义实例标签。以key-value键值对形式指定，最多支持10个标签。key不能以 &quot;jrn:&quot; 或“jdc-”开头，仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;。

    *
    * @param userTags
    */
    public InstanceSpec userTags(List<Tag> userTags) {
        this.userTags = userTags;
        return this;
    }



    /**
     * add item to 自定义脚本。目前仅支持启动脚本，即 &#x60;launch-script&#x60;，须 &#x60;base64&#x60; 编码且编码前数据长度不能超过16KB。
**linux系统**：支持 &#x60;bash&#x60; 和 &#x60;python&#x60;，编码前须分别以 &#x60;#!/bin/bash&#x60; 和 &#x60;#!/usr/bin/env python&#x60; 作为内容首行。

     *
     * @param userdata
     */
    public void addUserdata(Userdata userdata) {
        if (this.userdata == null) {
            this.userdata = new ArrayList<>();
        }
        this.userdata.add(userdata);
    }

    /**
     * add item to 用户自定义元数据。以key-value键值对形式指定，可在实例系统内通过元数据服务查询获取。最多支持40对键值对，且key不超过256字符，value不超过16KB，不区分大小写。
注意：key不要以连字符(-)结尾，否则此key不生效。

     *
     * @param metadata
     */
    public void addMetadata(Metadata metadata) {
        if (this.metadata == null) {
            this.metadata = new ArrayList<>();
        }
        this.metadata.add(metadata);
    }

    /**
     * add item to 传入指定调度的su列表：如果参数中的su不在用户配置的数据库su列表中，则禁止调度；传入的参数没有指定su列表时：1.用户数据库中配置了su，则走配置的su调度；2.用户未配置su则进行默认调度策略
     *
     * @param su
     */
    public void addSu(String su) {
        if (this.su == null) {
            this.su = new ArrayList<>();
        }
        this.su.add(su);
    }

    /**
     * add item to 自定义实例标签。以key-value键值对形式指定，最多支持10个标签。key不能以 &quot;jrn:&quot; 或“jdc-”开头，仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;。

     *
     * @param userTag
     */
    public void addUserTag(Tag userTag) {
        if (this.userTags == null) {
            this.userTags = new ArrayList<>();
        }
        this.userTags.add(userTag);
    }
}