/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.lb.model;


/**
 * redirectAction
 */
public class RedirectAction  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 重定向后协议，取值为Http、Https。不设置，表示重定向不修改请求协议，与客户端请求协议一致
     */
    private String protocol;

    /**
     * 重定向后端口，取值范围为1-65535。不设置，表示重定向不修改请求端口，与客户端请求端口一致
     */
    private Integer port;

    /**
     * 重定向后域名。不设置，表示重定向不修改请求域名，与客户端请求域名一致。支持输入IPv4地址和域名。域名输入限制为：仅支持输入大小写字母、数字、英文中划线“-”和点“.”，最少包括一个点&quot;.&quot;，不能以点&quot;.&quot;和中划线&quot;-&quot;开头或结尾，中划线&quot;-&quot;前后不能为点&quot;.&quot;，不区分大小写，且不能超过110字符
     */
    private String host;

    /**
     * 重定向后URL路径。不设置，表示重定向不修改请求URL路径，与客户端请求URL路径一致。必须以/开头，仅支持输入大小写字母、数字和特殊字符：-_.+!&#39;()%:@&amp;&#x3D;/，区分大小写，且不能超过128字符
     */
    private String path;

    /**
     * 重定向后查询参数，不需手动输入，系统默认添加，不超过128字符，不设置，表示重定向不修改查询参数，与客户端请求查询参数一致
     */
    private String query;

    /**
     * 取值为http_301、http_302。301表示永久性转移，302表示暂时性转移
     */
    private String statusCode;



    /**
     * get 重定向后协议，取值为Http、Https。不设置，表示重定向不修改请求协议，与客户端请求协议一致
     *
     * @return
     */
    public String getProtocol() {
        return protocol;
    }

    /**
     * set 重定向后协议，取值为Http、Https。不设置，表示重定向不修改请求协议，与客户端请求协议一致
     *
     * @param protocol
     */
    public void setProtocol(String protocol) {
        this.protocol = protocol;
    }


    /**
     * get 重定向后端口，取值范围为1-65535。不设置，表示重定向不修改请求端口，与客户端请求端口一致
     *
     * @return
     */
    public Integer getPort() {
        return port;
    }

    /**
     * set 重定向后端口，取值范围为1-65535。不设置，表示重定向不修改请求端口，与客户端请求端口一致
     *
     * @param port
     */
    public void setPort(Integer port) {
        this.port = port;
    }


    /**
     * get 重定向后域名。不设置，表示重定向不修改请求域名，与客户端请求域名一致。支持输入IPv4地址和域名。域名输入限制为：仅支持输入大小写字母、数字、英文中划线“-”和点“.”，最少包括一个点&quot;.&quot;，不能以点&quot;.&quot;和中划线&quot;-&quot;开头或结尾，中划线&quot;-&quot;前后不能为点&quot;.&quot;，不区分大小写，且不能超过110字符
     *
     * @return
     */
    public String getHost() {
        return host;
    }

    /**
     * set 重定向后域名。不设置，表示重定向不修改请求域名，与客户端请求域名一致。支持输入IPv4地址和域名。域名输入限制为：仅支持输入大小写字母、数字、英文中划线“-”和点“.”，最少包括一个点&quot;.&quot;，不能以点&quot;.&quot;和中划线&quot;-&quot;开头或结尾，中划线&quot;-&quot;前后不能为点&quot;.&quot;，不区分大小写，且不能超过110字符
     *
     * @param host
     */
    public void setHost(String host) {
        this.host = host;
    }


    /**
     * get 重定向后URL路径。不设置，表示重定向不修改请求URL路径，与客户端请求URL路径一致。必须以/开头，仅支持输入大小写字母、数字和特殊字符：-_.+!&#39;()%:@&amp;&#x3D;/，区分大小写，且不能超过128字符
     *
     * @return
     */
    public String getPath() {
        return path;
    }

    /**
     * set 重定向后URL路径。不设置，表示重定向不修改请求URL路径，与客户端请求URL路径一致。必须以/开头，仅支持输入大小写字母、数字和特殊字符：-_.+!&#39;()%:@&amp;&#x3D;/，区分大小写，且不能超过128字符
     *
     * @param path
     */
    public void setPath(String path) {
        this.path = path;
    }


    /**
     * get 重定向后查询参数，不需手动输入，系统默认添加，不超过128字符，不设置，表示重定向不修改查询参数，与客户端请求查询参数一致
     *
     * @return
     */
    public String getQuery() {
        return query;
    }

    /**
     * set 重定向后查询参数，不需手动输入，系统默认添加，不超过128字符，不设置，表示重定向不修改查询参数，与客户端请求查询参数一致
     *
     * @param query
     */
    public void setQuery(String query) {
        this.query = query;
    }


    /**
     * get 取值为http_301、http_302。301表示永久性转移，302表示暂时性转移
     *
     * @return
     */
    public String getStatusCode() {
        return statusCode;
    }

    /**
     * set 取值为http_301、http_302。301表示永久性转移，302表示暂时性转移
     *
     * @param statusCode
     */
    public void setStatusCode(String statusCode) {
        this.statusCode = statusCode;
    }



    /**
     * set 重定向后协议，取值为Http、Https。不设置，表示重定向不修改请求协议，与客户端请求协议一致
     *
     * @param protocol
     */
    public RedirectAction protocol(String protocol) {
        this.protocol = protocol;
        return this;
    }


    /**
     * set 重定向后端口，取值范围为1-65535。不设置，表示重定向不修改请求端口，与客户端请求端口一致
     *
     * @param port
     */
    public RedirectAction port(Integer port) {
        this.port = port;
        return this;
    }


    /**
     * set 重定向后域名。不设置，表示重定向不修改请求域名，与客户端请求域名一致。支持输入IPv4地址和域名。域名输入限制为：仅支持输入大小写字母、数字、英文中划线“-”和点“.”，最少包括一个点&quot;.&quot;，不能以点&quot;.&quot;和中划线&quot;-&quot;开头或结尾，中划线&quot;-&quot;前后不能为点&quot;.&quot;，不区分大小写，且不能超过110字符
     *
     * @param host
     */
    public RedirectAction host(String host) {
        this.host = host;
        return this;
    }


    /**
     * set 重定向后URL路径。不设置，表示重定向不修改请求URL路径，与客户端请求URL路径一致。必须以/开头，仅支持输入大小写字母、数字和特殊字符：-_.+!&#39;()%:@&amp;&#x3D;/，区分大小写，且不能超过128字符
     *
     * @param path
     */
    public RedirectAction path(String path) {
        this.path = path;
        return this;
    }


    /**
     * set 重定向后查询参数，不需手动输入，系统默认添加，不超过128字符，不设置，表示重定向不修改查询参数，与客户端请求查询参数一致
     *
     * @param query
     */
    public RedirectAction query(String query) {
        this.query = query;
        return this;
    }


    /**
     * set 取值为http_301、http_302。301表示永久性转移，302表示暂时性转移
     *
     * @param statusCode
     */
    public RedirectAction statusCode(String statusCode) {
        this.statusCode = statusCode;
        return this;
    }


}