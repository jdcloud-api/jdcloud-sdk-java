/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.vpc.model;

import com.jdcloud.sdk.annotation.Required;

/**
 * addBandwidthPackageIPSpec
 */
public class AddBandwidthPackageIPSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 公网IP ID，当加入弹性公网IP时，为elasticIpId。有如下限制：已欠费的公网IP不能加入共享带宽包；包年包月的公网IP不能加入共享带宽包；一个公网IP同时只能加入一个公网带宽包；公网IP和共享带宽包的地域不同或者线路不同时，公网IP不能加入对应共享带宽包；一个共享带宽包默认可加入20个公网IP
     * Required:true
     */
    @Required
    private String publicIpId;

    /**
     * 单个公网IP带宽上限，大小不能超过共享带宽包的带宽上限，单位为Mbps，可取值0或-1，0表示禁止流量通过， -1表示关闭单个公网IP带宽限速，默认值为-1，表示公网IP加入共享带宽包后带宽上限默认为共享带宽包的带宽上限
     */
    private Integer bandwidthMbps;


    /**
     * get 公网IP ID，当加入弹性公网IP时，为elasticIpId。有如下限制：已欠费的公网IP不能加入共享带宽包；包年包月的公网IP不能加入共享带宽包；一个公网IP同时只能加入一个公网带宽包；公网IP和共享带宽包的地域不同或者线路不同时，公网IP不能加入对应共享带宽包；一个共享带宽包默认可加入20个公网IP
     *
     * @return
     */
    public String getPublicIpId() {
        return publicIpId;
    }

    /**
     * set 公网IP ID，当加入弹性公网IP时，为elasticIpId。有如下限制：已欠费的公网IP不能加入共享带宽包；包年包月的公网IP不能加入共享带宽包；一个公网IP同时只能加入一个公网带宽包；公网IP和共享带宽包的地域不同或者线路不同时，公网IP不能加入对应共享带宽包；一个共享带宽包默认可加入20个公网IP
     *
     * @param publicIpId
     */
    public void setPublicIpId(String publicIpId) {
        this.publicIpId = publicIpId;
    }

    /**
     * get 单个公网IP带宽上限，大小不能超过共享带宽包的带宽上限，单位为Mbps，可取值0或-1，0表示禁止流量通过， -1表示关闭单个公网IP带宽限速，默认值为-1，表示公网IP加入共享带宽包后带宽上限默认为共享带宽包的带宽上限
     *
     * @return
     */
    public Integer getBandwidthMbps() {
        return bandwidthMbps;
    }

    /**
     * set 单个公网IP带宽上限，大小不能超过共享带宽包的带宽上限，单位为Mbps，可取值0或-1，0表示禁止流量通过， -1表示关闭单个公网IP带宽限速，默认值为-1，表示公网IP加入共享带宽包后带宽上限默认为共享带宽包的带宽上限
     *
     * @param bandwidthMbps
     */
    public void setBandwidthMbps(Integer bandwidthMbps) {
        this.bandwidthMbps = bandwidthMbps;
    }


    /**
     * set 公网IP ID，当加入弹性公网IP时，为elasticIpId。有如下限制：已欠费的公网IP不能加入共享带宽包；包年包月的公网IP不能加入共享带宽包；一个公网IP同时只能加入一个公网带宽包；公网IP和共享带宽包的地域不同或者线路不同时，公网IP不能加入对应共享带宽包；一个共享带宽包默认可加入20个公网IP
     *
     * @param publicIpId
     */
    public AddBandwidthPackageIPSpec publicIpId(String publicIpId) {
        this.publicIpId = publicIpId;
        return this;
    }

    /**
     * set 单个公网IP带宽上限，大小不能超过共享带宽包的带宽上限，单位为Mbps，可取值0或-1，0表示禁止流量通过， -1表示关闭单个公网IP带宽限速，默认值为-1，表示公网IP加入共享带宽包后带宽上限默认为共享带宽包的带宽上限
     *
     * @param bandwidthMbps
     */
    public AddBandwidthPackageIPSpec bandwidthMbps(Integer bandwidthMbps) {
        this.bandwidthMbps = bandwidthMbps;
        return this;
    }


}