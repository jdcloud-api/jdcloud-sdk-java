/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;

/**
 * 更新Notebook资源属性的参数，支持更新镜像、资源配置、存储、数据集、模型等配置。

## 支持更新的属性
- **镜像配置**: 更换镜像来源和镜像ID
- **工作负载配置**: 更换资源队列、CPU、内存、GPU配置
- **存储配置**: 更新存储空间挂载配置
- **数据集配置**: 更新数据集挂载配置
- **模型配置**: 更新模型挂载配置
- **公网访问配置**: 更新或移除负载均衡配置(仅私有资源池)
- **代码配置**: 更新代码库挂载配置
- **环境变量配置**: 新增、修改、删除或清空用户环境变量
- **节点亲和性配置**: 更新节点调度亲和性规则
- **RDMA配置**: 开启或关闭RDMA高速网络
- **SSH配置**: 更新SSH连接配置

## 使用说明
- 更新操作需要在Notebook停止状态下进行
- 传null的字段表示不修改该属性
- imageSource和imageId必须同时传或者都不传

 */
public class UpdateResourcePropertiesParam  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传

     */
    private String imageSource;

    /**
     * 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传
- 用于指定要更换的镜像

     */
    private String imageId;

    /**
     * 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

     */
    private LbSpec lbSpec;

    /**
     * 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

     */
    private WorkloadSpec workloadSpec;

    /**
     * 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

     */
    
    private List<StorageSpec> storages;
    /**
     * 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

     */
    
    private List<DatasetSpec> datasets;
    /**
     * 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

     */
    
    private List<ModelSpec> models;
    /**
     * 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

     */
    
    private List<NbCodeConfig> codes;
    /**
     * 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

     */
    
    private List<NotebookEnv> envs;
    /**
     * 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

     */
    
    private List<NotebookNodeAffinity> nodeAffinities;
    /**
     * 是否启用RDMA高速网络。

## 更新语义
- 不传或传&#x60;null&#x60;表示不修改原配置
- 传&#x60;true&#x60;表示开启，传&#x60;false&#x60;表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传&#x60;true&#x60;也会静默按&#x60;false&#x60;处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

     */
    private Boolean rdma;

    /**
     * SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

     */
    private SshSpec sshSpec;

    /**
     * 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

     */
    private Integer taskPriority;



    /**
     * get 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传

     *
     * @return
     */
    public String getImageSource() {
        return imageSource;
    }

    /**
     * set 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传

     *
     * @param imageSource
     */
    public void setImageSource(String imageSource) {
        this.imageSource = imageSource;
    }


    /**
     * get 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传
- 用于指定要更换的镜像

     *
     * @return
     */
    public String getImageId() {
        return imageId;
    }

    /**
     * set 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传
- 用于指定要更换的镜像

     *
     * @param imageId
     */
    public void setImageId(String imageId) {
        this.imageId = imageId;
    }


    /**
     * get 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

     *
     * @return
     */
    public LbSpec getLbSpec() {
        return lbSpec;
    }

    /**
     * set 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

     *
     * @param lbSpec
     */
    public void setLbSpec(LbSpec lbSpec) {
        this.lbSpec = lbSpec;
    }


    /**
     * get 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

     *
     * @return
     */
    public WorkloadSpec getWorkloadSpec() {
        return workloadSpec;
    }

    /**
     * set 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

     *
     * @param workloadSpec
     */
    public void setWorkloadSpec(WorkloadSpec workloadSpec) {
        this.workloadSpec = workloadSpec;
    }


    /**
    * get 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

    *
    * @return
    */
    public List<StorageSpec> getStorages() {
        return storages;
    }

    /**
    * set 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

    *
    * @param storages
    */
    public void setStorages(List<StorageSpec> storages) {
        this.storages = storages;
    }


    /**
    * get 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

    *
    * @return
    */
    public List<DatasetSpec> getDatasets() {
        return datasets;
    }

    /**
    * set 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

    *
    * @param datasets
    */
    public void setDatasets(List<DatasetSpec> datasets) {
        this.datasets = datasets;
    }


    /**
    * get 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

    *
    * @return
    */
    public List<ModelSpec> getModels() {
        return models;
    }

    /**
    * set 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

    *
    * @param models
    */
    public void setModels(List<ModelSpec> models) {
        this.models = models;
    }


    /**
    * get 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

    *
    * @return
    */
    public List<NbCodeConfig> getCodes() {
        return codes;
    }

    /**
    * set 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

    *
    * @param codes
    */
    public void setCodes(List<NbCodeConfig> codes) {
        this.codes = codes;
    }


    /**
    * get 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

    *
    * @return
    */
    public List<NotebookEnv> getEnvs() {
        return envs;
    }

    /**
    * set 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

    *
    * @param envs
    */
    public void setEnvs(List<NotebookEnv> envs) {
        this.envs = envs;
    }


    /**
    * get 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

    *
    * @return
    */
    public List<NotebookNodeAffinity> getNodeAffinities() {
        return nodeAffinities;
    }

    /**
    * set 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

    *
    * @param nodeAffinities
    */
    public void setNodeAffinities(List<NotebookNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
    }


    /**
     * get 是否启用RDMA高速网络。

## 更新语义
- 不传或传&#x60;null&#x60;表示不修改原配置
- 传&#x60;true&#x60;表示开启，传&#x60;false&#x60;表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传&#x60;true&#x60;也会静默按&#x60;false&#x60;处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

     *
     * @return
     */
    public Boolean getRdma() {
        return rdma;
    }

    /**
     * set 是否启用RDMA高速网络。

## 更新语义
- 不传或传&#x60;null&#x60;表示不修改原配置
- 传&#x60;true&#x60;表示开启，传&#x60;false&#x60;表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传&#x60;true&#x60;也会静默按&#x60;false&#x60;处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

     *
     * @param rdma
     */
    public void setRdma(Boolean rdma) {
        this.rdma = rdma;
    }


    /**
     * get SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

     *
     * @return
     */
    public SshSpec getSshSpec() {
        return sshSpec;
    }

    /**
     * set SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

     *
     * @param sshSpec
     */
    public void setSshSpec(SshSpec sshSpec) {
        this.sshSpec = sshSpec;
    }


    /**
     * get 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

     *
     * @return
     */
    public Integer getTaskPriority() {
        return taskPriority;
    }

    /**
     * set 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

     *
     * @param taskPriority
     */
    public void setTaskPriority(Integer taskPriority) {
        this.taskPriority = taskPriority;
    }



    /**
     * set 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传

     *
     * @param imageSource 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam imageSource(String imageSource) {
        this.imageSource = imageSource;
        return this;
    }


    /**
     * set 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传
- 用于指定要更换的镜像

     *
     * @param imageId 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数&#x60;imageSource&#x60;和&#x60;imageId&#x60;必须同时传或者都不传
- 用于指定要更换的镜像

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam imageId(String imageId) {
        this.imageId = imageId;
        return this;
    }


    /**
     * set 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

     *
     * @param lbSpec 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam lbSpec(LbSpec lbSpec) {
        this.lbSpec = lbSpec;
        return this;
    }


    /**
     * set 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

     *
     * @param workloadSpec 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam workloadSpec(WorkloadSpec workloadSpec) {
        this.workloadSpec = workloadSpec;
        return this;
    }


    /**
    * set 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

    *
    * @param storages 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam storages(List<StorageSpec> storages) {
        this.storages = storages;
        return this;
    }


    /**
    * set 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

    *
    * @param datasets 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam datasets(List<DatasetSpec> datasets) {
        this.datasets = datasets;
        return this;
    }


    /**
    * set 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

    *
    * @param models 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam models(List<ModelSpec> models) {
        this.models = models;
        return this;
    }


    /**
    * set 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

    *
    * @param codes 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam codes(List<NbCodeConfig> codes) {
        this.codes = codes;
        return this;
    }


    /**
    * set 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

    *
    * @param envs 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam envs(List<NotebookEnv> envs) {
        this.envs = envs;
        return this;
    }


    /**
    * set 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

    *
    * @param nodeAffinities 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

    * @return UpdateResourcePropertiesParam
    */
    public UpdateResourcePropertiesParam nodeAffinities(List<NotebookNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
        return this;
    }


    /**
     * set 是否启用RDMA高速网络。

## 更新语义
- 不传或传&#x60;null&#x60;表示不修改原配置
- 传&#x60;true&#x60;表示开启，传&#x60;false&#x60;表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传&#x60;true&#x60;也会静默按&#x60;false&#x60;处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

     *
     * @param rdma 是否启用RDMA高速网络。

## 更新语义
- 不传或传&#x60;null&#x60;表示不修改原配置
- 传&#x60;true&#x60;表示开启，传&#x60;false&#x60;表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传&#x60;true&#x60;也会静默按&#x60;false&#x60;处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam rdma(Boolean rdma) {
        this.rdma = rdma;
        return this;
    }


    /**
     * set SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

     *
     * @param sshSpec SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam sshSpec(SshSpec sshSpec) {
        this.sshSpec = sshSpec;
        return this;
    }


    /**
     * set 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

     *
     * @param taskPriority 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

     * @return UpdateResourcePropertiesParam
     */
    public UpdateResourcePropertiesParam taskPriority(Integer taskPriority) {
        this.taskPriority = taskPriority;
        return this;
    }



    /**
     * add item to 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到&#x60;/mnt/workspace&#x60;

     *
     * @param storage
     */
    public void addStorage(StorageSpec storage) {
        if (this.storages == null) {
            this.storages = new ArrayList<>();
        }
        this.storages.add(storage);
    }

    /**
     * add item to 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

     *
     * @param dataset
     */
    public void addDataset(DatasetSpec dataset) {
        if (this.datasets == null) {
            this.datasets = new ArrayList<>();
        }
        this.datasets.add(dataset);
    }

    /**
     * add item to 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

     *
     * @param model
     */
    public void addModel(ModelSpec model) {
        if (this.models == null) {
            this.models = new ArrayList<>();
        }
        this.models.add(model);
    }

    /**
     * add item to 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

     *
     * @param code
     */
    public void addCode(NbCodeConfig code) {
        if (this.codes == null) {
            this.codes = new ArrayList<>();
        }
        this.codes.add(code);
    }

    /**
     * add item to 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

     *
     * @param env
     */
    public void addEnv(NotebookEnv env) {
        if (this.envs == null) {
            this.envs = new ArrayList<>();
        }
        this.envs.add(env);
    }

    /**
     * add item to 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

     *
     * @param nodeAffinitie
     */
    public void addNodeAffinitie(NotebookNodeAffinity nodeAffinitie) {
        if (this.nodeAffinities == null) {
            this.nodeAffinities = new ArrayList<>();
        }
        this.nodeAffinities.add(nodeAffinitie);
    }
}