/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;
import com.jdcloud.sdk.annotation.Required;

/**
 * 创建Notebook实例的配置信息，包含实例名称、镜像、资源配置、存储、数据集、模型等完整配置。

## 配置项说明
- **基础配置**: 名称、描述、应用类型
- **镜像配置**: 镜像来源、镜像ID、镜像名称、镜像URL
- **资源配置**: 队列、CPU、内存、GPU等计算资源配置
- **存储配置**: 挂载的存储空间列表
- **数据配置**: 挂载的数据集列表
- **模型配置**: 挂载的模型列表
- **网络配置**: 公网访问配置(LB)、公网出口配置、SSH配置
- **权限配置**: 资源归属权限、资源组
- **高级配置**: 环境变量、节点亲和性、自定义标签、调度优先级

 */
public class NotebookSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * Notebook实例名称。

## 命名规则
- 长度：1~32字符
- 支持字符：中文、数字、大小写字母、英文下划线&quot;_&quot;、中划线&quot;-&quot;、点&quot;.&quot;
- 示例：my-notebook-01

     * Required:true
     */
    @Required
    private String name;

    /**
     * Notebook实例描述信息。

## 描述规则
- 长度：不超过256字符
- 建议包含实例用途、责任人等关键信息

     */
    private String description;

    /**
     * 应用类型，指定Notebook运行的开发环境类型。

## 支持的应用类型
- JupyterLab: JupyterLab交互式开发环境，支持Python、R等多种编程语言

     * Required:true
     */
    @Required
    private String appType;

    /**
     * 镜像来源，指定使用公共镜像还是自定义镜像。

## 支持的镜像来源
- public: 公共镜像，由平台提供的预置镜像
- self: 自定义镜像，用户自己保存或上传的镜像

     * Required:true
     */
    @Required
    private String imageSource;

    /**
     * 镜像ID，用于唯一标识镜像。

## 使用说明
- 公共镜像(public): 镜像ID由平台提供
- 自定义镜像(self): 镜像ID为用户保存镜像时生成的ID

     */
    private String imageId;

    /**
     * 镜像名称，用于显示和识别镜像。
     * Required:true
     */
    @Required
    private String imageName;

    /**
     * 镜像URL地址，完整的镜像仓库地址。

## 格式示例
- registry.example.com:5000/namespace/image:tag

     * Required:true
     */
    @Required
    private String imageUrl;

    /**
     * 负载均衡配置，用于配置Notebook的公网SSH访问能力。

## 使用说明
- **公共资源池**: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB，无需指定lbId/lbPort
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 不需要公网SSH访问时不要指定此参数

     */
    private LbSpec lbSpec;

    /**
     * 公网出口配置，用于配置Notebook的出公网访问方式。

## 使用说明
- switchStatus必须显式传值，为on时开启公网出口，为off时关闭公网出口且egressType无效
- **公共资源池**: switchStatus&#x3D;on时可设置 internetEgress.egressType&#x3D;SHARE_GATEWAY，经平台共享NAT网关出公网
- **私有资源池**: 暂不支持公网出口配置

     */
    private InternetEgressInfo internetEgress;

    /**
     * 工作负载资源配置，定义Notebook的计算资源需求。

## 配置说明
- **公共资源池**: 必须指定规格ID(flavorId)和逻辑可用区编码(logicAzCode)
- **私有资源池**: 必须指定CPU和内存，可选GPU配置

     * Required:true
     */
    @Required
    private WorkloadSpec workloadSpec;

    /**
     * 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

     * Required:true
     */
    @Required
    
    private List<StorageSpec> storages;
    /**
     * 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

     */
    
    private List<DatasetSpec> datasets;
    /**
     * 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

     */
    
    private List<ModelSpec> models;
    /**
     * 工作空间中的资源归属权限，控制资源的可见范围。

## 权限类型
- public: 公开，工作空间中所有用户可见
- private: 私有，仅管理员和拥有者可见

## 默认值
- 默认为public

## 权限说明
- 管理员可查看工作空间中全部资源
- 其他用户只能查看归属自己的private权限的资源或public权限的资源

     */
    private String permission;

    /**
     * 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

     */
    
    private List<NotebookNodeAffinity> nodeAffinities;
    /**
     * 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

     */
    
    private List<NbCodeConfig> codes;
    /**
     * 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

     */
    
    private List<NotebookEnv> envs;
    /**
     * SSH配置，用于配置SSH远程连接能力。

## 配置说明
- 开启SSH后可通过SSH客户端连接Notebook
- 可配置公钥进行免密登录

     */
    private SshSpec sshSpec;

    /**
     * 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

     */
    
    private List<Tag> userTags;
    /**
     * 资源组ID，用于资源分组管理。

## 使用说明
- 将资源归属到指定的资源组
- 便于按资源组进行权限管理和成本核算

     */
    private String resourceGroupId;



    /**
     * get Notebook实例名称。

## 命名规则
- 长度：1~32字符
- 支持字符：中文、数字、大小写字母、英文下划线&quot;_&quot;、中划线&quot;-&quot;、点&quot;.&quot;
- 示例：my-notebook-01

     *
     * @return
     */
    public String getName() {
        return name;
    }

    /**
     * set Notebook实例名称。

## 命名规则
- 长度：1~32字符
- 支持字符：中文、数字、大小写字母、英文下划线&quot;_&quot;、中划线&quot;-&quot;、点&quot;.&quot;
- 示例：my-notebook-01

     *
     * @param name
     */
    public void setName(String name) {
        this.name = name;
    }


    /**
     * get Notebook实例描述信息。

## 描述规则
- 长度：不超过256字符
- 建议包含实例用途、责任人等关键信息

     *
     * @return
     */
    public String getDescription() {
        return description;
    }

    /**
     * set Notebook实例描述信息。

## 描述规则
- 长度：不超过256字符
- 建议包含实例用途、责任人等关键信息

     *
     * @param description
     */
    public void setDescription(String description) {
        this.description = description;
    }


    /**
     * get 应用类型，指定Notebook运行的开发环境类型。

## 支持的应用类型
- JupyterLab: JupyterLab交互式开发环境，支持Python、R等多种编程语言

     *
     * @return
     */
    public String getAppType() {
        return appType;
    }

    /**
     * set 应用类型，指定Notebook运行的开发环境类型。

## 支持的应用类型
- JupyterLab: JupyterLab交互式开发环境，支持Python、R等多种编程语言

     *
     * @param appType
     */
    public void setAppType(String appType) {
        this.appType = appType;
    }


    /**
     * get 镜像来源，指定使用公共镜像还是自定义镜像。

## 支持的镜像来源
- public: 公共镜像，由平台提供的预置镜像
- self: 自定义镜像，用户自己保存或上传的镜像

     *
     * @return
     */
    public String getImageSource() {
        return imageSource;
    }

    /**
     * set 镜像来源，指定使用公共镜像还是自定义镜像。

## 支持的镜像来源
- public: 公共镜像，由平台提供的预置镜像
- self: 自定义镜像，用户自己保存或上传的镜像

     *
     * @param imageSource
     */
    public void setImageSource(String imageSource) {
        this.imageSource = imageSource;
    }


    /**
     * get 镜像ID，用于唯一标识镜像。

## 使用说明
- 公共镜像(public): 镜像ID由平台提供
- 自定义镜像(self): 镜像ID为用户保存镜像时生成的ID

     *
     * @return
     */
    public String getImageId() {
        return imageId;
    }

    /**
     * set 镜像ID，用于唯一标识镜像。

## 使用说明
- 公共镜像(public): 镜像ID由平台提供
- 自定义镜像(self): 镜像ID为用户保存镜像时生成的ID

     *
     * @param imageId
     */
    public void setImageId(String imageId) {
        this.imageId = imageId;
    }


    /**
     * get 镜像名称，用于显示和识别镜像。
     *
     * @return
     */
    public String getImageName() {
        return imageName;
    }

    /**
     * set 镜像名称，用于显示和识别镜像。
     *
     * @param imageName
     */
    public void setImageName(String imageName) {
        this.imageName = imageName;
    }


    /**
     * get 镜像URL地址，完整的镜像仓库地址。

## 格式示例
- registry.example.com:5000/namespace/image:tag

     *
     * @return
     */
    public String getImageUrl() {
        return imageUrl;
    }

    /**
     * set 镜像URL地址，完整的镜像仓库地址。

## 格式示例
- registry.example.com:5000/namespace/image:tag

     *
     * @param imageUrl
     */
    public void setImageUrl(String imageUrl) {
        this.imageUrl = imageUrl;
    }


    /**
     * get 负载均衡配置，用于配置Notebook的公网SSH访问能力。

## 使用说明
- **公共资源池**: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB，无需指定lbId/lbPort
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 不需要公网SSH访问时不要指定此参数

     *
     * @return
     */
    public LbSpec getLbSpec() {
        return lbSpec;
    }

    /**
     * set 负载均衡配置，用于配置Notebook的公网SSH访问能力。

## 使用说明
- **公共资源池**: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB，无需指定lbId/lbPort
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 不需要公网SSH访问时不要指定此参数

     *
     * @param lbSpec
     */
    public void setLbSpec(LbSpec lbSpec) {
        this.lbSpec = lbSpec;
    }


    /**
     * get 公网出口配置，用于配置Notebook的出公网访问方式。

## 使用说明
- switchStatus必须显式传值，为on时开启公网出口，为off时关闭公网出口且egressType无效
- **公共资源池**: switchStatus&#x3D;on时可设置 internetEgress.egressType&#x3D;SHARE_GATEWAY，经平台共享NAT网关出公网
- **私有资源池**: 暂不支持公网出口配置

     *
     * @return
     */
    public InternetEgressInfo getInternetEgress() {
        return internetEgress;
    }

    /**
     * set 公网出口配置，用于配置Notebook的出公网访问方式。

## 使用说明
- switchStatus必须显式传值，为on时开启公网出口，为off时关闭公网出口且egressType无效
- **公共资源池**: switchStatus&#x3D;on时可设置 internetEgress.egressType&#x3D;SHARE_GATEWAY，经平台共享NAT网关出公网
- **私有资源池**: 暂不支持公网出口配置

     *
     * @param internetEgress
     */
    public void setInternetEgress(InternetEgressInfo internetEgress) {
        this.internetEgress = internetEgress;
    }


    /**
     * get 工作负载资源配置，定义Notebook的计算资源需求。

## 配置说明
- **公共资源池**: 必须指定规格ID(flavorId)和逻辑可用区编码(logicAzCode)
- **私有资源池**: 必须指定CPU和内存，可选GPU配置

     *
     * @return
     */
    public WorkloadSpec getWorkloadSpec() {
        return workloadSpec;
    }

    /**
     * set 工作负载资源配置，定义Notebook的计算资源需求。

## 配置说明
- **公共资源池**: 必须指定规格ID(flavorId)和逻辑可用区编码(logicAzCode)
- **私有资源池**: 必须指定CPU和内存，可选GPU配置

     *
     * @param workloadSpec
     */
    public void setWorkloadSpec(WorkloadSpec workloadSpec) {
        this.workloadSpec = workloadSpec;
    }


    /**
    * get 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

    *
    * @return
    */
    public List<StorageSpec> getStorages() {
        return storages;
    }

    /**
    * set 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

    *
    * @param storages
    */
    public void setStorages(List<StorageSpec> storages) {
        this.storages = storages;
    }


    /**
    * get 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

    *
    * @return
    */
    public List<DatasetSpec> getDatasets() {
        return datasets;
    }

    /**
    * set 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

    *
    * @param datasets
    */
    public void setDatasets(List<DatasetSpec> datasets) {
        this.datasets = datasets;
    }


    /**
    * get 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

    *
    * @return
    */
    public List<ModelSpec> getModels() {
        return models;
    }

    /**
    * set 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

    *
    * @param models
    */
    public void setModels(List<ModelSpec> models) {
        this.models = models;
    }


    /**
     * get 工作空间中的资源归属权限，控制资源的可见范围。

## 权限类型
- public: 公开，工作空间中所有用户可见
- private: 私有，仅管理员和拥有者可见

## 默认值
- 默认为public

## 权限说明
- 管理员可查看工作空间中全部资源
- 其他用户只能查看归属自己的private权限的资源或public权限的资源

     *
     * @return
     */
    public String getPermission() {
        return permission;
    }

    /**
     * set 工作空间中的资源归属权限，控制资源的可见范围。

## 权限类型
- public: 公开，工作空间中所有用户可见
- private: 私有，仅管理员和拥有者可见

## 默认值
- 默认为public

## 权限说明
- 管理员可查看工作空间中全部资源
- 其他用户只能查看归属自己的private权限的资源或public权限的资源

     *
     * @param permission
     */
    public void setPermission(String permission) {
        this.permission = permission;
    }


    /**
    * get 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

    *
    * @return
    */
    public List<NotebookNodeAffinity> getNodeAffinities() {
        return nodeAffinities;
    }

    /**
    * set 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

    *
    * @param nodeAffinities
    */
    public void setNodeAffinities(List<NotebookNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
    }


    /**
    * get 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

    *
    * @return
    */
    public List<NbCodeConfig> getCodes() {
        return codes;
    }

    /**
    * set 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

    *
    * @param codes
    */
    public void setCodes(List<NbCodeConfig> codes) {
        this.codes = codes;
    }


    /**
    * get 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

    *
    * @return
    */
    public List<NotebookEnv> getEnvs() {
        return envs;
    }

    /**
    * set 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

    *
    * @param envs
    */
    public void setEnvs(List<NotebookEnv> envs) {
        this.envs = envs;
    }


    /**
     * get SSH配置，用于配置SSH远程连接能力。

## 配置说明
- 开启SSH后可通过SSH客户端连接Notebook
- 可配置公钥进行免密登录

     *
     * @return
     */
    public SshSpec getSshSpec() {
        return sshSpec;
    }

    /**
     * set SSH配置，用于配置SSH远程连接能力。

## 配置说明
- 开启SSH后可通过SSH客户端连接Notebook
- 可配置公钥进行免密登录

     *
     * @param sshSpec
     */
    public void setSshSpec(SshSpec sshSpec) {
        this.sshSpec = sshSpec;
    }


    /**
    * get 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

    *
    * @return
    */
    public List<Tag> getUserTags() {
        return userTags;
    }

    /**
    * set 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

    *
    * @param userTags
    */
    public void setUserTags(List<Tag> userTags) {
        this.userTags = userTags;
    }


    /**
     * get 资源组ID，用于资源分组管理。

## 使用说明
- 将资源归属到指定的资源组
- 便于按资源组进行权限管理和成本核算

     *
     * @return
     */
    public String getResourceGroupId() {
        return resourceGroupId;
    }

    /**
     * set 资源组ID，用于资源分组管理。

## 使用说明
- 将资源归属到指定的资源组
- 便于按资源组进行权限管理和成本核算

     *
     * @param resourceGroupId
     */
    public void setResourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
    }



    /**
     * set Notebook实例名称。

## 命名规则
- 长度：1~32字符
- 支持字符：中文、数字、大小写字母、英文下划线&quot;_&quot;、中划线&quot;-&quot;、点&quot;.&quot;
- 示例：my-notebook-01

     *
     * @param name Notebook实例名称。

## 命名规则
- 长度：1~32字符
- 支持字符：中文、数字、大小写字母、英文下划线&quot;_&quot;、中划线&quot;-&quot;、点&quot;.&quot;
- 示例：my-notebook-01

     * @return NotebookSpec
     */
    public NotebookSpec name(String name) {
        this.name = name;
        return this;
    }


    /**
     * set Notebook实例描述信息。

## 描述规则
- 长度：不超过256字符
- 建议包含实例用途、责任人等关键信息

     *
     * @param description Notebook实例描述信息。

## 描述规则
- 长度：不超过256字符
- 建议包含实例用途、责任人等关键信息

     * @return NotebookSpec
     */
    public NotebookSpec description(String description) {
        this.description = description;
        return this;
    }


    /**
     * set 应用类型，指定Notebook运行的开发环境类型。

## 支持的应用类型
- JupyterLab: JupyterLab交互式开发环境，支持Python、R等多种编程语言

     *
     * @param appType 应用类型，指定Notebook运行的开发环境类型。

## 支持的应用类型
- JupyterLab: JupyterLab交互式开发环境，支持Python、R等多种编程语言

     * @return NotebookSpec
     */
    public NotebookSpec appType(String appType) {
        this.appType = appType;
        return this;
    }


    /**
     * set 镜像来源，指定使用公共镜像还是自定义镜像。

## 支持的镜像来源
- public: 公共镜像，由平台提供的预置镜像
- self: 自定义镜像，用户自己保存或上传的镜像

     *
     * @param imageSource 镜像来源，指定使用公共镜像还是自定义镜像。

## 支持的镜像来源
- public: 公共镜像，由平台提供的预置镜像
- self: 自定义镜像，用户自己保存或上传的镜像

     * @return NotebookSpec
     */
    public NotebookSpec imageSource(String imageSource) {
        this.imageSource = imageSource;
        return this;
    }


    /**
     * set 镜像ID，用于唯一标识镜像。

## 使用说明
- 公共镜像(public): 镜像ID由平台提供
- 自定义镜像(self): 镜像ID为用户保存镜像时生成的ID

     *
     * @param imageId 镜像ID，用于唯一标识镜像。

## 使用说明
- 公共镜像(public): 镜像ID由平台提供
- 自定义镜像(self): 镜像ID为用户保存镜像时生成的ID

     * @return NotebookSpec
     */
    public NotebookSpec imageId(String imageId) {
        this.imageId = imageId;
        return this;
    }


    /**
     * set 镜像名称，用于显示和识别镜像。
     *
     * @param imageName 镜像名称，用于显示和识别镜像。
     * @return NotebookSpec
     */
    public NotebookSpec imageName(String imageName) {
        this.imageName = imageName;
        return this;
    }


    /**
     * set 镜像URL地址，完整的镜像仓库地址。

## 格式示例
- registry.example.com:5000/namespace/image:tag

     *
     * @param imageUrl 镜像URL地址，完整的镜像仓库地址。

## 格式示例
- registry.example.com:5000/namespace/image:tag

     * @return NotebookSpec
     */
    public NotebookSpec imageUrl(String imageUrl) {
        this.imageUrl = imageUrl;
        return this;
    }


    /**
     * set 负载均衡配置，用于配置Notebook的公网SSH访问能力。

## 使用说明
- **公共资源池**: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB，无需指定lbId/lbPort
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 不需要公网SSH访问时不要指定此参数

     *
     * @param lbSpec 负载均衡配置，用于配置Notebook的公网SSH访问能力。

## 使用说明
- **公共资源池**: 可通过lbEnable&#x3D;true开启公网访问，系统自动分配LB，无需指定lbId/lbPort
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 不需要公网SSH访问时不要指定此参数

     * @return NotebookSpec
     */
    public NotebookSpec lbSpec(LbSpec lbSpec) {
        this.lbSpec = lbSpec;
        return this;
    }


    /**
     * set 公网出口配置，用于配置Notebook的出公网访问方式。

## 使用说明
- switchStatus必须显式传值，为on时开启公网出口，为off时关闭公网出口且egressType无效
- **公共资源池**: switchStatus&#x3D;on时可设置 internetEgress.egressType&#x3D;SHARE_GATEWAY，经平台共享NAT网关出公网
- **私有资源池**: 暂不支持公网出口配置

     *
     * @param internetEgress 公网出口配置，用于配置Notebook的出公网访问方式。

## 使用说明
- switchStatus必须显式传值，为on时开启公网出口，为off时关闭公网出口且egressType无效
- **公共资源池**: switchStatus&#x3D;on时可设置 internetEgress.egressType&#x3D;SHARE_GATEWAY，经平台共享NAT网关出公网
- **私有资源池**: 暂不支持公网出口配置

     * @return NotebookSpec
     */
    public NotebookSpec internetEgress(InternetEgressInfo internetEgress) {
        this.internetEgress = internetEgress;
        return this;
    }


    /**
     * set 工作负载资源配置，定义Notebook的计算资源需求。

## 配置说明
- **公共资源池**: 必须指定规格ID(flavorId)和逻辑可用区编码(logicAzCode)
- **私有资源池**: 必须指定CPU和内存，可选GPU配置

     *
     * @param workloadSpec 工作负载资源配置，定义Notebook的计算资源需求。

## 配置说明
- **公共资源池**: 必须指定规格ID(flavorId)和逻辑可用区编码(logicAzCode)
- **私有资源池**: 必须指定CPU和内存，可选GPU配置

     * @return NotebookSpec
     */
    public NotebookSpec workloadSpec(WorkloadSpec workloadSpec) {
        this.workloadSpec = workloadSpec;
        return this;
    }


    /**
    * set 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

    *
    * @param storages 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

    * @return NotebookSpec
    */
    public NotebookSpec storages(List<StorageSpec> storages) {
        this.storages = storages;
        return this;
    }


    /**
    * set 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

    *
    * @param datasets 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

    * @return NotebookSpec
    */
    public NotebookSpec datasets(List<DatasetSpec> datasets) {
        this.datasets = datasets;
        return this;
    }


    /**
    * set 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

    *
    * @param models 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

    * @return NotebookSpec
    */
    public NotebookSpec models(List<ModelSpec> models) {
        this.models = models;
        return this;
    }


    /**
     * set 工作空间中的资源归属权限，控制资源的可见范围。

## 权限类型
- public: 公开，工作空间中所有用户可见
- private: 私有，仅管理员和拥有者可见

## 默认值
- 默认为public

## 权限说明
- 管理员可查看工作空间中全部资源
- 其他用户只能查看归属自己的private权限的资源或public权限的资源

     *
     * @param permission 工作空间中的资源归属权限，控制资源的可见范围。

## 权限类型
- public: 公开，工作空间中所有用户可见
- private: 私有，仅管理员和拥有者可见

## 默认值
- 默认为public

## 权限说明
- 管理员可查看工作空间中全部资源
- 其他用户只能查看归属自己的private权限的资源或public权限的资源

     * @return NotebookSpec
     */
    public NotebookSpec permission(String permission) {
        this.permission = permission;
        return this;
    }


    /**
    * set 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

    *
    * @param nodeAffinities 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

    * @return NotebookSpec
    */
    public NotebookSpec nodeAffinities(List<NotebookNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
        return this;
    }


    /**
    * set 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

    *
    * @param codes 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

    * @return NotebookSpec
    */
    public NotebookSpec codes(List<NbCodeConfig> codes) {
        this.codes = codes;
        return this;
    }


    /**
    * set 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

    *
    * @param envs 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

    * @return NotebookSpec
    */
    public NotebookSpec envs(List<NotebookEnv> envs) {
        this.envs = envs;
        return this;
    }


    /**
     * set SSH配置，用于配置SSH远程连接能力。

## 配置说明
- 开启SSH后可通过SSH客户端连接Notebook
- 可配置公钥进行免密登录

     *
     * @param sshSpec SSH配置，用于配置SSH远程连接能力。

## 配置说明
- 开启SSH后可通过SSH客户端连接Notebook
- 可配置公钥进行免密登录

     * @return NotebookSpec
     */
    public NotebookSpec sshSpec(SshSpec sshSpec) {
        this.sshSpec = sshSpec;
        return this;
    }


    /**
    * set 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

    *
    * @param userTags 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

    * @return NotebookSpec
    */
    public NotebookSpec userTags(List<Tag> userTags) {
        this.userTags = userTags;
        return this;
    }


    /**
     * set 资源组ID，用于资源分组管理。

## 使用说明
- 将资源归属到指定的资源组
- 便于按资源组进行权限管理和成本核算

     *
     * @param resourceGroupId 资源组ID，用于资源分组管理。

## 使用说明
- 将资源归属到指定的资源组
- 便于按资源组进行权限管理和成本核算

     * @return NotebookSpec
     */
    public NotebookSpec resourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
        return this;
    }



    /**
     * add item to 存储空间配置列表，定义Notebook挂载的存储资源。

## 配置说明
- 第一块存储默认作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 支持cfs、oss、jpfs三种存储类型
- 列表顺序决定挂载顺序

     *
     * @param storage
     */
    public void addStorage(StorageSpec storage) {
        if (this.storages == null) {
            this.storages = new ArrayList<>();
        }
        this.storages.add(storage);
    }

    /**
     * add item to 数据集配置列表，定义Notebook挂载的数据集资源。

## 配置说明
- 支持公共数据集和个人数据集
- 可选择数据集版本
- 建议挂载到&#x60;/mnt/datasets/&#x60;路径下

     *
     * @param dataset
     */
    public void addDataset(DatasetSpec dataset) {
        if (this.datasets == null) {
            this.datasets = new ArrayList<>();
        }
        this.datasets.add(dataset);
    }

    /**
     * add item to 模型配置列表，定义Notebook挂载的模型资源。

## 配置说明
- 支持公共模型和个人模型
- 可选择模型版本
- 建议挂载到&#x60;/mnt/models/&#x60;路径下

     *
     * @param model
     */
    public void addModel(ModelSpec model) {
        if (this.models == null) {
            this.models = new ArrayList<>();
        }
        this.models.add(model);
    }

    /**
     * add item to 节点亲和性配置，控制Pod调度到特定节点。

## 配置说明
- 支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系
- 适用于需要调度到特定硬件或标签节点的场景
- 例如：调度到特定GPU型号的节点

## 限制
- 公共资源池不支持设置节点亲和性，仅私有资源池有效

     *
     * @param nodeAffinitie
     */
    public void addNodeAffinitie(NotebookNodeAffinity nodeAffinitie) {
        if (this.nodeAffinities == null) {
            this.nodeAffinities = new ArrayList<>();
        }
        this.nodeAffinities.add(nodeAffinitie);
    }

    /**
     * add item to 代码库配置列表，定义Notebook挂载的代码仓库。

## 配置说明
- 支持Git代码仓库
- 需要预先在工作空间中配置代码库
- 指定挂载路径后代码会自动拉取到指定目录

     *
     * @param code
     */
    public void addCode(NbCodeConfig code) {
        if (this.codes == null) {
            this.codes = new ArrayList<>();
        }
        this.codes.add(code);
    }

    /**
     * add item to 用户环境变量列表。该参数可选，未配置时不注入用户环境变量。

## 配置规则
- 最多支持100个环境变量
- 变量名仅支持大小写字母、数字和下划线，必须以字母或下划线开头，最长255个字符
- 单个变量值最大2048字节，全部用户环境变量总量最大32768字节
- 用户变量名不能重复，变量名区分大小写
- 与平台系统变量同名时允许配置，Notebook运行时以平台系统变量为准
- 请勿使用环境变量保存密码、密钥或Token等敏感信息

     *
     * @param env
     */
    public void addEnv(NotebookEnv env) {
        if (this.envs == null) {
            this.envs = new ArrayList<>();
        }
        this.envs.add(env);
    }

    /**
     * add item to 自定义实例标签，用于资源分类和管理。

## 标签规则
- 以key-value键值对形式指定
- 最多支持10个标签
- key不能以 &quot;jrn:&quot; 或&quot;jdc-&quot;开头
- 仅支持中文、大/小写英文、数字及如下符号：&#x60;\_.,:\/&#x3D;+-@&#x60;
- 示例：{&quot;env&quot;: &quot;dev&quot;, &quot;team&quot;: &quot;ai&quot;}

     *
     * @param userTag
     */
    public void addUserTag(Tag userTag) {
        if (this.userTags == null) {
            this.userTags = new ArrayList<>();
        }
        this.userTags.add(userTag);
    }
}