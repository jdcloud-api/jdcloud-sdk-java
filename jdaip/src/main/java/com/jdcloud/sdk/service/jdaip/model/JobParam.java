/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;
import com.jdcloud.sdk.annotation.Required;

/**
 * 创建训练任务需要的参数。

训练任务支持两种框架类型：PyTorch 和 Ray。根据框架类型的不同，资源配置方式有所差异：
- **PyTorch**：使用 &#x60;resource&#x60; 字段配置统一的节点资源，或使用 &#x60;roleResource&#x60; 配置分布式训练的不同角色
- **Ray**：必须使用 &#x60;roleResource&#x60; 配置 Head 和 Worker 角色

 */
public class JobParam  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 训练任务名称。

**命名规则：**
- 长度：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线 &quot;-&quot; 和英文下划线 &quot;_&quot;
- 同一工作空间内名称不能重复

**示例：**
- &#x60;mnist-training-001&#x60;
- &#x60;LLaMA预训练任务&#x60;

     * Required:true
     */
    @Required
    private String name;

    /**
     * 训练任务的描述信息。

**限制：** 不超过256个字符

**建议：** 简要说明训练目的、数据集、模型等关键信息

     */
    private String description;

    /**
     * 镜像可见性，决定镜像的使用权限范围。

**可选值：**
- &#x60;public&#x60;：表示产品页面[资产管理-镜像]对应的公共镜像，平台预置镜像，所有用户可使用
- &#x60;private&#x60;：表示产品页面[资产管理-镜像]对应的我的镜像

     * Required:true
     */
    @Required
    private String imageVisibility;

    /**
     * 镜像ID，用于指定训练环境的基础镜像。

**示例：** &#x60;img-9axxxxxxjh&#x60;

     * Required:true
     */
    @Required
    private String imageId;

    /**
     * 镜像完整地址，包含镜像仓库地址和标签。

**格式：** &#x60;registry地址/命名空间/镜像名:标签&#x60;

**示例：**
- &#x60;registry.jdcloud.com/jdaip/pytorch:2.0-cuda11.8&#x60;
- &#x60;registry.jdcloud.com/custom/my-training:v1.0&#x60;

     * Required:true
     */
    @Required
    private String imageUrl;

    /**
     * 训练任务类型，决定底层调度框架和资源管理方式。

**可选值：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习

**注意：** 大小写敏感，必须使用小写

     * Required:true
     */
    @Required
    private String jobType;

    /**
     * 启动命令，指定训练脚本的执行方式。

**常用格式：**
- Python 脚本：&#x60;python /mnt/ws/train.py [参数]&#x60;
- Shell 脚本：&#x60;bash /mnt/ws/start.sh&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/train.py&#x60;

**注意事项：**
- 路径需与存储挂载路径对应
- 支持传入命令行参数
- 建议使用绝对路径

**示例：**
- 单机训练：&#x60;python /mnt/ws/train.py --epochs 100 --lr 0.001&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/distributed_train.py&#x60;
- 使用环境变量：&#x60;python /mnt/ws/train.py --data_dir $DATA_DIR&#x60;

     * Required:true
     */
    @Required
    private String command;

    /**
     * **已废弃：** 请参考 &#x60;roleResource&#x60; 字段

     */
    private Integer replica;

    /**
     * 任务优先级，范围[1, 9]; 当队列开启优先级调度时生效
     */
    private Integer taskPriority;

    /**
     * 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

     */
    
    private List<EnvParamForJob> envs;
    /**
     * **已废弃：** 请使用 &#x60;roleResource&#x60; 字段，支持更灵活的角色资源配置

     */
    private ResourceParamForJob resource;

    /**
     * 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

     */
    
    private List<StorageSpaceParamForJob> storageSpaces;
    /**
     * 本地存储挂载配置，与 &#x60;storageSpaces&#x60; / &#x60;datasets&#x60; / &#x60;models&#x60; 平级。

在训练节点本地高速盘上为训练容器分配一块临时空间，仅支持专属资源池，每个任务最多一个。
字段定义见 &#x60;localStorageParamForJob&#x60;。未开启时不传本字段。

     */
    private LocalStorageParamForJob localStorage;

    /**
     * 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

     */
    
    private List<DatasetParamForJob> datasets;
    /**
     * 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

     */
    
    private List<ModelParamForJob> models;
    /**
     * 角色资源配置信息，用于配置不同角色的节点资源。

**适用场景：**
- Ray 分布式训练（必须配置 Head 和 Worker）
- PyTorch 分布式训练（可配置不同规格的 Worker）

     */
    private RoleResourceParamForJob roleResource;

    /**
     * 出公网配置（任务级，仅公共资源池训练任务生效）。不需要出公网时不传此参数。

     */
    private InternetEgressForJob internetEgress;

    /**
     * 框架高级配置，JSON 格式字符串。

**适用框架：** 仅支持 Ray 框架

**使用场景：**
- 配置 Ray 集群环境变量
- 自定义 NCCL、CUDA 等参数

**示例：**
  {&quot;ray&quot;: {&quot;env_vars&quot;: {&quot;NCCL_DEBUG&quot;: &quot;INFO&quot;, &quot;NCCL_IB_DISABLE&quot;: &quot;0&quot;, &quot;NCCL_PROTO&quot;: &quot;simple&quot;}}}

     */
    private String advancedConfig;

    /**
     * 重启策略配置，定义任务异常时的自动重启行为。

**默认行为：** 禁用自动重启

**推荐场景：**
- 长时间训练任务：建议启用，应对节点故障
- 快速实验任务：可不启用，快速发现问题

     */
    private RestartPolicyParamForJob restartPolicy;

    /**
     * 算力健康检测策略，用于检测GPU等算力资源是否正常。

**检测内容：**
- GPU 是否可用
- GPU 驱动是否正常
- CUDA 环境是否正常
- 高速网络、网卡是否正常
- 其他环境配置等

**默认行为：** 禁用健康检测

     */
    private HealthCheckPolicyParamForJob healthCheckPolicy;

    /**
     * 工作空间中的资源归属权限。

**可选值：**
- &#x60;public&#x60;：公开资源，工作空间所有成员可见可操作
- &#x60;private&#x60;：私有资源，仅创建者和空间管理员可见可操作

**默认值：** &#x60;public&#x60;

     */
    private String permission;

    /**
     * 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

     */
    
    private List<JobNodeAffinity> nodeAffinities;
    /**
     * 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

     */
    
    private List<CodeParam> codes;
    /**
     * 公共池排队超时时间，单位：分钟。

**取值范围：** 5 ~ 1440

**默认值：** 5

**说明：**
- 仅公共资源池训练任务生效
- 排队超过此时间后，任务将自动回滚为创建失败

     */
    private Integer queuingTimeoutMinutes;

    /**
     * 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

     */
    
    private List<JobTag> userTags;
    /**
     * 资源组ID，用于将任务归属到特定的资源组进行管理。

**使用场景：**
- 按项目隔离资源
- 按团队分配资源配额
- 资源使用统计和计费

     */
    private String resourceGroupId;



    /**
     * get 训练任务名称。

**命名规则：**
- 长度：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线 &quot;-&quot; 和英文下划线 &quot;_&quot;
- 同一工作空间内名称不能重复

**示例：**
- &#x60;mnist-training-001&#x60;
- &#x60;LLaMA预训练任务&#x60;

     *
     * @return
     */
    public String getName() {
        return name;
    }

    /**
     * set 训练任务名称。

**命名规则：**
- 长度：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线 &quot;-&quot; 和英文下划线 &quot;_&quot;
- 同一工作空间内名称不能重复

**示例：**
- &#x60;mnist-training-001&#x60;
- &#x60;LLaMA预训练任务&#x60;

     *
     * @param name
     */
    public void setName(String name) {
        this.name = name;
    }


    /**
     * get 训练任务的描述信息。

**限制：** 不超过256个字符

**建议：** 简要说明训练目的、数据集、模型等关键信息

     *
     * @return
     */
    public String getDescription() {
        return description;
    }

    /**
     * set 训练任务的描述信息。

**限制：** 不超过256个字符

**建议：** 简要说明训练目的、数据集、模型等关键信息

     *
     * @param description
     */
    public void setDescription(String description) {
        this.description = description;
    }


    /**
     * get 镜像可见性，决定镜像的使用权限范围。

**可选值：**
- &#x60;public&#x60;：表示产品页面[资产管理-镜像]对应的公共镜像，平台预置镜像，所有用户可使用
- &#x60;private&#x60;：表示产品页面[资产管理-镜像]对应的我的镜像

     *
     * @return
     */
    public String getImageVisibility() {
        return imageVisibility;
    }

    /**
     * set 镜像可见性，决定镜像的使用权限范围。

**可选值：**
- &#x60;public&#x60;：表示产品页面[资产管理-镜像]对应的公共镜像，平台预置镜像，所有用户可使用
- &#x60;private&#x60;：表示产品页面[资产管理-镜像]对应的我的镜像

     *
     * @param imageVisibility
     */
    public void setImageVisibility(String imageVisibility) {
        this.imageVisibility = imageVisibility;
    }


    /**
     * get 镜像ID，用于指定训练环境的基础镜像。

**示例：** &#x60;img-9axxxxxxjh&#x60;

     *
     * @return
     */
    public String getImageId() {
        return imageId;
    }

    /**
     * set 镜像ID，用于指定训练环境的基础镜像。

**示例：** &#x60;img-9axxxxxxjh&#x60;

     *
     * @param imageId
     */
    public void setImageId(String imageId) {
        this.imageId = imageId;
    }


    /**
     * get 镜像完整地址，包含镜像仓库地址和标签。

**格式：** &#x60;registry地址/命名空间/镜像名:标签&#x60;

**示例：**
- &#x60;registry.jdcloud.com/jdaip/pytorch:2.0-cuda11.8&#x60;
- &#x60;registry.jdcloud.com/custom/my-training:v1.0&#x60;

     *
     * @return
     */
    public String getImageUrl() {
        return imageUrl;
    }

    /**
     * set 镜像完整地址，包含镜像仓库地址和标签。

**格式：** &#x60;registry地址/命名空间/镜像名:标签&#x60;

**示例：**
- &#x60;registry.jdcloud.com/jdaip/pytorch:2.0-cuda11.8&#x60;
- &#x60;registry.jdcloud.com/custom/my-training:v1.0&#x60;

     *
     * @param imageUrl
     */
    public void setImageUrl(String imageUrl) {
        this.imageUrl = imageUrl;
    }


    /**
     * get 训练任务类型，决定底层调度框架和资源管理方式。

**可选值：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习

**注意：** 大小写敏感，必须使用小写

     *
     * @return
     */
    public String getJobType() {
        return jobType;
    }

    /**
     * set 训练任务类型，决定底层调度框架和资源管理方式。

**可选值：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习

**注意：** 大小写敏感，必须使用小写

     *
     * @param jobType
     */
    public void setJobType(String jobType) {
        this.jobType = jobType;
    }


    /**
     * get 启动命令，指定训练脚本的执行方式。

**常用格式：**
- Python 脚本：&#x60;python /mnt/ws/train.py [参数]&#x60;
- Shell 脚本：&#x60;bash /mnt/ws/start.sh&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/train.py&#x60;

**注意事项：**
- 路径需与存储挂载路径对应
- 支持传入命令行参数
- 建议使用绝对路径

**示例：**
- 单机训练：&#x60;python /mnt/ws/train.py --epochs 100 --lr 0.001&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/distributed_train.py&#x60;
- 使用环境变量：&#x60;python /mnt/ws/train.py --data_dir $DATA_DIR&#x60;

     *
     * @return
     */
    public String getCommand() {
        return command;
    }

    /**
     * set 启动命令，指定训练脚本的执行方式。

**常用格式：**
- Python 脚本：&#x60;python /mnt/ws/train.py [参数]&#x60;
- Shell 脚本：&#x60;bash /mnt/ws/start.sh&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/train.py&#x60;

**注意事项：**
- 路径需与存储挂载路径对应
- 支持传入命令行参数
- 建议使用绝对路径

**示例：**
- 单机训练：&#x60;python /mnt/ws/train.py --epochs 100 --lr 0.001&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/distributed_train.py&#x60;
- 使用环境变量：&#x60;python /mnt/ws/train.py --data_dir $DATA_DIR&#x60;

     *
     * @param command
     */
    public void setCommand(String command) {
        this.command = command;
    }


    /**
     * get **已废弃：** 请参考 &#x60;roleResource&#x60; 字段

     *
     * @return
     */
    public Integer getReplica() {
        return replica;
    }

    /**
     * set **已废弃：** 请参考 &#x60;roleResource&#x60; 字段

     *
     * @param replica
     */
    public void setReplica(Integer replica) {
        this.replica = replica;
    }


    /**
     * get 任务优先级，范围[1, 9]; 当队列开启优先级调度时生效
     *
     * @return
     */
    public Integer getTaskPriority() {
        return taskPriority;
    }

    /**
     * set 任务优先级，范围[1, 9]; 当队列开启优先级调度时生效
     *
     * @param taskPriority
     */
    public void setTaskPriority(Integer taskPriority) {
        this.taskPriority = taskPriority;
    }


    /**
    * get 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

    *
    * @return
    */
    public List<EnvParamForJob> getEnvs() {
        return envs;
    }

    /**
    * set 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

    *
    * @param envs
    */
    public void setEnvs(List<EnvParamForJob> envs) {
        this.envs = envs;
    }


    /**
     * get **已废弃：** 请使用 &#x60;roleResource&#x60; 字段，支持更灵活的角色资源配置

     *
     * @return
     */
    public ResourceParamForJob getResource() {
        return resource;
    }

    /**
     * set **已废弃：** 请使用 &#x60;roleResource&#x60; 字段，支持更灵活的角色资源配置

     *
     * @param resource
     */
    public void setResource(ResourceParamForJob resource) {
        this.resource = resource;
    }


    /**
    * get 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

    *
    * @return
    */
    public List<StorageSpaceParamForJob> getStorageSpaces() {
        return storageSpaces;
    }

    /**
    * set 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

    *
    * @param storageSpaces
    */
    public void setStorageSpaces(List<StorageSpaceParamForJob> storageSpaces) {
        this.storageSpaces = storageSpaces;
    }


    /**
     * get 本地存储挂载配置，与 &#x60;storageSpaces&#x60; / &#x60;datasets&#x60; / &#x60;models&#x60; 平级。

在训练节点本地高速盘上为训练容器分配一块临时空间，仅支持专属资源池，每个任务最多一个。
字段定义见 &#x60;localStorageParamForJob&#x60;。未开启时不传本字段。

     *
     * @return
     */
    public LocalStorageParamForJob getLocalStorage() {
        return localStorage;
    }

    /**
     * set 本地存储挂载配置，与 &#x60;storageSpaces&#x60; / &#x60;datasets&#x60; / &#x60;models&#x60; 平级。

在训练节点本地高速盘上为训练容器分配一块临时空间，仅支持专属资源池，每个任务最多一个。
字段定义见 &#x60;localStorageParamForJob&#x60;。未开启时不传本字段。

     *
     * @param localStorage
     */
    public void setLocalStorage(LocalStorageParamForJob localStorage) {
        this.localStorage = localStorage;
    }


    /**
    * get 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

    *
    * @return
    */
    public List<DatasetParamForJob> getDatasets() {
        return datasets;
    }

    /**
    * set 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

    *
    * @param datasets
    */
    public void setDatasets(List<DatasetParamForJob> datasets) {
        this.datasets = datasets;
    }


    /**
    * get 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

    *
    * @return
    */
    public List<ModelParamForJob> getModels() {
        return models;
    }

    /**
    * set 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

    *
    * @param models
    */
    public void setModels(List<ModelParamForJob> models) {
        this.models = models;
    }


    /**
     * get 角色资源配置信息，用于配置不同角色的节点资源。

**适用场景：**
- Ray 分布式训练（必须配置 Head 和 Worker）
- PyTorch 分布式训练（可配置不同规格的 Worker）

     *
     * @return
     */
    public RoleResourceParamForJob getRoleResource() {
        return roleResource;
    }

    /**
     * set 角色资源配置信息，用于配置不同角色的节点资源。

**适用场景：**
- Ray 分布式训练（必须配置 Head 和 Worker）
- PyTorch 分布式训练（可配置不同规格的 Worker）

     *
     * @param roleResource
     */
    public void setRoleResource(RoleResourceParamForJob roleResource) {
        this.roleResource = roleResource;
    }


    /**
     * get 出公网配置（任务级，仅公共资源池训练任务生效）。不需要出公网时不传此参数。

     *
     * @return
     */
    public InternetEgressForJob getInternetEgress() {
        return internetEgress;
    }

    /**
     * set 出公网配置（任务级，仅公共资源池训练任务生效）。不需要出公网时不传此参数。

     *
     * @param internetEgress
     */
    public void setInternetEgress(InternetEgressForJob internetEgress) {
        this.internetEgress = internetEgress;
    }


    /**
     * get 框架高级配置，JSON 格式字符串。

**适用框架：** 仅支持 Ray 框架

**使用场景：**
- 配置 Ray 集群环境变量
- 自定义 NCCL、CUDA 等参数

**示例：**
  {&quot;ray&quot;: {&quot;env_vars&quot;: {&quot;NCCL_DEBUG&quot;: &quot;INFO&quot;, &quot;NCCL_IB_DISABLE&quot;: &quot;0&quot;, &quot;NCCL_PROTO&quot;: &quot;simple&quot;}}}

     *
     * @return
     */
    public String getAdvancedConfig() {
        return advancedConfig;
    }

    /**
     * set 框架高级配置，JSON 格式字符串。

**适用框架：** 仅支持 Ray 框架

**使用场景：**
- 配置 Ray 集群环境变量
- 自定义 NCCL、CUDA 等参数

**示例：**
  {&quot;ray&quot;: {&quot;env_vars&quot;: {&quot;NCCL_DEBUG&quot;: &quot;INFO&quot;, &quot;NCCL_IB_DISABLE&quot;: &quot;0&quot;, &quot;NCCL_PROTO&quot;: &quot;simple&quot;}}}

     *
     * @param advancedConfig
     */
    public void setAdvancedConfig(String advancedConfig) {
        this.advancedConfig = advancedConfig;
    }


    /**
     * get 重启策略配置，定义任务异常时的自动重启行为。

**默认行为：** 禁用自动重启

**推荐场景：**
- 长时间训练任务：建议启用，应对节点故障
- 快速实验任务：可不启用，快速发现问题

     *
     * @return
     */
    public RestartPolicyParamForJob getRestartPolicy() {
        return restartPolicy;
    }

    /**
     * set 重启策略配置，定义任务异常时的自动重启行为。

**默认行为：** 禁用自动重启

**推荐场景：**
- 长时间训练任务：建议启用，应对节点故障
- 快速实验任务：可不启用，快速发现问题

     *
     * @param restartPolicy
     */
    public void setRestartPolicy(RestartPolicyParamForJob restartPolicy) {
        this.restartPolicy = restartPolicy;
    }


    /**
     * get 算力健康检测策略，用于检测GPU等算力资源是否正常。

**检测内容：**
- GPU 是否可用
- GPU 驱动是否正常
- CUDA 环境是否正常
- 高速网络、网卡是否正常
- 其他环境配置等

**默认行为：** 禁用健康检测

     *
     * @return
     */
    public HealthCheckPolicyParamForJob getHealthCheckPolicy() {
        return healthCheckPolicy;
    }

    /**
     * set 算力健康检测策略，用于检测GPU等算力资源是否正常。

**检测内容：**
- GPU 是否可用
- GPU 驱动是否正常
- CUDA 环境是否正常
- 高速网络、网卡是否正常
- 其他环境配置等

**默认行为：** 禁用健康检测

     *
     * @param healthCheckPolicy
     */
    public void setHealthCheckPolicy(HealthCheckPolicyParamForJob healthCheckPolicy) {
        this.healthCheckPolicy = healthCheckPolicy;
    }


    /**
     * get 工作空间中的资源归属权限。

**可选值：**
- &#x60;public&#x60;：公开资源，工作空间所有成员可见可操作
- &#x60;private&#x60;：私有资源，仅创建者和空间管理员可见可操作

**默认值：** &#x60;public&#x60;

     *
     * @return
     */
    public String getPermission() {
        return permission;
    }

    /**
     * set 工作空间中的资源归属权限。

**可选值：**
- &#x60;public&#x60;：公开资源，工作空间所有成员可见可操作
- &#x60;private&#x60;：私有资源，仅创建者和空间管理员可见可操作

**默认值：** &#x60;public&#x60;

     *
     * @param permission
     */
    public void setPermission(String permission) {
        this.permission = permission;
    }


    /**
    * get 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

    *
    * @return
    */
    public List<JobNodeAffinity> getNodeAffinities() {
        return nodeAffinities;
    }

    /**
    * set 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

    *
    * @param nodeAffinities
    */
    public void setNodeAffinities(List<JobNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
    }


    /**
    * get 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

    *
    * @return
    */
    public List<CodeParam> getCodes() {
        return codes;
    }

    /**
    * set 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

    *
    * @param codes
    */
    public void setCodes(List<CodeParam> codes) {
        this.codes = codes;
    }


    /**
     * get 公共池排队超时时间，单位：分钟。

**取值范围：** 5 ~ 1440

**默认值：** 5

**说明：**
- 仅公共资源池训练任务生效
- 排队超过此时间后，任务将自动回滚为创建失败

     *
     * @return
     */
    public Integer getQueuingTimeoutMinutes() {
        return queuingTimeoutMinutes;
    }

    /**
     * set 公共池排队超时时间，单位：分钟。

**取值范围：** 5 ~ 1440

**默认值：** 5

**说明：**
- 仅公共资源池训练任务生效
- 排队超过此时间后，任务将自动回滚为创建失败

     *
     * @param queuingTimeoutMinutes
     */
    public void setQueuingTimeoutMinutes(Integer queuingTimeoutMinutes) {
        this.queuingTimeoutMinutes = queuingTimeoutMinutes;
    }


    /**
    * get 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

    *
    * @return
    */
    public List<JobTag> getUserTags() {
        return userTags;
    }

    /**
    * set 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

    *
    * @param userTags
    */
    public void setUserTags(List<JobTag> userTags) {
        this.userTags = userTags;
    }


    /**
     * get 资源组ID，用于将任务归属到特定的资源组进行管理。

**使用场景：**
- 按项目隔离资源
- 按团队分配资源配额
- 资源使用统计和计费

     *
     * @return
     */
    public String getResourceGroupId() {
        return resourceGroupId;
    }

    /**
     * set 资源组ID，用于将任务归属到特定的资源组进行管理。

**使用场景：**
- 按项目隔离资源
- 按团队分配资源配额
- 资源使用统计和计费

     *
     * @param resourceGroupId
     */
    public void setResourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
    }



    /**
     * set 训练任务名称。

**命名规则：**
- 长度：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线 &quot;-&quot; 和英文下划线 &quot;_&quot;
- 同一工作空间内名称不能重复

**示例：**
- &#x60;mnist-training-001&#x60;
- &#x60;LLaMA预训练任务&#x60;

     *
     * @param name 训练任务名称。

**命名规则：**
- 长度：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线 &quot;-&quot; 和英文下划线 &quot;_&quot;
- 同一工作空间内名称不能重复

**示例：**
- &#x60;mnist-training-001&#x60;
- &#x60;LLaMA预训练任务&#x60;

     * @return JobParam
     */
    public JobParam name(String name) {
        this.name = name;
        return this;
    }


    /**
     * set 训练任务的描述信息。

**限制：** 不超过256个字符

**建议：** 简要说明训练目的、数据集、模型等关键信息

     *
     * @param description 训练任务的描述信息。

**限制：** 不超过256个字符

**建议：** 简要说明训练目的、数据集、模型等关键信息

     * @return JobParam
     */
    public JobParam description(String description) {
        this.description = description;
        return this;
    }


    /**
     * set 镜像可见性，决定镜像的使用权限范围。

**可选值：**
- &#x60;public&#x60;：表示产品页面[资产管理-镜像]对应的公共镜像，平台预置镜像，所有用户可使用
- &#x60;private&#x60;：表示产品页面[资产管理-镜像]对应的我的镜像

     *
     * @param imageVisibility 镜像可见性，决定镜像的使用权限范围。

**可选值：**
- &#x60;public&#x60;：表示产品页面[资产管理-镜像]对应的公共镜像，平台预置镜像，所有用户可使用
- &#x60;private&#x60;：表示产品页面[资产管理-镜像]对应的我的镜像

     * @return JobParam
     */
    public JobParam imageVisibility(String imageVisibility) {
        this.imageVisibility = imageVisibility;
        return this;
    }


    /**
     * set 镜像ID，用于指定训练环境的基础镜像。

**示例：** &#x60;img-9axxxxxxjh&#x60;

     *
     * @param imageId 镜像ID，用于指定训练环境的基础镜像。

**示例：** &#x60;img-9axxxxxxjh&#x60;

     * @return JobParam
     */
    public JobParam imageId(String imageId) {
        this.imageId = imageId;
        return this;
    }


    /**
     * set 镜像完整地址，包含镜像仓库地址和标签。

**格式：** &#x60;registry地址/命名空间/镜像名:标签&#x60;

**示例：**
- &#x60;registry.jdcloud.com/jdaip/pytorch:2.0-cuda11.8&#x60;
- &#x60;registry.jdcloud.com/custom/my-training:v1.0&#x60;

     *
     * @param imageUrl 镜像完整地址，包含镜像仓库地址和标签。

**格式：** &#x60;registry地址/命名空间/镜像名:标签&#x60;

**示例：**
- &#x60;registry.jdcloud.com/jdaip/pytorch:2.0-cuda11.8&#x60;
- &#x60;registry.jdcloud.com/custom/my-training:v1.0&#x60;

     * @return JobParam
     */
    public JobParam imageUrl(String imageUrl) {
        this.imageUrl = imageUrl;
        return this;
    }


    /**
     * set 训练任务类型，决定底层调度框架和资源管理方式。

**可选值：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习

**注意：** 大小写敏感，必须使用小写

     *
     * @param jobType 训练任务类型，决定底层调度框架和资源管理方式。

**可选值：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习

**注意：** 大小写敏感，必须使用小写

     * @return JobParam
     */
    public JobParam jobType(String jobType) {
        this.jobType = jobType;
        return this;
    }


    /**
     * set 启动命令，指定训练脚本的执行方式。

**常用格式：**
- Python 脚本：&#x60;python /mnt/ws/train.py [参数]&#x60;
- Shell 脚本：&#x60;bash /mnt/ws/start.sh&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/train.py&#x60;

**注意事项：**
- 路径需与存储挂载路径对应
- 支持传入命令行参数
- 建议使用绝对路径

**示例：**
- 单机训练：&#x60;python /mnt/ws/train.py --epochs 100 --lr 0.001&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/distributed_train.py&#x60;
- 使用环境变量：&#x60;python /mnt/ws/train.py --data_dir $DATA_DIR&#x60;

     *
     * @param command 启动命令，指定训练脚本的执行方式。

**常用格式：**
- Python 脚本：&#x60;python /mnt/ws/train.py [参数]&#x60;
- Shell 脚本：&#x60;bash /mnt/ws/start.sh&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/train.py&#x60;

**注意事项：**
- 路径需与存储挂载路径对应
- 支持传入命令行参数
- 建议使用绝对路径

**示例：**
- 单机训练：&#x60;python /mnt/ws/train.py --epochs 100 --lr 0.001&#x60;
- 分布式训练：&#x60;torchrun --nproc_per_node&#x3D;4 /mnt/ws/distributed_train.py&#x60;
- 使用环境变量：&#x60;python /mnt/ws/train.py --data_dir $DATA_DIR&#x60;

     * @return JobParam
     */
    public JobParam command(String command) {
        this.command = command;
        return this;
    }


    /**
     * set **已废弃：** 请参考 &#x60;roleResource&#x60; 字段

     *
     * @param replica **已废弃：** 请参考 &#x60;roleResource&#x60; 字段

     * @return JobParam
     */
    public JobParam replica(Integer replica) {
        this.replica = replica;
        return this;
    }


    /**
     * set 任务优先级，范围[1, 9]; 当队列开启优先级调度时生效
     *
     * @param taskPriority 任务优先级，范围[1, 9]; 当队列开启优先级调度时生效
     * @return JobParam
     */
    public JobParam taskPriority(Integer taskPriority) {
        this.taskPriority = taskPriority;
        return this;
    }


    /**
    * set 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

    *
    * @param envs 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

    * @return JobParam
    */
    public JobParam envs(List<EnvParamForJob> envs) {
        this.envs = envs;
        return this;
    }


    /**
     * set **已废弃：** 请使用 &#x60;roleResource&#x60; 字段，支持更灵活的角色资源配置

     *
     * @param resource **已废弃：** 请使用 &#x60;roleResource&#x60; 字段，支持更灵活的角色资源配置

     * @return JobParam
     */
    public JobParam resource(ResourceParamForJob resource) {
        this.resource = resource;
        return this;
    }


    /**
    * set 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

    *
    * @param storageSpaces 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

    * @return JobParam
    */
    public JobParam storageSpaces(List<StorageSpaceParamForJob> storageSpaces) {
        this.storageSpaces = storageSpaces;
        return this;
    }


    /**
     * set 本地存储挂载配置，与 &#x60;storageSpaces&#x60; / &#x60;datasets&#x60; / &#x60;models&#x60; 平级。

在训练节点本地高速盘上为训练容器分配一块临时空间，仅支持专属资源池，每个任务最多一个。
字段定义见 &#x60;localStorageParamForJob&#x60;。未开启时不传本字段。

     *
     * @param localStorage 本地存储挂载配置，与 &#x60;storageSpaces&#x60; / &#x60;datasets&#x60; / &#x60;models&#x60; 平级。

在训练节点本地高速盘上为训练容器分配一块临时空间，仅支持专属资源池，每个任务最多一个。
字段定义见 &#x60;localStorageParamForJob&#x60;。未开启时不传本字段。

     * @return JobParam
     */
    public JobParam localStorage(LocalStorageParamForJob localStorage) {
        this.localStorage = localStorage;
        return this;
    }


    /**
    * set 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

    *
    * @param datasets 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

    * @return JobParam
    */
    public JobParam datasets(List<DatasetParamForJob> datasets) {
        this.datasets = datasets;
        return this;
    }


    /**
    * set 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

    *
    * @param models 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

    * @return JobParam
    */
    public JobParam models(List<ModelParamForJob> models) {
        this.models = models;
        return this;
    }


    /**
     * set 角色资源配置信息，用于配置不同角色的节点资源。

**适用场景：**
- Ray 分布式训练（必须配置 Head 和 Worker）
- PyTorch 分布式训练（可配置不同规格的 Worker）

     *
     * @param roleResource 角色资源配置信息，用于配置不同角色的节点资源。

**适用场景：**
- Ray 分布式训练（必须配置 Head 和 Worker）
- PyTorch 分布式训练（可配置不同规格的 Worker）

     * @return JobParam
     */
    public JobParam roleResource(RoleResourceParamForJob roleResource) {
        this.roleResource = roleResource;
        return this;
    }


    /**
     * set 出公网配置（任务级，仅公共资源池训练任务生效）。不需要出公网时不传此参数。

     *
     * @param internetEgress 出公网配置（任务级，仅公共资源池训练任务生效）。不需要出公网时不传此参数。

     * @return JobParam
     */
    public JobParam internetEgress(InternetEgressForJob internetEgress) {
        this.internetEgress = internetEgress;
        return this;
    }


    /**
     * set 框架高级配置，JSON 格式字符串。

**适用框架：** 仅支持 Ray 框架

**使用场景：**
- 配置 Ray 集群环境变量
- 自定义 NCCL、CUDA 等参数

**示例：**
  {&quot;ray&quot;: {&quot;env_vars&quot;: {&quot;NCCL_DEBUG&quot;: &quot;INFO&quot;, &quot;NCCL_IB_DISABLE&quot;: &quot;0&quot;, &quot;NCCL_PROTO&quot;: &quot;simple&quot;}}}

     *
     * @param advancedConfig 框架高级配置，JSON 格式字符串。

**适用框架：** 仅支持 Ray 框架

**使用场景：**
- 配置 Ray 集群环境变量
- 自定义 NCCL、CUDA 等参数

**示例：**
  {&quot;ray&quot;: {&quot;env_vars&quot;: {&quot;NCCL_DEBUG&quot;: &quot;INFO&quot;, &quot;NCCL_IB_DISABLE&quot;: &quot;0&quot;, &quot;NCCL_PROTO&quot;: &quot;simple&quot;}}}

     * @return JobParam
     */
    public JobParam advancedConfig(String advancedConfig) {
        this.advancedConfig = advancedConfig;
        return this;
    }


    /**
     * set 重启策略配置，定义任务异常时的自动重启行为。

**默认行为：** 禁用自动重启

**推荐场景：**
- 长时间训练任务：建议启用，应对节点故障
- 快速实验任务：可不启用，快速发现问题

     *
     * @param restartPolicy 重启策略配置，定义任务异常时的自动重启行为。

**默认行为：** 禁用自动重启

**推荐场景：**
- 长时间训练任务：建议启用，应对节点故障
- 快速实验任务：可不启用，快速发现问题

     * @return JobParam
     */
    public JobParam restartPolicy(RestartPolicyParamForJob restartPolicy) {
        this.restartPolicy = restartPolicy;
        return this;
    }


    /**
     * set 算力健康检测策略，用于检测GPU等算力资源是否正常。

**检测内容：**
- GPU 是否可用
- GPU 驱动是否正常
- CUDA 环境是否正常
- 高速网络、网卡是否正常
- 其他环境配置等

**默认行为：** 禁用健康检测

     *
     * @param healthCheckPolicy 算力健康检测策略，用于检测GPU等算力资源是否正常。

**检测内容：**
- GPU 是否可用
- GPU 驱动是否正常
- CUDA 环境是否正常
- 高速网络、网卡是否正常
- 其他环境配置等

**默认行为：** 禁用健康检测

     * @return JobParam
     */
    public JobParam healthCheckPolicy(HealthCheckPolicyParamForJob healthCheckPolicy) {
        this.healthCheckPolicy = healthCheckPolicy;
        return this;
    }


    /**
     * set 工作空间中的资源归属权限。

**可选值：**
- &#x60;public&#x60;：公开资源，工作空间所有成员可见可操作
- &#x60;private&#x60;：私有资源，仅创建者和空间管理员可见可操作

**默认值：** &#x60;public&#x60;

     *
     * @param permission 工作空间中的资源归属权限。

**可选值：**
- &#x60;public&#x60;：公开资源，工作空间所有成员可见可操作
- &#x60;private&#x60;：私有资源，仅创建者和空间管理员可见可操作

**默认值：** &#x60;public&#x60;

     * @return JobParam
     */
    public JobParam permission(String permission) {
        this.permission = permission;
        return this;
    }


    /**
    * set 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

    *
    * @param nodeAffinities 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

    * @return JobParam
    */
    public JobParam nodeAffinities(List<JobNodeAffinity> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
        return this;
    }


    /**
    * set 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

    *
    * @param codes 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

    * @return JobParam
    */
    public JobParam codes(List<CodeParam> codes) {
        this.codes = codes;
        return this;
    }


    /**
     * set 公共池排队超时时间，单位：分钟。

**取值范围：** 5 ~ 1440

**默认值：** 5

**说明：**
- 仅公共资源池训练任务生效
- 排队超过此时间后，任务将自动回滚为创建失败

     *
     * @param queuingTimeoutMinutes 公共池排队超时时间，单位：分钟。

**取值范围：** 5 ~ 1440

**默认值：** 5

**说明：**
- 仅公共资源池训练任务生效
- 排队超过此时间后，任务将自动回滚为创建失败

     * @return JobParam
     */
    public JobParam queuingTimeoutMinutes(Integer queuingTimeoutMinutes) {
        this.queuingTimeoutMinutes = queuingTimeoutMinutes;
        return this;
    }


    /**
    * set 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

    *
    * @param userTags 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

    * @return JobParam
    */
    public JobParam userTags(List<JobTag> userTags) {
        this.userTags = userTags;
        return this;
    }


    /**
     * set 资源组ID，用于将任务归属到特定的资源组进行管理。

**使用场景：**
- 按项目隔离资源
- 按团队分配资源配额
- 资源使用统计和计费

     *
     * @param resourceGroupId 资源组ID，用于将任务归属到特定的资源组进行管理。

**使用场景：**
- 按项目隔离资源
- 按团队分配资源配额
- 资源使用统计和计费

     * @return JobParam
     */
    public JobParam resourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
        return this;
    }



    /**
     * add item to 环境变量列表，用于向训练脚本传递配置参数。

**使用场景：**
- 传递训练参数（如学习率、批次大小）
- 配置框架参数（如 CUDA 设置）
- 传递敏感信息（如 API Key，建议使用密钥管理）

**示例：**
  [{&quot;name&quot;: &quot;CUDA_VISIBLE_DEVICES&quot;, &quot;value&quot;: &quot;0,1,2,3&quot;},
   {&quot;name&quot;: &quot;BATCH_SIZE&quot;, &quot;value&quot;: &quot;32&quot;},
   {&quot;name&quot;: &quot;LEARNING_RATE&quot;, &quot;value&quot;: &quot;0.001&quot;}]

     *
     * @param env
     */
    public void addEnv(EnvParamForJob env) {
        if (this.envs == null) {
            this.envs = new ArrayList<>();
        }
        this.envs.add(env);
    }

    /**
     * add item to 存储空间配置列表，用于挂载外部共享存储到训练容器中。

**支持的存储类型：**
- &#x60;oss&#x60;：对象存储服务，适合大规模数据存储
- &#x60;cfs&#x60;：云文件系统，提供高性能文件存储
- &#x60;jpfs&#x60;：京东云并行文件系统，高并发场景优化

**使用场景：**
- 挂载训练数据目录
- 挂载输出目录保存模型和日志
- 挂载共享存储用于分布式训练

**说明：** 训练节点本地临时盘请使用顶层字段 &#x60;localStorage&#x60;，不要放在本列表中。

     *
     * @param storageSpace
     */
    public void addStorageSpace(StorageSpaceParamForJob storageSpace) {
        if (this.storageSpaces == null) {
            this.storageSpaces = new ArrayList<>();
        }
        this.storageSpaces.add(storageSpace);
    }

    /**
     * add item to 数据集配置列表，用于挂载已创建的数据集到训练容器。

**优势：**
- 支持数据集版本管理
- 支持公开/私有数据集
- 自动处理存储挂载

**使用场景：**
- 挂载训练数据集
- 挂载验证数据集
- 挂载测试数据集

     *
     * @param dataset
     */
    public void addDataset(DatasetParamForJob dataset) {
        if (this.datasets == null) {
            this.datasets = new ArrayList<>();
        }
        this.datasets.add(dataset);
    }

    /**
     * add item to 模型配置列表，用于挂载预训练模型或检查点文件。

**使用场景：**
- 加载预训练模型进行迁移学习
- 加载检查点继续训练
- 挂载基础模型进行微调

     *
     * @param model
     */
    public void addModel(ModelParamForJob model) {
        if (this.models == null) {
            this.models = new ArrayList<>();
        }
        this.models.add(model);
    }

    /**
     * add item to 节点亲和性配置，用于将任务调度到特定的计算节点。支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

**使用场景：**
- 指定特定 GPU 型号的节点
- 指定特定机架或可用区的节点
- 避免调度到特定节点

**注意：** 配置不当可能导致任务无法调度

     *
     * @param nodeAffinitie
     */
    public void addNodeAffinitie(JobNodeAffinity nodeAffinitie) {
        if (this.nodeAffinities == null) {
            this.nodeAffinities = new ArrayList<>();
        }
        this.nodeAffinities.add(nodeAffinitie);
    }

    /**
     * add item to 代码仓库配置列表，用于从代码仓库拉取训练代码。

**支持的代码仓库：**
- Git 仓库
- GitHub
- GitLab

**使用场景：**
- 自动拉取最新代码
- 指定代码分支或CommitID
- 代码版本管理

     *
     * @param code
     */
    public void addCode(CodeParam code) {
        if (this.codes == null) {
            this.codes = new ArrayList<>();
        }
        this.codes.add(code);
    }

    /**
     * add item to 用户自定义标签，用于资源分类和筛选。

**限制：**
- 最多支持 10 个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持中文、大小写英文、数字及符号：&#x60;\_.,:\/&#x3D;+-@&#x60;

**使用场景：**
- 标记项目或团队
- 成本分账
- 资源分类管理

     *
     * @param userTag
     */
    public void addUserTag(JobTag userTag) {
        if (this.userTags == null) {
            this.userTags = new ArrayList<>();
        }
        this.userTags.add(userTag);
    }
}