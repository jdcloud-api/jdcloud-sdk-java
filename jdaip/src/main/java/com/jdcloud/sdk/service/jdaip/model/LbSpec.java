/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;


/**
 * 负载均衡配置，仅用于创建/更新Notebook时配置公网访问能力的请求入参。

## 使用场景
- 通过公网SSH连接Notebook
- 通过公网访问Notebook自定义服务

## 配置说明
- **公共资源池**: LbSpec不为空仅表示需要公网访问，lbId/lbPort由系统自动分配，无需指定
- **私有资源池**: 需要传入与资源队列可通信的负载均衡ID和端口
- 负载均衡需要与资源队列在同一VPC下
- 端口需要未被占用

 */
public class LbSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 负载均衡实例ID，需要在京东云负载均衡产品中预先创建。

## 要求
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 负载均衡需要与资源队列在同一VPC下
- 支持四层(TCP)负载均衡

     */
    private String lbId;

    /**
     * 监听端口，负载均衡监听此端口并转发到Notebook。

## 端口范围
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 1-65534
- 需要指定一个未被占用的空闲可用端口
- 建议使用1024以上端口

     */
    private Integer lbPort;

    /**
     * 是否开启公网访问，仅公共资源池有效。

## 使用说明
- 公共资源池: 为true时表示需要公网访问，系统会自动分配LB，无需指定lbId和lbPort
- 私有资源池: 无效，需要通过lbId和lbPort指定负载均衡
- 默认为false

     */
    private Boolean lbEnable;



    /**
     * get 负载均衡实例ID，需要在京东云负载均衡产品中预先创建。

## 要求
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 负载均衡需要与资源队列在同一VPC下
- 支持四层(TCP)负载均衡

     *
     * @return
     */
    public String getLbId() {
        return lbId;
    }

    /**
     * set 负载均衡实例ID，需要在京东云负载均衡产品中预先创建。

## 要求
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 负载均衡需要与资源队列在同一VPC下
- 支持四层(TCP)负载均衡

     *
     * @param lbId
     */
    public void setLbId(String lbId) {
        this.lbId = lbId;
    }


    /**
     * get 监听端口，负载均衡监听此端口并转发到Notebook。

## 端口范围
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 1-65534
- 需要指定一个未被占用的空闲可用端口
- 建议使用1024以上端口

     *
     * @return
     */
    public Integer getLbPort() {
        return lbPort;
    }

    /**
     * set 监听端口，负载均衡监听此端口并转发到Notebook。

## 端口范围
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 1-65534
- 需要指定一个未被占用的空闲可用端口
- 建议使用1024以上端口

     *
     * @param lbPort
     */
    public void setLbPort(Integer lbPort) {
        this.lbPort = lbPort;
    }


    /**
     * get 是否开启公网访问，仅公共资源池有效。

## 使用说明
- 公共资源池: 为true时表示需要公网访问，系统会自动分配LB，无需指定lbId和lbPort
- 私有资源池: 无效，需要通过lbId和lbPort指定负载均衡
- 默认为false

     *
     * @return
     */
    public Boolean getLbEnable() {
        return lbEnable;
    }

    /**
     * set 是否开启公网访问，仅公共资源池有效。

## 使用说明
- 公共资源池: 为true时表示需要公网访问，系统会自动分配LB，无需指定lbId和lbPort
- 私有资源池: 无效，需要通过lbId和lbPort指定负载均衡
- 默认为false

     *
     * @param lbEnable
     */
    public void setLbEnable(Boolean lbEnable) {
        this.lbEnable = lbEnable;
    }



    /**
     * set 负载均衡实例ID，需要在京东云负载均衡产品中预先创建。

## 要求
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 负载均衡需要与资源队列在同一VPC下
- 支持四层(TCP)负载均衡

     *
     * @param lbId 负载均衡实例ID，需要在京东云负载均衡产品中预先创建。

## 要求
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 负载均衡需要与资源队列在同一VPC下
- 支持四层(TCP)负载均衡

     * @return LbSpec
     */
    public LbSpec lbId(String lbId) {
        this.lbId = lbId;
        return this;
    }


    /**
     * set 监听端口，负载均衡监听此端口并转发到Notebook。

## 端口范围
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 1-65534
- 需要指定一个未被占用的空闲可用端口
- 建议使用1024以上端口

     *
     * @param lbPort 监听端口，负载均衡监听此端口并转发到Notebook。

## 端口范围
- 私有资源池必填，公共资源池无需指定(系统自动分配)
- 1-65534
- 需要指定一个未被占用的空闲可用端口
- 建议使用1024以上端口

     * @return LbSpec
     */
    public LbSpec lbPort(Integer lbPort) {
        this.lbPort = lbPort;
        return this;
    }


    /**
     * set 是否开启公网访问，仅公共资源池有效。

## 使用说明
- 公共资源池: 为true时表示需要公网访问，系统会自动分配LB，无需指定lbId和lbPort
- 私有资源池: 无效，需要通过lbId和lbPort指定负载均衡
- 默认为false

     *
     * @param lbEnable 是否开启公网访问，仅公共资源池有效。

## 使用说明
- 公共资源池: 为true时表示需要公网访问，系统会自动分配LB，无需指定lbId和lbPort
- 私有资源池: 无效，需要通过lbId和lbPort指定负载均衡
- 默认为false

     * @return LbSpec
     */
    public LbSpec lbEnable(Boolean lbEnable) {
        this.lbEnable = lbEnable;
        return this;
    }


}