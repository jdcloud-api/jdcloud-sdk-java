/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;
import com.jdcloud.sdk.annotation.Required;

/**
 * 创建微调运行需要的参数。

微调运行是实际执行训练任务的实体，包含完整的训练配置。

## 配置要点

- **基础配置**：名称、描述
- **微调配置**：训练阶段、微调方法、训练参数
- **数据配置**：训练数据集、验证数据集
- **资源配置**：GPU、CPU、内存
- **存储配置**：模型输出位置

## 创建流程

1. 准备数据集并上传
2. 配置资源规格
3. 设置微调参数
4. 提交创建请求
5. 等待训练完成

 */
public class RunParamForExperiment  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 微调运行名称。

## 命名规则
- 长度限制：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;

## 建议
名称应体现训练特点，便于识别和管理。

     * Required:true
     */
    @Required
    private String name;

    /**
     * 微调运行的描述信息。

## 限制
不超过256个字符。

## 建议
建议填写训练目标、参数特点等信息。

     */
    private String description;

    /**
     * 微调配置参数。

包含训练阶段、微调方法、训练参数等核心配置。

     * Required:true
     */
    @Required
    private FinetuningConfigParamForExperiment finetuningConfig;

    /**
     * 验证数据集来源。

## 可选值

| 值 | 说明 | 要求 |
|----|------|------|
| &#x60;split&#x60; | 从训练数据集拆分 | 必须设置 evalDatasetSplitRatio |
| &#x60;provided&#x60; | 单独提供验证数据集 | 必须在 datasets 中配置 evaluation 数据集 |

## 选择建议
- 数据量充足时，使用 &#x60;split&#x60; 自动拆分
- 有专用验证数据时，使用 &#x60;provided&#x60;

     */
    private String evalDatasetSource;

    /**
     * 验证集拆分比例。

## 范围
0 &lt; evalDatasetSplitRatio &lt; 1

## 说明
当 &#x60;evalDatasetSource&#x60; 为 &#x60;split&#x60; 时必填。

## 建议值
- 0.1：10% 作为验证集
- 0.2：20% 作为验证集

     */
    private Double evalDatasetSplitRatio;

    /**
     * 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

     * Required:true
     */
    @Required
    
    private List<DatasetParamForExperiment> datasets;
    /**
     * 资源配置信息。

配置训练所需的计算资源。

     * Required:true
     */
    @Required
    private ResourceParamForExperiment resource;

    /**
     * 存储空间配置。

用于保存训练后的模型、报告等数据。

     * Required:true
     */
    @Required
    private StorageSpaceParamForExperiment storageSpace;

    /**
     * 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

     */
    
    private List<NodeAffinityForExperiment> nodeAffinities;
    /**
     * 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

     */
    
    private List<RunTag> userTags;
    /**
     * 资源组ID。

## 用途
用于资源分组管理和成本分摊。

     */
    private String resourceGroupId;



    /**
     * get 微调运行名称。

## 命名规则
- 长度限制：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;

## 建议
名称应体现训练特点，便于识别和管理。

     *
     * @return
     */
    public String getName() {
        return name;
    }

    /**
     * set 微调运行名称。

## 命名规则
- 长度限制：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;

## 建议
名称应体现训练特点，便于识别和管理。

     *
     * @param name
     */
    public void setName(String name) {
        this.name = name;
    }


    /**
     * get 微调运行的描述信息。

## 限制
不超过256个字符。

## 建议
建议填写训练目标、参数特点等信息。

     *
     * @return
     */
    public String getDescription() {
        return description;
    }

    /**
     * set 微调运行的描述信息。

## 限制
不超过256个字符。

## 建议
建议填写训练目标、参数特点等信息。

     *
     * @param description
     */
    public void setDescription(String description) {
        this.description = description;
    }


    /**
     * get 微调配置参数。

包含训练阶段、微调方法、训练参数等核心配置。

     *
     * @return
     */
    public FinetuningConfigParamForExperiment getFinetuningConfig() {
        return finetuningConfig;
    }

    /**
     * set 微调配置参数。

包含训练阶段、微调方法、训练参数等核心配置。

     *
     * @param finetuningConfig
     */
    public void setFinetuningConfig(FinetuningConfigParamForExperiment finetuningConfig) {
        this.finetuningConfig = finetuningConfig;
    }


    /**
     * get 验证数据集来源。

## 可选值

| 值 | 说明 | 要求 |
|----|------|------|
| &#x60;split&#x60; | 从训练数据集拆分 | 必须设置 evalDatasetSplitRatio |
| &#x60;provided&#x60; | 单独提供验证数据集 | 必须在 datasets 中配置 evaluation 数据集 |

## 选择建议
- 数据量充足时，使用 &#x60;split&#x60; 自动拆分
- 有专用验证数据时，使用 &#x60;provided&#x60;

     *
     * @return
     */
    public String getEvalDatasetSource() {
        return evalDatasetSource;
    }

    /**
     * set 验证数据集来源。

## 可选值

| 值 | 说明 | 要求 |
|----|------|------|
| &#x60;split&#x60; | 从训练数据集拆分 | 必须设置 evalDatasetSplitRatio |
| &#x60;provided&#x60; | 单独提供验证数据集 | 必须在 datasets 中配置 evaluation 数据集 |

## 选择建议
- 数据量充足时，使用 &#x60;split&#x60; 自动拆分
- 有专用验证数据时，使用 &#x60;provided&#x60;

     *
     * @param evalDatasetSource
     */
    public void setEvalDatasetSource(String evalDatasetSource) {
        this.evalDatasetSource = evalDatasetSource;
    }


    /**
     * get 验证集拆分比例。

## 范围
0 &lt; evalDatasetSplitRatio &lt; 1

## 说明
当 &#x60;evalDatasetSource&#x60; 为 &#x60;split&#x60; 时必填。

## 建议值
- 0.1：10% 作为验证集
- 0.2：20% 作为验证集

     *
     * @return
     */
    public Double getEvalDatasetSplitRatio() {
        return evalDatasetSplitRatio;
    }

    /**
     * set 验证集拆分比例。

## 范围
0 &lt; evalDatasetSplitRatio &lt; 1

## 说明
当 &#x60;evalDatasetSource&#x60; 为 &#x60;split&#x60; 时必填。

## 建议值
- 0.1：10% 作为验证集
- 0.2：20% 作为验证集

     *
     * @param evalDatasetSplitRatio
     */
    public void setEvalDatasetSplitRatio(Double evalDatasetSplitRatio) {
        this.evalDatasetSplitRatio = evalDatasetSplitRatio;
    }


    /**
    * get 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

    *
    * @return
    */
    public List<DatasetParamForExperiment> getDatasets() {
        return datasets;
    }

    /**
    * set 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

    *
    * @param datasets
    */
    public void setDatasets(List<DatasetParamForExperiment> datasets) {
        this.datasets = datasets;
    }


    /**
     * get 资源配置信息。

配置训练所需的计算资源。

     *
     * @return
     */
    public ResourceParamForExperiment getResource() {
        return resource;
    }

    /**
     * set 资源配置信息。

配置训练所需的计算资源。

     *
     * @param resource
     */
    public void setResource(ResourceParamForExperiment resource) {
        this.resource = resource;
    }


    /**
     * get 存储空间配置。

用于保存训练后的模型、报告等数据。

     *
     * @return
     */
    public StorageSpaceParamForExperiment getStorageSpace() {
        return storageSpace;
    }

    /**
     * set 存储空间配置。

用于保存训练后的模型、报告等数据。

     *
     * @param storageSpace
     */
    public void setStorageSpace(StorageSpaceParamForExperiment storageSpace) {
        this.storageSpace = storageSpace;
    }


    /**
    * get 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

    *
    * @return
    */
    public List<NodeAffinityForExperiment> getNodeAffinities() {
        return nodeAffinities;
    }

    /**
    * set 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

    *
    * @param nodeAffinities
    */
    public void setNodeAffinities(List<NodeAffinityForExperiment> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
    }


    /**
    * get 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

    *
    * @return
    */
    public List<RunTag> getUserTags() {
        return userTags;
    }

    /**
    * set 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

    *
    * @param userTags
    */
    public void setUserTags(List<RunTag> userTags) {
        this.userTags = userTags;
    }


    /**
     * get 资源组ID。

## 用途
用于资源分组管理和成本分摊。

     *
     * @return
     */
    public String getResourceGroupId() {
        return resourceGroupId;
    }

    /**
     * set 资源组ID。

## 用途
用于资源分组管理和成本分摊。

     *
     * @param resourceGroupId
     */
    public void setResourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
    }



    /**
     * set 微调运行名称。

## 命名规则
- 长度限制：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;

## 建议
名称应体现训练特点，便于识别和管理。

     *
     * @param name 微调运行名称。

## 命名规则
- 长度限制：1~32个字符
- 支持字符：中文、大小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;

## 建议
名称应体现训练特点，便于识别和管理。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment name(String name) {
        this.name = name;
        return this;
    }


    /**
     * set 微调运行的描述信息。

## 限制
不超过256个字符。

## 建议
建议填写训练目标、参数特点等信息。

     *
     * @param description 微调运行的描述信息。

## 限制
不超过256个字符。

## 建议
建议填写训练目标、参数特点等信息。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment description(String description) {
        this.description = description;
        return this;
    }


    /**
     * set 微调配置参数。

包含训练阶段、微调方法、训练参数等核心配置。

     *
     * @param finetuningConfig 微调配置参数。

包含训练阶段、微调方法、训练参数等核心配置。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment finetuningConfig(FinetuningConfigParamForExperiment finetuningConfig) {
        this.finetuningConfig = finetuningConfig;
        return this;
    }


    /**
     * set 验证数据集来源。

## 可选值

| 值 | 说明 | 要求 |
|----|------|------|
| &#x60;split&#x60; | 从训练数据集拆分 | 必须设置 evalDatasetSplitRatio |
| &#x60;provided&#x60; | 单独提供验证数据集 | 必须在 datasets 中配置 evaluation 数据集 |

## 选择建议
- 数据量充足时，使用 &#x60;split&#x60; 自动拆分
- 有专用验证数据时，使用 &#x60;provided&#x60;

     *
     * @param evalDatasetSource 验证数据集来源。

## 可选值

| 值 | 说明 | 要求 |
|----|------|------|
| &#x60;split&#x60; | 从训练数据集拆分 | 必须设置 evalDatasetSplitRatio |
| &#x60;provided&#x60; | 单独提供验证数据集 | 必须在 datasets 中配置 evaluation 数据集 |

## 选择建议
- 数据量充足时，使用 &#x60;split&#x60; 自动拆分
- 有专用验证数据时，使用 &#x60;provided&#x60;

     * @return RunParamForExperiment
     */
    public RunParamForExperiment evalDatasetSource(String evalDatasetSource) {
        this.evalDatasetSource = evalDatasetSource;
        return this;
    }


    /**
     * set 验证集拆分比例。

## 范围
0 &lt; evalDatasetSplitRatio &lt; 1

## 说明
当 &#x60;evalDatasetSource&#x60; 为 &#x60;split&#x60; 时必填。

## 建议值
- 0.1：10% 作为验证集
- 0.2：20% 作为验证集

     *
     * @param evalDatasetSplitRatio 验证集拆分比例。

## 范围
0 &lt; evalDatasetSplitRatio &lt; 1

## 说明
当 &#x60;evalDatasetSource&#x60; 为 &#x60;split&#x60; 时必填。

## 建议值
- 0.1：10% 作为验证集
- 0.2：20% 作为验证集

     * @return RunParamForExperiment
     */
    public RunParamForExperiment evalDatasetSplitRatio(Double evalDatasetSplitRatio) {
        this.evalDatasetSplitRatio = evalDatasetSplitRatio;
        return this;
    }


    /**
    * set 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

    *
    * @param datasets 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

    * @return RunParamForExperiment
    */
    public RunParamForExperiment datasets(List<DatasetParamForExperiment> datasets) {
        this.datasets = datasets;
        return this;
    }


    /**
     * set 资源配置信息。

配置训练所需的计算资源。

     *
     * @param resource 资源配置信息。

配置训练所需的计算资源。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment resource(ResourceParamForExperiment resource) {
        this.resource = resource;
        return this;
    }


    /**
     * set 存储空间配置。

用于保存训练后的模型、报告等数据。

     *
     * @param storageSpace 存储空间配置。

用于保存训练后的模型、报告等数据。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment storageSpace(StorageSpaceParamForExperiment storageSpace) {
        this.storageSpace = storageSpace;
        return this;
    }


    /**
    * set 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

    *
    * @param nodeAffinities 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

    * @return RunParamForExperiment
    */
    public RunParamForExperiment nodeAffinities(List<NodeAffinityForExperiment> nodeAffinities) {
        this.nodeAffinities = nodeAffinities;
        return this;
    }


    /**
    * set 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

    *
    * @param userTags 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

    * @return RunParamForExperiment
    */
    public RunParamForExperiment userTags(List<RunTag> userTags) {
        this.userTags = userTags;
        return this;
    }


    /**
     * set 资源组ID。

## 用途
用于资源分组管理和成本分摊。

     *
     * @param resourceGroupId 资源组ID。

## 用途
用于资源分组管理和成本分摊。

     * @return RunParamForExperiment
     */
    public RunParamForExperiment resourceGroupId(String resourceGroupId) {
        this.resourceGroupId = resourceGroupId;
        return this;
    }



    /**
     * add item to 数据集配置列表。

## 配置要求
- 至少包含一个 &#x60;training&#x60; 类型的数据集
- 当 &#x60;evalDatasetSource&#x60; 为 &#x60;provided&#x60; 时，需要包含 &#x60;evaluation&#x60; 类型的数据集

     *
     * @param dataset
     */
    public void addDataset(DatasetParamForExperiment dataset) {
        if (this.datasets == null) {
            this.datasets = new ArrayList<>();
        }
        this.datasets.add(dataset);
    }

    /**
     * add item to 节点亲和性配置。

## 用途
- 将任务调度到特定节点
- 使用特定硬件配置的节点

## 说明
支持配置多个亲和性规则，每个元素之间是&quot;或&quot;的关系。

     *
     * @param nodeAffinitie
     */
    public void addNodeAffinitie(NodeAffinityForExperiment nodeAffinitie) {
        if (this.nodeAffinities == null) {
            this.nodeAffinities = new ArrayList<>();
        }
        this.nodeAffinities.add(nodeAffinitie);
    }

    /**
     * add item to 自定义实例标签。

## 格式
key-value 键值对形式。

## 限制
- 最多支持10个标签
- key 不能以 &#x60;jrn:&#x60; 或 &#x60;jdc-&#x60; 开头
- key 支持字符：中文、大小写英文、数字及 &#x60;_.,:/&#x3D;+-@&#x60;

## 用途
- 资源分类
- 成本分析
- 权限控制

     *
     * @param userTag
     */
    public void addUserTag(RunTag userTag) {
        if (this.userTags == null) {
            this.userTags = new ArrayList<>();
        }
        this.userTags.add(userTag);
    }
}