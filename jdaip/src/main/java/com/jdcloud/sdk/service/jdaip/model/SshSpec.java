/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;

/**
 * SSH配置，仅用于创建/更新Notebook时的请求入参。

## 包含字段
- sshPasswordAuth: 密码认证开关
- publicKeys: SSH公钥列表

## 说明
- 本结构仅描述入参，不含任何连接地址字段
- 连接地址(sshConnection、sshVpcConnection、sshPodConnection)由系统在查询详情时生成，见响应结构 sshConnectionInfo

## 使用场景
- 创建Notebook时开启并配置SSH免密登录
- 更新Notebook属性时调整SSH配置

 */
public class SshSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * SSH密码认证开关，控制是否允许密码登录。

## 取值
- yes: 允许密码认证（默认）
- no: 禁用密码认证，仅允许公钥登录

## 使用说明
- 不传时默认为yes，密码认证可用
- 设为no时确保已配置publicKeys，否则将无法登录

     */
    private String sshPasswordAuth;

    /**
     * SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

     */
    
    private List<String> publicKeys;


    /**
     * get SSH密码认证开关，控制是否允许密码登录。

## 取值
- yes: 允许密码认证（默认）
- no: 禁用密码认证，仅允许公钥登录

## 使用说明
- 不传时默认为yes，密码认证可用
- 设为no时确保已配置publicKeys，否则将无法登录

     *
     * @return
     */
    public String getSshPasswordAuth() {
        return sshPasswordAuth;
    }

    /**
     * set SSH密码认证开关，控制是否允许密码登录。

## 取值
- yes: 允许密码认证（默认）
- no: 禁用密码认证，仅允许公钥登录

## 使用说明
- 不传时默认为yes，密码认证可用
- 设为no时确保已配置publicKeys，否则将无法登录

     *
     * @param sshPasswordAuth
     */
    public void setSshPasswordAuth(String sshPasswordAuth) {
        this.sshPasswordAuth = sshPasswordAuth;
    }


    /**
    * get SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

    *
    * @return
    */
    public List<String> getPublicKeys() {
        return publicKeys;
    }

    /**
    * set SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

    *
    * @param publicKeys
    */
    public void setPublicKeys(List<String> publicKeys) {
        this.publicKeys = publicKeys;
    }



    /**
     * set SSH密码认证开关，控制是否允许密码登录。

## 取值
- yes: 允许密码认证（默认）
- no: 禁用密码认证，仅允许公钥登录

## 使用说明
- 不传时默认为yes，密码认证可用
- 设为no时确保已配置publicKeys，否则将无法登录

     *
     * @param sshPasswordAuth SSH密码认证开关，控制是否允许密码登录。

## 取值
- yes: 允许密码认证（默认）
- no: 禁用密码认证，仅允许公钥登录

## 使用说明
- 不传时默认为yes，密码认证可用
- 设为no时确保已配置publicKeys，否则将无法登录

     * @return SshSpec
     */
    public SshSpec sshPasswordAuth(String sshPasswordAuth) {
        this.sshPasswordAuth = sshPasswordAuth;
        return this;
    }


    /**
    * set SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

    *
    * @param publicKeys SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

    * @return SshSpec
    */
    public SshSpec publicKeys(List<String> publicKeys) {
        this.publicKeys = publicKeys;
        return this;
    }



    /**
     * add item to SSH公钥列表，用于免密登录。

## 格式
- 标准SSH公钥格式
- 示例：ssh-rsa AAAAB3NzaC1yc2E...

## 限制
- 最多10条，超出部分将校验失败

     *
     * @param publicKey
     */
    public void addPublicKey(String publicKey) {
        if (this.publicKeys == null) {
            this.publicKeys = new ArrayList<>();
        }
        this.publicKeys.add(publicKey);
    }
}