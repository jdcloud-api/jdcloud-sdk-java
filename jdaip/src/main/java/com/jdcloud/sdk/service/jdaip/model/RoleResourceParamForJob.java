/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;

/**
 * 角色资源配置参数，用于创建分布式训练任务。

 */
public class RoleResourceParamForJob  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池：** 使用工作空间绑定的队列，需要用户及角色拥有该队列的使用权限

**示例：** &#x60;joybuilder-public-queue&#x60;、&#x60;joybuilder-exclusive-queue&#x60;、&#x60;queue-2xxx**********2d*********8b8&#x60;

     */
    private String queueId;

    /**
     * RDMA 开关，控制是否为该任务开启 RDMA 高速网络。

**生效范围：** 仅对专属资源池中的GPU分布式任务生效，含CPU或非整卡的任务不生效，单实例任务不生效；公共资源池不生效。

**可选值：**
- &#x60;on&#x60;：开启 RDMA
- &#x60;off&#x60;：关闭 RDMA

**默认值（不设置时）：**
- GPU 分布式任务：默认为 &#x60;on&#x60;；若所选计算资源不支持 RDMA，必须显式传 &#x60;off&#x60; 关闭。
- 单实例任务：默认为 &#x60;off&#x60;。
- 带CPU的任务：默认为 &#x60;off&#x60;。

     */
    private String rdmaSwitch;

    /**
     * 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

     */
    
    private List<RoleResourceInfoForJob> roles;


    /**
     * get 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池：** 使用工作空间绑定的队列，需要用户及角色拥有该队列的使用权限

**示例：** &#x60;joybuilder-public-queue&#x60;、&#x60;joybuilder-exclusive-queue&#x60;、&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @return
     */
    public String getQueueId() {
        return queueId;
    }

    /**
     * set 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池：** 使用工作空间绑定的队列，需要用户及角色拥有该队列的使用权限

**示例：** &#x60;joybuilder-public-queue&#x60;、&#x60;joybuilder-exclusive-queue&#x60;、&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @param queueId
     */
    public void setQueueId(String queueId) {
        this.queueId = queueId;
    }


    /**
     * get RDMA 开关，控制是否为该任务开启 RDMA 高速网络。

**生效范围：** 仅对专属资源池中的GPU分布式任务生效，含CPU或非整卡的任务不生效，单实例任务不生效；公共资源池不生效。

**可选值：**
- &#x60;on&#x60;：开启 RDMA
- &#x60;off&#x60;：关闭 RDMA

**默认值（不设置时）：**
- GPU 分布式任务：默认为 &#x60;on&#x60;；若所选计算资源不支持 RDMA，必须显式传 &#x60;off&#x60; 关闭。
- 单实例任务：默认为 &#x60;off&#x60;。
- 带CPU的任务：默认为 &#x60;off&#x60;。

     *
     * @return
     */
    public String getRdmaSwitch() {
        return rdmaSwitch;
    }

    /**
     * set RDMA 开关，控制是否为该任务开启 RDMA 高速网络。

**生效范围：** 仅对专属资源池中的GPU分布式任务生效，含CPU或非整卡的任务不生效，单实例任务不生效；公共资源池不生效。

**可选值：**
- &#x60;on&#x60;：开启 RDMA
- &#x60;off&#x60;：关闭 RDMA

**默认值（不设置时）：**
- GPU 分布式任务：默认为 &#x60;on&#x60;；若所选计算资源不支持 RDMA，必须显式传 &#x60;off&#x60; 关闭。
- 单实例任务：默认为 &#x60;off&#x60;。
- 带CPU的任务：默认为 &#x60;off&#x60;。

     *
     * @param rdmaSwitch
     */
    public void setRdmaSwitch(String rdmaSwitch) {
        this.rdmaSwitch = rdmaSwitch;
    }


    /**
    * get 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

    *
    * @return
    */
    public List<RoleResourceInfoForJob> getRoles() {
        return roles;
    }

    /**
    * set 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

    *
    * @param roles
    */
    public void setRoles(List<RoleResourceInfoForJob> roles) {
        this.roles = roles;
    }



    /**
     * set 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池：** 使用工作空间绑定的队列，需要用户及角色拥有该队列的使用权限

**示例：** &#x60;joybuilder-public-queue&#x60;、&#x60;joybuilder-exclusive-queue&#x60;、&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @param queueId 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池：** 使用工作空间绑定的队列，需要用户及角色拥有该队列的使用权限

**示例：** &#x60;joybuilder-public-queue&#x60;、&#x60;joybuilder-exclusive-queue&#x60;、&#x60;queue-2xxx**********2d*********8b8&#x60;

     * @return RoleResourceParamForJob
     */
    public RoleResourceParamForJob queueId(String queueId) {
        this.queueId = queueId;
        return this;
    }


    /**
     * set RDMA 开关，控制是否为该任务开启 RDMA 高速网络。

**生效范围：** 仅对专属资源池中的GPU分布式任务生效，含CPU或非整卡的任务不生效，单实例任务不生效；公共资源池不生效。

**可选值：**
- &#x60;on&#x60;：开启 RDMA
- &#x60;off&#x60;：关闭 RDMA

**默认值（不设置时）：**
- GPU 分布式任务：默认为 &#x60;on&#x60;；若所选计算资源不支持 RDMA，必须显式传 &#x60;off&#x60; 关闭。
- 单实例任务：默认为 &#x60;off&#x60;。
- 带CPU的任务：默认为 &#x60;off&#x60;。

     *
     * @param rdmaSwitch RDMA 开关，控制是否为该任务开启 RDMA 高速网络。

**生效范围：** 仅对专属资源池中的GPU分布式任务生效，含CPU或非整卡的任务不生效，单实例任务不生效；公共资源池不生效。

**可选值：**
- &#x60;on&#x60;：开启 RDMA
- &#x60;off&#x60;：关闭 RDMA

**默认值（不设置时）：**
- GPU 分布式任务：默认为 &#x60;on&#x60;；若所选计算资源不支持 RDMA，必须显式传 &#x60;off&#x60; 关闭。
- 单实例任务：默认为 &#x60;off&#x60;。
- 带CPU的任务：默认为 &#x60;off&#x60;。

     * @return RoleResourceParamForJob
     */
    public RoleResourceParamForJob rdmaSwitch(String rdmaSwitch) {
        this.rdmaSwitch = rdmaSwitch;
        return this;
    }


    /**
    * set 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

    *
    * @param roles 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

    * @return RoleResourceParamForJob
    */
    public RoleResourceParamForJob roles(List<RoleResourceInfoForJob> roles) {
        this.roles = roles;
        return this;
    }



    /**
     * add item to 角色资源配置详细信息列表。

**配置规则：**
- PyTorch 任务：仅支持 Worker 角色
- Ray 任务：必须包含 Head 角色，可根据需求增加 Worker 角色

     *
     * @param role
     */
    public void addRole(RoleResourceInfoForJob role) {
        if (this.roles == null) {
            this.roles = new ArrayList<>();
        }
        this.roles.add(role);
    }
}