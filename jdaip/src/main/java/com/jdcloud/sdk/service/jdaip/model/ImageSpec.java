/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;
import com.jdcloud.sdk.annotation.Required;

/**
 * 保存镜像的配置信息，定义将Notebook实例保存为镜像的参数。

## 配置项说明
- **镜像信息**: 名称、用途、架构
- **镜像标签**: 用于分类管理
- **目标仓库**: 镜像推送目标
- **权限配置**: 镜像归属权限

## 保存流程
1. 将运行中的Notebook容器打包成镜像
2. 推送到指定的镜像仓库
3. 注册到用户的镜像资产中

 */
public class ImageSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 镜像名称，用于注册到我的镜像仓库。

## 命名规则
- 长度：1~128字符
- 支持字符：小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 必须以字母开头
- 示例：my-notebook-image-v1

     * Required:true
     */
    @Required
    private String imgName;

    /**
     * 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

     * Required:true
     */
    @Required
    
    private List<String> imgUsage;
    /**
     * 镜像架构类型，指定镜像支持的硬件架构。

## 支持的架构
- cpu: CPU镜像
- gpu: GPU镜像

     * Required:true
     */
    @Required
    private String imgArch;

    /**
     * 镜像标签，用于分类管理镜像。

## 使用说明
- 多个标签用逗号分隔
- 不超过128字符
- 示例：pytorch,python3.9,ai

     */
    private String imgLabels;

    /**
     * 目标镜像仓库注册表。

     * Required:true
     */
    @Required
    private String imageRegistry;

    /**
     * 目标镜像名称，在镜像仓库中的名称。

## 命名规则
- 通常包含命名空间
- 示例：myproject/notebook-image

     * Required:true
     */
    @Required
    private String imageName;

    /**
     * 目标镜像Tag，用于区分同一镜像的不同版本。

## 命名规则
- 仅支持小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 示例：v1.0.0, latest, 20230601

     * Required:true
     */
    @Required
    private String imageTag;

    /**
     * 注册到资产中的镜像归属权限。

## 权限类型
- public: 公开，所有用户可使用
- private: 私有，仅自己可使用
- 默认为public

     */
    private String permission;



    /**
     * get 镜像名称，用于注册到我的镜像仓库。

## 命名规则
- 长度：1~128字符
- 支持字符：小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 必须以字母开头
- 示例：my-notebook-image-v1

     *
     * @return
     */
    public String getImgName() {
        return imgName;
    }

    /**
     * set 镜像名称，用于注册到我的镜像仓库。

## 命名规则
- 长度：1~128字符
- 支持字符：小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 必须以字母开头
- 示例：my-notebook-image-v1

     *
     * @param imgName
     */
    public void setImgName(String imgName) {
        this.imgName = imgName;
    }


    /**
    * get 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

    *
    * @return
    */
    public List<String> getImgUsage() {
        return imgUsage;
    }

    /**
    * set 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

    *
    * @param imgUsage
    */
    public void setImgUsage(List<String> imgUsage) {
        this.imgUsage = imgUsage;
    }


    /**
     * get 镜像架构类型，指定镜像支持的硬件架构。

## 支持的架构
- cpu: CPU镜像
- gpu: GPU镜像

     *
     * @return
     */
    public String getImgArch() {
        return imgArch;
    }

    /**
     * set 镜像架构类型，指定镜像支持的硬件架构。

## 支持的架构
- cpu: CPU镜像
- gpu: GPU镜像

     *
     * @param imgArch
     */
    public void setImgArch(String imgArch) {
        this.imgArch = imgArch;
    }


    /**
     * get 镜像标签，用于分类管理镜像。

## 使用说明
- 多个标签用逗号分隔
- 不超过128字符
- 示例：pytorch,python3.9,ai

     *
     * @return
     */
    public String getImgLabels() {
        return imgLabels;
    }

    /**
     * set 镜像标签，用于分类管理镜像。

## 使用说明
- 多个标签用逗号分隔
- 不超过128字符
- 示例：pytorch,python3.9,ai

     *
     * @param imgLabels
     */
    public void setImgLabels(String imgLabels) {
        this.imgLabels = imgLabels;
    }


    /**
     * get 目标镜像仓库注册表。

     *
     * @return
     */
    public String getImageRegistry() {
        return imageRegistry;
    }

    /**
     * set 目标镜像仓库注册表。

     *
     * @param imageRegistry
     */
    public void setImageRegistry(String imageRegistry) {
        this.imageRegistry = imageRegistry;
    }


    /**
     * get 目标镜像名称，在镜像仓库中的名称。

## 命名规则
- 通常包含命名空间
- 示例：myproject/notebook-image

     *
     * @return
     */
    public String getImageName() {
        return imageName;
    }

    /**
     * set 目标镜像名称，在镜像仓库中的名称。

## 命名规则
- 通常包含命名空间
- 示例：myproject/notebook-image

     *
     * @param imageName
     */
    public void setImageName(String imageName) {
        this.imageName = imageName;
    }


    /**
     * get 目标镜像Tag，用于区分同一镜像的不同版本。

## 命名规则
- 仅支持小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 示例：v1.0.0, latest, 20230601

     *
     * @return
     */
    public String getImageTag() {
        return imageTag;
    }

    /**
     * set 目标镜像Tag，用于区分同一镜像的不同版本。

## 命名规则
- 仅支持小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 示例：v1.0.0, latest, 20230601

     *
     * @param imageTag
     */
    public void setImageTag(String imageTag) {
        this.imageTag = imageTag;
    }


    /**
     * get 注册到资产中的镜像归属权限。

## 权限类型
- public: 公开，所有用户可使用
- private: 私有，仅自己可使用
- 默认为public

     *
     * @return
     */
    public String getPermission() {
        return permission;
    }

    /**
     * set 注册到资产中的镜像归属权限。

## 权限类型
- public: 公开，所有用户可使用
- private: 私有，仅自己可使用
- 默认为public

     *
     * @param permission
     */
    public void setPermission(String permission) {
        this.permission = permission;
    }



    /**
     * set 镜像名称，用于注册到我的镜像仓库。

## 命名规则
- 长度：1~128字符
- 支持字符：小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 必须以字母开头
- 示例：my-notebook-image-v1

     *
     * @param imgName 镜像名称，用于注册到我的镜像仓库。

## 命名规则
- 长度：1~128字符
- 支持字符：小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 必须以字母开头
- 示例：my-notebook-image-v1

     * @return ImageSpec
     */
    public ImageSpec imgName(String imgName) {
        this.imgName = imgName;
        return this;
    }


    /**
    * set 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

    *
    * @param imgUsage 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

    * @return ImageSpec
    */
    public ImageSpec imgUsage(List<String> imgUsage) {
        this.imgUsage = imgUsage;
        return this;
    }


    /**
     * set 镜像架构类型，指定镜像支持的硬件架构。

## 支持的架构
- cpu: CPU镜像
- gpu: GPU镜像

     *
     * @param imgArch 镜像架构类型，指定镜像支持的硬件架构。

## 支持的架构
- cpu: CPU镜像
- gpu: GPU镜像

     * @return ImageSpec
     */
    public ImageSpec imgArch(String imgArch) {
        this.imgArch = imgArch;
        return this;
    }


    /**
     * set 镜像标签，用于分类管理镜像。

## 使用说明
- 多个标签用逗号分隔
- 不超过128字符
- 示例：pytorch,python3.9,ai

     *
     * @param imgLabels 镜像标签，用于分类管理镜像。

## 使用说明
- 多个标签用逗号分隔
- 不超过128字符
- 示例：pytorch,python3.9,ai

     * @return ImageSpec
     */
    public ImageSpec imgLabels(String imgLabels) {
        this.imgLabels = imgLabels;
        return this;
    }


    /**
     * set 目标镜像仓库注册表。

     *
     * @param imageRegistry 目标镜像仓库注册表。

     * @return ImageSpec
     */
    public ImageSpec imageRegistry(String imageRegistry) {
        this.imageRegistry = imageRegistry;
        return this;
    }


    /**
     * set 目标镜像名称，在镜像仓库中的名称。

## 命名规则
- 通常包含命名空间
- 示例：myproject/notebook-image

     *
     * @param imageName 目标镜像名称，在镜像仓库中的名称。

## 命名规则
- 通常包含命名空间
- 示例：myproject/notebook-image

     * @return ImageSpec
     */
    public ImageSpec imageName(String imageName) {
        this.imageName = imageName;
        return this;
    }


    /**
     * set 目标镜像Tag，用于区分同一镜像的不同版本。

## 命名规则
- 仅支持小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 示例：v1.0.0, latest, 20230601

     *
     * @param imageTag 目标镜像Tag，用于区分同一镜像的不同版本。

## 命名规则
- 仅支持小写字母、数字、英文中划线&quot;-&quot;、英文下划线&quot;_&quot;、点&quot;.&quot;
- 示例：v1.0.0, latest, 20230601

     * @return ImageSpec
     */
    public ImageSpec imageTag(String imageTag) {
        this.imageTag = imageTag;
        return this;
    }


    /**
     * set 注册到资产中的镜像归属权限。

## 权限类型
- public: 公开，所有用户可使用
- private: 私有，仅自己可使用
- 默认为public

     *
     * @param permission 注册到资产中的镜像归属权限。

## 权限类型
- public: 公开，所有用户可使用
- private: 私有，仅自己可使用
- 默认为public

     * @return ImageSpec
     */
    public ImageSpec permission(String permission) {
        this.permission = permission;
        return this;
    }



    /**
     * add item to 镜像用途，指定镜像可用于哪些场景。

## 支持的用途
- training: 训练任务
- notebook: Notebook开发环境
- inference: 推理服务
- simulation: 具身仿真
- 可多选

     *
     * @param imgUsage
     */
    public void addImgUsage(String imgUsage) {
        if (this.imgUsage == null) {
            this.imgUsage = new ArrayList<>();
        }
        this.imgUsage.add(imgUsage);
    }
}