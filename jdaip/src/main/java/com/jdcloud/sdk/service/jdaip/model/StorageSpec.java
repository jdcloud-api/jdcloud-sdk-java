/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import com.jdcloud.sdk.annotation.Required;

/**
 * 存储配置，定义Notebook挂载的存储资源。

## 存储类型说明
- **cfs**: 京东云文件存储(CFS)，基于NFS协议，适合共享存储场景
- **oss**: 京东云对象存储(OSS)，适合大数据存储和备份场景
- **jpfs**: 京东云并行文件系统(JPFS)，高性能存储，适合AI训练场景

## 挂载规则
- 第一个存储作为工作目录，必须挂载到&#x60;/mnt/workspace&#x60;
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 网络要求
- cfs和jpfs类型存储需要与队列在同一VPC下
- 跨VPC需要打通对等连接(VPC Peering)

 */
public class StorageSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 存储类型，决定存储后端的类型和访问方式。

## 支持类型
- cfs: 京东云文件存储，NFS协议
- oss: 京东云对象存储，S3协议
- jpfs: 京东云并行文件系统，高性能

## 选择建议
- 开发调试: cfs，成本低
- 大数据存储: oss，容量大
- 高性能训练: jpfs，速度快

     * Required:true
     */
    @Required
    private String storageType;

    /**
     * 存储资源ID，用于标识存储实例。

## 各类型ID格式
- cfs: 文件系统ID，如 fs-xxxxxxxx
- oss: Bucket名称
- jpfs: 文件系统ID，如 jfs-xxxxxxxx

     * Required:true
     */
    @Required
    private String storageId;

    /**
     * 存储路径，指定要挂载的目录或对象路径。

## 各类型路径格式
- cfs: &#x60;10.0.23.45:/dir-path&#x60; 或 &#x60;/dir-path&#x60;
- oss: &#x60;oss://bucket.s3.cn-north-1.jdcloud-oss.com/object-path&#x60;
- jpfs: &#x60;fs-xxxxxxxxxx:/dir-path&#x60; 或 &#x60;/dir-path&#x60;

## 使用说明
- 路径必须存在，否则挂载失败
- 支持挂载子目录

     * Required:true
     */
    @Required
    private String storagePath;

    /**
     * 挂载路径，指定存储在Notebook容器中的挂载位置。

## 挂载规则
- 第一个存储必须挂载到&#x60;/mnt/workspace&#x60;，作为工作目录
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 系统目录列表
&#x60;/, /bin, /boot, /dev, /etc, /home, /lib, /lib32, /lib64, /libx32, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var&#x60;

     * Required:true
     */
    @Required
    private String mountPath;

    /**
     * 是否以只读模式挂载存储。

## 使用说明
- 仅oss类型存储有效
- true: 只读模式，无法写入
- false: 读写模式，可读写
- 默认为false(读写模式)

     */
    private Boolean readonly;



    /**
     * get 存储类型，决定存储后端的类型和访问方式。

## 支持类型
- cfs: 京东云文件存储，NFS协议
- oss: 京东云对象存储，S3协议
- jpfs: 京东云并行文件系统，高性能

## 选择建议
- 开发调试: cfs，成本低
- 大数据存储: oss，容量大
- 高性能训练: jpfs，速度快

     *
     * @return
     */
    public String getStorageType() {
        return storageType;
    }

    /**
     * set 存储类型，决定存储后端的类型和访问方式。

## 支持类型
- cfs: 京东云文件存储，NFS协议
- oss: 京东云对象存储，S3协议
- jpfs: 京东云并行文件系统，高性能

## 选择建议
- 开发调试: cfs，成本低
- 大数据存储: oss，容量大
- 高性能训练: jpfs，速度快

     *
     * @param storageType
     */
    public void setStorageType(String storageType) {
        this.storageType = storageType;
    }


    /**
     * get 存储资源ID，用于标识存储实例。

## 各类型ID格式
- cfs: 文件系统ID，如 fs-xxxxxxxx
- oss: Bucket名称
- jpfs: 文件系统ID，如 jfs-xxxxxxxx

     *
     * @return
     */
    public String getStorageId() {
        return storageId;
    }

    /**
     * set 存储资源ID，用于标识存储实例。

## 各类型ID格式
- cfs: 文件系统ID，如 fs-xxxxxxxx
- oss: Bucket名称
- jpfs: 文件系统ID，如 jfs-xxxxxxxx

     *
     * @param storageId
     */
    public void setStorageId(String storageId) {
        this.storageId = storageId;
    }


    /**
     * get 存储路径，指定要挂载的目录或对象路径。

## 各类型路径格式
- cfs: &#x60;10.0.23.45:/dir-path&#x60; 或 &#x60;/dir-path&#x60;
- oss: &#x60;oss://bucket.s3.cn-north-1.jdcloud-oss.com/object-path&#x60;
- jpfs: &#x60;fs-xxxxxxxxxx:/dir-path&#x60; 或 &#x60;/dir-path&#x60;

## 使用说明
- 路径必须存在，否则挂载失败
- 支持挂载子目录

     *
     * @return
     */
    public String getStoragePath() {
        return storagePath;
    }

    /**
     * set 存储路径，指定要挂载的目录或对象路径。

## 各类型路径格式
- cfs: &#x60;10.0.23.45:/dir-path&#x60; 或 &#x60;/dir-path&#x60;
- oss: &#x60;oss://bucket.s3.cn-north-1.jdcloud-oss.com/object-path&#x60;
- jpfs: &#x60;fs-xxxxxxxxxx:/dir-path&#x60; 或 &#x60;/dir-path&#x60;

## 使用说明
- 路径必须存在，否则挂载失败
- 支持挂载子目录

     *
     * @param storagePath
     */
    public void setStoragePath(String storagePath) {
        this.storagePath = storagePath;
    }


    /**
     * get 挂载路径，指定存储在Notebook容器中的挂载位置。

## 挂载规则
- 第一个存储必须挂载到&#x60;/mnt/workspace&#x60;，作为工作目录
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 系统目录列表
&#x60;/, /bin, /boot, /dev, /etc, /home, /lib, /lib32, /lib64, /libx32, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var&#x60;

     *
     * @return
     */
    public String getMountPath() {
        return mountPath;
    }

    /**
     * set 挂载路径，指定存储在Notebook容器中的挂载位置。

## 挂载规则
- 第一个存储必须挂载到&#x60;/mnt/workspace&#x60;，作为工作目录
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 系统目录列表
&#x60;/, /bin, /boot, /dev, /etc, /home, /lib, /lib32, /lib64, /libx32, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var&#x60;

     *
     * @param mountPath
     */
    public void setMountPath(String mountPath) {
        this.mountPath = mountPath;
    }


    /**
     * get 是否以只读模式挂载存储。

## 使用说明
- 仅oss类型存储有效
- true: 只读模式，无法写入
- false: 读写模式，可读写
- 默认为false(读写模式)

     *
     * @return
     */
    public Boolean getReadonly() {
        return readonly;
    }

    /**
     * set 是否以只读模式挂载存储。

## 使用说明
- 仅oss类型存储有效
- true: 只读模式，无法写入
- false: 读写模式，可读写
- 默认为false(读写模式)

     *
     * @param readonly
     */
    public void setReadonly(Boolean readonly) {
        this.readonly = readonly;
    }



    /**
     * set 存储类型，决定存储后端的类型和访问方式。

## 支持类型
- cfs: 京东云文件存储，NFS协议
- oss: 京东云对象存储，S3协议
- jpfs: 京东云并行文件系统，高性能

## 选择建议
- 开发调试: cfs，成本低
- 大数据存储: oss，容量大
- 高性能训练: jpfs，速度快

     *
     * @param storageType 存储类型，决定存储后端的类型和访问方式。

## 支持类型
- cfs: 京东云文件存储，NFS协议
- oss: 京东云对象存储，S3协议
- jpfs: 京东云并行文件系统，高性能

## 选择建议
- 开发调试: cfs，成本低
- 大数据存储: oss，容量大
- 高性能训练: jpfs，速度快

     * @return StorageSpec
     */
    public StorageSpec storageType(String storageType) {
        this.storageType = storageType;
        return this;
    }


    /**
     * set 存储资源ID，用于标识存储实例。

## 各类型ID格式
- cfs: 文件系统ID，如 fs-xxxxxxxx
- oss: Bucket名称
- jpfs: 文件系统ID，如 jfs-xxxxxxxx

     *
     * @param storageId 存储资源ID，用于标识存储实例。

## 各类型ID格式
- cfs: 文件系统ID，如 fs-xxxxxxxx
- oss: Bucket名称
- jpfs: 文件系统ID，如 jfs-xxxxxxxx

     * @return StorageSpec
     */
    public StorageSpec storageId(String storageId) {
        this.storageId = storageId;
        return this;
    }


    /**
     * set 存储路径，指定要挂载的目录或对象路径。

## 各类型路径格式
- cfs: &#x60;10.0.23.45:/dir-path&#x60; 或 &#x60;/dir-path&#x60;
- oss: &#x60;oss://bucket.s3.cn-north-1.jdcloud-oss.com/object-path&#x60;
- jpfs: &#x60;fs-xxxxxxxxxx:/dir-path&#x60; 或 &#x60;/dir-path&#x60;

## 使用说明
- 路径必须存在，否则挂载失败
- 支持挂载子目录

     *
     * @param storagePath 存储路径，指定要挂载的目录或对象路径。

## 各类型路径格式
- cfs: &#x60;10.0.23.45:/dir-path&#x60; 或 &#x60;/dir-path&#x60;
- oss: &#x60;oss://bucket.s3.cn-north-1.jdcloud-oss.com/object-path&#x60;
- jpfs: &#x60;fs-xxxxxxxxxx:/dir-path&#x60; 或 &#x60;/dir-path&#x60;

## 使用说明
- 路径必须存在，否则挂载失败
- 支持挂载子目录

     * @return StorageSpec
     */
    public StorageSpec storagePath(String storagePath) {
        this.storagePath = storagePath;
        return this;
    }


    /**
     * set 挂载路径，指定存储在Notebook容器中的挂载位置。

## 挂载规则
- 第一个存储必须挂载到&#x60;/mnt/workspace&#x60;，作为工作目录
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 系统目录列表
&#x60;/, /bin, /boot, /dev, /etc, /home, /lib, /lib32, /lib64, /libx32, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var&#x60;

     *
     * @param mountPath 挂载路径，指定存储在Notebook容器中的挂载位置。

## 挂载规则
- 第一个存储必须挂载到&#x60;/mnt/workspace&#x60;，作为工作目录
- 其他存储建议挂载到&#x60;/mnt/&#x60;开头的路径
- 不支持挂载到系统目录

## 系统目录列表
&#x60;/, /bin, /boot, /dev, /etc, /home, /lib, /lib32, /lib64, /libx32, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var&#x60;

     * @return StorageSpec
     */
    public StorageSpec mountPath(String mountPath) {
        this.mountPath = mountPath;
        return this;
    }


    /**
     * set 是否以只读模式挂载存储。

## 使用说明
- 仅oss类型存储有效
- true: 只读模式，无法写入
- false: 读写模式，可读写
- 默认为false(读写模式)

     *
     * @param readonly 是否以只读模式挂载存储。

## 使用说明
- 仅oss类型存储有效
- true: 只读模式，无法写入
- false: 读写模式，可读写
- 默认为false(读写模式)

     * @return StorageSpec
     */
    public StorageSpec readonly(Boolean readonly) {
        this.readonly = readonly;
        return this;
    }


}