/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import java.util.List;
import java.util.ArrayList;

/**
 * 重启策略配置，定义训练任务异常时的自动重启行为。

**适用范围：**
- 仅支持 **异构节点池**（专属资源池）
- 仅支持 **PyTorch** 类型任务
- 公共资源池和 Ray 类型任务不支持此功能

**启用重启策略后，系统会在以下情况自动重启任务：**
1. 节点硬件故障
2. 任务进程异常退出
3. 算力环境异常
4. 任务长时间无响应
5. 进程状态异常

**注意：** 自动重启本质是删除旧实例并创建全新的训练实例（Pod），顶层字段 &#x60;localStorage&#x60; 配置的本地存储数据不会保留，请勿依赖本地存储保存需要跨重启保留的关键数据，建议配合 checkpoint 机制将关键数据保存至共享存储（OSS/CFS/JPFS）

 */
public class RestartPolicyParamForJob  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 是否启用自动重启。

**可选值：**
- &#x60;0&#x60;：禁用自动重启
- &#x60;1&#x60;：启用自动重启

**默认值：** &#x60;0&#x60;（禁用）

     */
    private Integer enable;

    /**
     * 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

     */
    
    private List<String> triggerConditions;
    /**
     * 任务卡住超时时间，超过此时间未响应将被判定为卡住并触发重启。

**单位：** 秒

**默认值：** 1800（30分钟）

**取值范围：** 60 ~ 86400（1分钟 ~ 24小时）

**建议：** 根据训练任务的 checkpoint 间隔设置，避免数据丢失过多

     */
    private Integer taskHangTimeout;

    /**
     * 进程巡检启动延迟时间，任务运行超过此时间后才开始进程巡检。

**单位：** 秒

**默认值：** 300（5分钟）

**取值范围：** 180 ~ 86400（3分钟 ~ 24小时）

**说明：** 给任务足够的启动时间，避免误判

     */
    private Integer processInspectionStartDelay;

    /**
     * 最大重启次数，超过此次数后将不再重启，任务保持失败状态。

**默认值：** 3

**取值范围：** 1 ~ 10

**建议：** 根据任务重要性设置，重要任务可设置较大的重启次数

     */
    private Integer maxRestartCount;



    /**
     * get 是否启用自动重启。

**可选值：**
- &#x60;0&#x60;：禁用自动重启
- &#x60;1&#x60;：启用自动重启

**默认值：** &#x60;0&#x60;（禁用）

     *
     * @return
     */
    public Integer getEnable() {
        return enable;
    }

    /**
     * set 是否启用自动重启。

**可选值：**
- &#x60;0&#x60;：禁用自动重启
- &#x60;1&#x60;：启用自动重启

**默认值：** &#x60;0&#x60;（禁用）

     *
     * @param enable
     */
    public void setEnable(Integer enable) {
        this.enable = enable;
    }


    /**
    * get 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

    *
    * @return
    */
    public List<String> getTriggerConditions() {
        return triggerConditions;
    }

    /**
    * set 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

    *
    * @param triggerConditions
    */
    public void setTriggerConditions(List<String> triggerConditions) {
        this.triggerConditions = triggerConditions;
    }


    /**
     * get 任务卡住超时时间，超过此时间未响应将被判定为卡住并触发重启。

**单位：** 秒

**默认值：** 1800（30分钟）

**取值范围：** 60 ~ 86400（1分钟 ~ 24小时）

**建议：** 根据训练任务的 checkpoint 间隔设置，避免数据丢失过多

     *
     * @return
     */
    public Integer getTaskHangTimeout() {
        return taskHangTimeout;
    }

    /**
     * set 任务卡住超时时间，超过此时间未响应将被判定为卡住并触发重启。

**单位：** 秒

**默认值：** 1800（30分钟）

**取值范围：** 60 ~ 86400（1分钟 ~ 24小时）

**建议：** 根据训练任务的 checkpoint 间隔设置，避免数据丢失过多

     *
     * @param taskHangTimeout
     */
    public void setTaskHangTimeout(Integer taskHangTimeout) {
        this.taskHangTimeout = taskHangTimeout;
    }


    /**
     * get 进程巡检启动延迟时间，任务运行超过此时间后才开始进程巡检。

**单位：** 秒

**默认值：** 300（5分钟）

**取值范围：** 180 ~ 86400（3分钟 ~ 24小时）

**说明：** 给任务足够的启动时间，避免误判

     *
     * @return
     */
    public Integer getProcessInspectionStartDelay() {
        return processInspectionStartDelay;
    }

    /**
     * set 进程巡检启动延迟时间，任务运行超过此时间后才开始进程巡检。

**单位：** 秒

**默认值：** 300（5分钟）

**取值范围：** 180 ~ 86400（3分钟 ~ 24小时）

**说明：** 给任务足够的启动时间，避免误判

     *
     * @param processInspectionStartDelay
     */
    public void setProcessInspectionStartDelay(Integer processInspectionStartDelay) {
        this.processInspectionStartDelay = processInspectionStartDelay;
    }


    /**
     * get 最大重启次数，超过此次数后将不再重启，任务保持失败状态。

**默认值：** 3

**取值范围：** 1 ~ 10

**建议：** 根据任务重要性设置，重要任务可设置较大的重启次数

     *
     * @return
     */
    public Integer getMaxRestartCount() {
        return maxRestartCount;
    }

    /**
     * set 最大重启次数，超过此次数后将不再重启，任务保持失败状态。

**默认值：** 3

**取值范围：** 1 ~ 10

**建议：** 根据任务重要性设置，重要任务可设置较大的重启次数

     *
     * @param maxRestartCount
     */
    public void setMaxRestartCount(Integer maxRestartCount) {
        this.maxRestartCount = maxRestartCount;
    }



    /**
     * set 是否启用自动重启。

**可选值：**
- &#x60;0&#x60;：禁用自动重启
- &#x60;1&#x60;：启用自动重启

**默认值：** &#x60;0&#x60;（禁用）

     *
     * @param enable 是否启用自动重启。

**可选值：**
- &#x60;0&#x60;：禁用自动重启
- &#x60;1&#x60;：启用自动重启

**默认值：** &#x60;0&#x60;（禁用）

     * @return RestartPolicyParamForJob
     */
    public RestartPolicyParamForJob enable(Integer enable) {
        this.enable = enable;
        return this;
    }


    /**
    * set 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

    *
    * @param triggerConditions 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

    * @return RestartPolicyParamForJob
    */
    public RestartPolicyParamForJob triggerConditions(List<String> triggerConditions) {
        this.triggerConditions = triggerConditions;
        return this;
    }


    /**
     * set 任务卡住超时时间，超过此时间未响应将被判定为卡住并触发重启。

**单位：** 秒

**默认值：** 1800（30分钟）

**取值范围：** 60 ~ 86400（1分钟 ~ 24小时）

**建议：** 根据训练任务的 checkpoint 间隔设置，避免数据丢失过多

     *
     * @param taskHangTimeout 任务卡住超时时间，超过此时间未响应将被判定为卡住并触发重启。

**单位：** 秒

**默认值：** 1800（30分钟）

**取值范围：** 60 ~ 86400（1分钟 ~ 24小时）

**建议：** 根据训练任务的 checkpoint 间隔设置，避免数据丢失过多

     * @return RestartPolicyParamForJob
     */
    public RestartPolicyParamForJob taskHangTimeout(Integer taskHangTimeout) {
        this.taskHangTimeout = taskHangTimeout;
        return this;
    }


    /**
     * set 进程巡检启动延迟时间，任务运行超过此时间后才开始进程巡检。

**单位：** 秒

**默认值：** 300（5分钟）

**取值范围：** 180 ~ 86400（3分钟 ~ 24小时）

**说明：** 给任务足够的启动时间，避免误判

     *
     * @param processInspectionStartDelay 进程巡检启动延迟时间，任务运行超过此时间后才开始进程巡检。

**单位：** 秒

**默认值：** 300（5分钟）

**取值范围：** 180 ~ 86400（3分钟 ~ 24小时）

**说明：** 给任务足够的启动时间，避免误判

     * @return RestartPolicyParamForJob
     */
    public RestartPolicyParamForJob processInspectionStartDelay(Integer processInspectionStartDelay) {
        this.processInspectionStartDelay = processInspectionStartDelay;
        return this;
    }


    /**
     * set 最大重启次数，超过此次数后将不再重启，任务保持失败状态。

**默认值：** 3

**取值范围：** 1 ~ 10

**建议：** 根据任务重要性设置，重要任务可设置较大的重启次数

     *
     * @param maxRestartCount 最大重启次数，超过此次数后将不再重启，任务保持失败状态。

**默认值：** 3

**取值范围：** 1 ~ 10

**建议：** 根据任务重要性设置，重要任务可设置较大的重启次数

     * @return RestartPolicyParamForJob
     */
    public RestartPolicyParamForJob maxRestartCount(Integer maxRestartCount) {
        this.maxRestartCount = maxRestartCount;
        return this;
    }



    /**
     * add item to 触发重启的条件列表。

**可选值：**
- &#x60;resource-failed&#x60;：节点故障（如 GPU 硬件故障、节点宕机）
- &#x60;task-failed&#x60;：任务失败（如代码错误、内存溢出）
- &#x60;environment-failed&#x60;：算力健康检测不通过（如 GPU 驱动异常、CUDA 错误）
- &#x60;task-hang&#x60;：任务卡住（如死锁、梯度爆炸导致长时间无响应）
- &#x60;process-inspection-failed&#x60;：进程巡检失败（如进程僵死、资源泄露）

**默认值：** &#x60;[&quot;resource-failed&quot;]&#x60;

**注意：** 可选择多个条件

     *
     * @param triggerCondition
     */
    public void addTriggerCondition(String triggerCondition) {
        if (this.triggerConditions == null) {
            this.triggerConditions = new ArrayList<>();
        }
        this.triggerConditions.add(triggerCondition);
    }
}