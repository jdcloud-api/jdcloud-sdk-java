/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;


/**
 * **已废弃：** 请使用 &#x60;RoleResourceParamForJob&#x60; 

资源配置信息，定义训练任务所需的计算资源。

**资源配置方式：**
- 使用队列（queueId）+ 规格（flavorId）：适用于公共资源池和共享资源池
- 使用队列 + 自定义资源：适用于专属资源池

**资源单位说明：**
- GPU：虚拟 GPU 核数，支持小数（如 0.5 核）
- CPU：毫核（milliCore），1000m &#x3D; 1 核
- 内存：MiB（1024 KiB）

 */
public class ResourceParamForJob  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池/安全资源池：** 使用创建队列时返回的队列ID

**示例：**
- 公共资源池：&#x60;joybuilder-public-queue&#x60;
- 共享资源池：&#x60;joybuilder-exclusive-queue&#x60;
- 专属资源池：&#x60;queue-2xxx**********2d*********8b8&#x60;

     */
    private String queueId;

    /**
     * 规格ID，指定公共资源池或共享资源池的计算规格。

**说明：**
- 公共资源池和共享资源池必须填写此字段
- 专属资源池无需填写，由自定义资源配置决定

**获取方式：** 通过规格查询接口获取可用规格列表

     */
    private String flavorId;

    /**
     * 规格描述。

     */
    private Object flavorInfo;

    /**
     * GPU 卡类型，指定使用的 GPU 型号。

**常见型号：**
- &#x60;NVIDIA_G2&#x60;：通用 GPU 型号
- &#x60;NVIDIA_V100&#x60;：Tesla V100
- &#x60;NVIDIA_A100&#x60;：Ampere A100

**注意：** 不同地域支持的 GPU 型号可能不同

     */
    private String gpuDeviceModel;

    /**
     * 虚拟 GPU 核数，支持细粒度的 GPU 资源分配。

**可选值（英伟达）：**
- 小规格：&#x60;0.1&#x60;, &#x60;0.125&#x60;, &#x60;0.25&#x60;, &#x60;0.5&#x60;
- 中规格：&#x60;1&#x60;, &#x60;2&#x60;, &#x60;3&#x60;, &#x60;4&#x60;
- 大规格：&#x60;5&#x60;, &#x60;6&#x60;, &#x60;7&#x60;, &#x60;8&#x60;

**昇腾整机规格：** &#x60;16&#x60;

**使用建议：**
- 小模型训练：0.5 ~ 2 核
- 中等模型：2 ~ 4 核
- 大模型训练：4 ~ 8 核

     */
    private Double vcudaCore;

    /**
     * CPU 大小，以毫核为单位。

**换算关系：**
- 1000m &#x3D; 1 核 CPU
- 100m &#x3D; 0.1 核 CPU

**示例：**
- 4000m &#x3D; 4 核 CPU
- 32000m &#x3D; 32 核 CPU

**建议：** CPU 与 GPU 比例建议 4:1 ~ 8:1

     */
    private Integer cpuMilli;

    /**
     * 内存大小，以 MiB 为单位。

**换算关系：**
- 1024 MiB &#x3D; 1 GiB

**示例：**
- 16384 &#x3D; 16 GiB
- 65536 &#x3D; 64 GiB

**建议：** 内存建议为 GPU 显存的 2~4 倍

     */
    private Integer memoryMiB;



    /**
     * get 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池/安全资源池：** 使用创建队列时返回的队列ID

**示例：**
- 公共资源池：&#x60;joybuilder-public-queue&#x60;
- 共享资源池：&#x60;joybuilder-exclusive-queue&#x60;
- 专属资源池：&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @return
     */
    public String getQueueId() {
        return queueId;
    }

    /**
     * set 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池/安全资源池：** 使用创建队列时返回的队列ID

**示例：**
- 公共资源池：&#x60;joybuilder-public-queue&#x60;
- 共享资源池：&#x60;joybuilder-exclusive-queue&#x60;
- 专属资源池：&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @param queueId
     */
    public void setQueueId(String queueId) {
        this.queueId = queueId;
    }


    /**
     * get 规格ID，指定公共资源池或共享资源池的计算规格。

**说明：**
- 公共资源池和共享资源池必须填写此字段
- 专属资源池无需填写，由自定义资源配置决定

**获取方式：** 通过规格查询接口获取可用规格列表

     *
     * @return
     */
    public String getFlavorId() {
        return flavorId;
    }

    /**
     * set 规格ID，指定公共资源池或共享资源池的计算规格。

**说明：**
- 公共资源池和共享资源池必须填写此字段
- 专属资源池无需填写，由自定义资源配置决定

**获取方式：** 通过规格查询接口获取可用规格列表

     *
     * @param flavorId
     */
    public void setFlavorId(String flavorId) {
        this.flavorId = flavorId;
    }


    /**
     * get 规格描述。

     *
     * @return
     */
    public Object getFlavorInfo() {
        return flavorInfo;
    }

    /**
     * set 规格描述。

     *
     * @param flavorInfo
     */
    public void setFlavorInfo(Object flavorInfo) {
        this.flavorInfo = flavorInfo;
    }


    /**
     * get GPU 卡类型，指定使用的 GPU 型号。

**常见型号：**
- &#x60;NVIDIA_G2&#x60;：通用 GPU 型号
- &#x60;NVIDIA_V100&#x60;：Tesla V100
- &#x60;NVIDIA_A100&#x60;：Ampere A100

**注意：** 不同地域支持的 GPU 型号可能不同

     *
     * @return
     */
    public String getGpuDeviceModel() {
        return gpuDeviceModel;
    }

    /**
     * set GPU 卡类型，指定使用的 GPU 型号。

**常见型号：**
- &#x60;NVIDIA_G2&#x60;：通用 GPU 型号
- &#x60;NVIDIA_V100&#x60;：Tesla V100
- &#x60;NVIDIA_A100&#x60;：Ampere A100

**注意：** 不同地域支持的 GPU 型号可能不同

     *
     * @param gpuDeviceModel
     */
    public void setGpuDeviceModel(String gpuDeviceModel) {
        this.gpuDeviceModel = gpuDeviceModel;
    }


    /**
     * get 虚拟 GPU 核数，支持细粒度的 GPU 资源分配。

**可选值（英伟达）：**
- 小规格：&#x60;0.1&#x60;, &#x60;0.125&#x60;, &#x60;0.25&#x60;, &#x60;0.5&#x60;
- 中规格：&#x60;1&#x60;, &#x60;2&#x60;, &#x60;3&#x60;, &#x60;4&#x60;
- 大规格：&#x60;5&#x60;, &#x60;6&#x60;, &#x60;7&#x60;, &#x60;8&#x60;

**昇腾整机规格：** &#x60;16&#x60;

**使用建议：**
- 小模型训练：0.5 ~ 2 核
- 中等模型：2 ~ 4 核
- 大模型训练：4 ~ 8 核

     *
     * @return
     */
    public Double getVcudaCore() {
        return vcudaCore;
    }

    /**
     * set 虚拟 GPU 核数，支持细粒度的 GPU 资源分配。

**可选值（英伟达）：**
- 小规格：&#x60;0.1&#x60;, &#x60;0.125&#x60;, &#x60;0.25&#x60;, &#x60;0.5&#x60;
- 中规格：&#x60;1&#x60;, &#x60;2&#x60;, &#x60;3&#x60;, &#x60;4&#x60;
- 大规格：&#x60;5&#x60;, &#x60;6&#x60;, &#x60;7&#x60;, &#x60;8&#x60;

**昇腾整机规格：** &#x60;16&#x60;

**使用建议：**
- 小模型训练：0.5 ~ 2 核
- 中等模型：2 ~ 4 核
- 大模型训练：4 ~ 8 核

     *
     * @param vcudaCore
     */
    public void setVcudaCore(Double vcudaCore) {
        this.vcudaCore = vcudaCore;
    }


    /**
     * get CPU 大小，以毫核为单位。

**换算关系：**
- 1000m &#x3D; 1 核 CPU
- 100m &#x3D; 0.1 核 CPU

**示例：**
- 4000m &#x3D; 4 核 CPU
- 32000m &#x3D; 32 核 CPU

**建议：** CPU 与 GPU 比例建议 4:1 ~ 8:1

     *
     * @return
     */
    public Integer getCpuMilli() {
        return cpuMilli;
    }

    /**
     * set CPU 大小，以毫核为单位。

**换算关系：**
- 1000m &#x3D; 1 核 CPU
- 100m &#x3D; 0.1 核 CPU

**示例：**
- 4000m &#x3D; 4 核 CPU
- 32000m &#x3D; 32 核 CPU

**建议：** CPU 与 GPU 比例建议 4:1 ~ 8:1

     *
     * @param cpuMilli
     */
    public void setCpuMilli(Integer cpuMilli) {
        this.cpuMilli = cpuMilli;
    }


    /**
     * get 内存大小，以 MiB 为单位。

**换算关系：**
- 1024 MiB &#x3D; 1 GiB

**示例：**
- 16384 &#x3D; 16 GiB
- 65536 &#x3D; 64 GiB

**建议：** 内存建议为 GPU 显存的 2~4 倍

     *
     * @return
     */
    public Integer getMemoryMiB() {
        return memoryMiB;
    }

    /**
     * set 内存大小，以 MiB 为单位。

**换算关系：**
- 1024 MiB &#x3D; 1 GiB

**示例：**
- 16384 &#x3D; 16 GiB
- 65536 &#x3D; 64 GiB

**建议：** 内存建议为 GPU 显存的 2~4 倍

     *
     * @param memoryMiB
     */
    public void setMemoryMiB(Integer memoryMiB) {
        this.memoryMiB = memoryMiB;
    }



    /**
     * set 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池/安全资源池：** 使用创建队列时返回的队列ID

**示例：**
- 公共资源池：&#x60;joybuilder-public-queue&#x60;
- 共享资源池：&#x60;joybuilder-exclusive-queue&#x60;
- 专属资源池：&#x60;queue-2xxx**********2d*********8b8&#x60;

     *
     * @param queueId 队列ID，指定任务运行的资源队列。

**公共资源池：** 固定使用 &#x60;joybuilder-public-queue&#x60;

**共享资源池：** 固定使用 &#x60;joybuilder-exclusive-queue&#x60;

**专属资源池/安全资源池：** 使用创建队列时返回的队列ID

**示例：**
- 公共资源池：&#x60;joybuilder-public-queue&#x60;
- 共享资源池：&#x60;joybuilder-exclusive-queue&#x60;
- 专属资源池：&#x60;queue-2xxx**********2d*********8b8&#x60;

     * @return ResourceParamForJob
     */
    public ResourceParamForJob queueId(String queueId) {
        this.queueId = queueId;
        return this;
    }


    /**
     * set 规格ID，指定公共资源池或共享资源池的计算规格。

**说明：**
- 公共资源池和共享资源池必须填写此字段
- 专属资源池无需填写，由自定义资源配置决定

**获取方式：** 通过规格查询接口获取可用规格列表

     *
     * @param flavorId 规格ID，指定公共资源池或共享资源池的计算规格。

**说明：**
- 公共资源池和共享资源池必须填写此字段
- 专属资源池无需填写，由自定义资源配置决定

**获取方式：** 通过规格查询接口获取可用规格列表

     * @return ResourceParamForJob
     */
    public ResourceParamForJob flavorId(String flavorId) {
        this.flavorId = flavorId;
        return this;
    }


    /**
     * set 规格描述。

     *
     * @param flavorInfo 规格描述。

     * @return ResourceParamForJob
     */
    public ResourceParamForJob flavorInfo(Object flavorInfo) {
        this.flavorInfo = flavorInfo;
        return this;
    }


    /**
     * set GPU 卡类型，指定使用的 GPU 型号。

**常见型号：**
- &#x60;NVIDIA_G2&#x60;：通用 GPU 型号
- &#x60;NVIDIA_V100&#x60;：Tesla V100
- &#x60;NVIDIA_A100&#x60;：Ampere A100

**注意：** 不同地域支持的 GPU 型号可能不同

     *
     * @param gpuDeviceModel GPU 卡类型，指定使用的 GPU 型号。

**常见型号：**
- &#x60;NVIDIA_G2&#x60;：通用 GPU 型号
- &#x60;NVIDIA_V100&#x60;：Tesla V100
- &#x60;NVIDIA_A100&#x60;：Ampere A100

**注意：** 不同地域支持的 GPU 型号可能不同

     * @return ResourceParamForJob
     */
    public ResourceParamForJob gpuDeviceModel(String gpuDeviceModel) {
        this.gpuDeviceModel = gpuDeviceModel;
        return this;
    }


    /**
     * set 虚拟 GPU 核数，支持细粒度的 GPU 资源分配。

**可选值（英伟达）：**
- 小规格：&#x60;0.1&#x60;, &#x60;0.125&#x60;, &#x60;0.25&#x60;, &#x60;0.5&#x60;
- 中规格：&#x60;1&#x60;, &#x60;2&#x60;, &#x60;3&#x60;, &#x60;4&#x60;
- 大规格：&#x60;5&#x60;, &#x60;6&#x60;, &#x60;7&#x60;, &#x60;8&#x60;

**昇腾整机规格：** &#x60;16&#x60;

**使用建议：**
- 小模型训练：0.5 ~ 2 核
- 中等模型：2 ~ 4 核
- 大模型训练：4 ~ 8 核

     *
     * @param vcudaCore 虚拟 GPU 核数，支持细粒度的 GPU 资源分配。

**可选值（英伟达）：**
- 小规格：&#x60;0.1&#x60;, &#x60;0.125&#x60;, &#x60;0.25&#x60;, &#x60;0.5&#x60;
- 中规格：&#x60;1&#x60;, &#x60;2&#x60;, &#x60;3&#x60;, &#x60;4&#x60;
- 大规格：&#x60;5&#x60;, &#x60;6&#x60;, &#x60;7&#x60;, &#x60;8&#x60;

**昇腾整机规格：** &#x60;16&#x60;

**使用建议：**
- 小模型训练：0.5 ~ 2 核
- 中等模型：2 ~ 4 核
- 大模型训练：4 ~ 8 核

     * @return ResourceParamForJob
     */
    public ResourceParamForJob vcudaCore(Double vcudaCore) {
        this.vcudaCore = vcudaCore;
        return this;
    }


    /**
     * set CPU 大小，以毫核为单位。

**换算关系：**
- 1000m &#x3D; 1 核 CPU
- 100m &#x3D; 0.1 核 CPU

**示例：**
- 4000m &#x3D; 4 核 CPU
- 32000m &#x3D; 32 核 CPU

**建议：** CPU 与 GPU 比例建议 4:1 ~ 8:1

     *
     * @param cpuMilli CPU 大小，以毫核为单位。

**换算关系：**
- 1000m &#x3D; 1 核 CPU
- 100m &#x3D; 0.1 核 CPU

**示例：**
- 4000m &#x3D; 4 核 CPU
- 32000m &#x3D; 32 核 CPU

**建议：** CPU 与 GPU 比例建议 4:1 ~ 8:1

     * @return ResourceParamForJob
     */
    public ResourceParamForJob cpuMilli(Integer cpuMilli) {
        this.cpuMilli = cpuMilli;
        return this;
    }


    /**
     * set 内存大小，以 MiB 为单位。

**换算关系：**
- 1024 MiB &#x3D; 1 GiB

**示例：**
- 16384 &#x3D; 16 GiB
- 65536 &#x3D; 64 GiB

**建议：** 内存建议为 GPU 显存的 2~4 倍

     *
     * @param memoryMiB 内存大小，以 MiB 为单位。

**换算关系：**
- 1024 MiB &#x3D; 1 GiB

**示例：**
- 16384 &#x3D; 16 GiB
- 65536 &#x3D; 64 GiB

**建议：** 内存建议为 GPU 显存的 2~4 倍

     * @return ResourceParamForJob
     */
    public ResourceParamForJob memoryMiB(Integer memoryMiB) {
        this.memoryMiB = memoryMiB;
        return this;
    }


}