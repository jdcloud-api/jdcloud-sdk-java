/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * 
 * 
 *
 * 
 * Contact: 
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

package com.jdcloud.sdk.service.jdaip.model;

import com.jdcloud.sdk.annotation.Required;

/**
 * 工作负载资源配置，定义Notebook的计算资源需求。

## 公共资源池配置
- 必须指定规格ID(flavorId)
- 必须指定逻辑可用区编码(logicAzCode)
- 规格包含固定的CPU、内存、GPU配置
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 不支持设置节点亲和性(nodeAffinities)

## 专享资源池配置
- 必须指定规格ID(flavorId)
- 必须指定逻辑可用区编码(logicAzCode)
- 规格包含固定的CPU、内存、GPU配置
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 不支持设置节点亲和性(nodeAffinities)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池配置
- 必须指定CPU和内存
- 可选配置GPU类型和数量
- 队列ID为用户工作空间中的私有队列ID

## GPU配置说明
- deviceModel: GPU设备类型，如 NVIDIA_H20-3e、NVIDIA_A100等
- vcudaCore: 虚拟GPU数量，支持切分(0.25/0.5/1/2/4/8等)

 */
public class WorkloadSpec  implements java.io.Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 资源队列ID，决定Notebook运行在哪个资源池。

## 公共资源池
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 使用平台共享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)

## 专享资源池
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 使用平台专享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池
- 队列ID为用户工作空间中的私有队列ID
- 使用用户专属资源，不计费
- 需要指定CPU和内存，可选GPU

     * Required:true
     */
    @Required
    private String queueId;

    /**
     * 公共/专享资源池的规格ID，定义固定的资源配置。

## 使用说明
- 仅公共/专享资源池需要指定
- 规格ID对应预定义的CPU、内存、GPU配置
- 可通过查询规格列表接口获取可用规格

## 示例
- cpu.2g4: 2核CPU，4GB内存
- gpu.t4.8g32: T4 GPU，8核CPU，32GB内存

     */
    private String flavorId;

    /**
     * CPU配置(单位：毫核)，仅私有资源池有效。

## 配置说明
- 1000毫核 &#x3D; 1核
- 示例：2000表示2核CPU

## 建议
- 开发调试：2-4核
- 模型训练：4-8核或更高

     */
    private Integer cpuM;

    /**
     * 内存配置(单位：MiB)，仅私有资源池有效。

## 配置说明
- 1024 MiB &#x3D; 1 GB
- 示例：8192表示8GB内存

## 建议
- 开发调试：4-8GB
- 模型训练：根据模型大小配置

     */
    private Integer memoryMiB;

    /**
     * GPU设备类型，仅私有资源池有效。

## 支持的GPU类型
- NVIDIA_H20-3e: 华为昇腾910B
- NVIDIA_A100: NVIDIA A100
- NVIDIA_A800: NVIDIA A800
- NVIDIA_T4: NVIDIA T4
- 更多型号请咨询平台支持

     */
    private String deviceModel;

    /**
     * 虚拟GPU数量，仅私有资源池有效。

## NVIDIA GPU配置
- 支持整数：1、2、4、8(卡)
- 支持小数：0.125、0.25、0.5(卡切分)

## 昇腾GPU配置
- 仅支持整数：1-8(卡)

## 使用说明
- 必须与deviceModel同时使用
- 小数表示GPU切分，共享GPU资源

     */
    private String vcudaCore;

    /**
     * 逻辑可用区编码，指定Notebook实例运行的逻辑可用区。

## 使用说明
- 公共/专享资源池必填，私有资源池无需指定
- 可通过查询可用区列表接口获取可用区编码
- 支持包含&quot;random&quot;关键字实现随机可用区调度

## 示例
- cn-north-1a: 指定可用区
- cn-north-1-random: 随机可用区

     */
    private String logicAzCode;

    /**
     * 物理集群名称，指定规格对应的异构可用区物理集群名称。

## 使用说明
- 仅公共/专享资源池有效，私有资源池无需指定

     */
    private String hpcClusterName;

    /**
     * 排队超时时间（分钟），Notebook在排队状态的最大等待时间。

## 使用说明
- 仅公共/专享资源池有效，私有资源池不支持此参数
- 不传或传0时默认使用系统配置 5分钟
- 取值范围：5~1440
- 超过排队超时时间后，Notebook将自动停止排队

## 示例
- 30: 排队30分钟后超时
- 60: 排队60分钟后超时

     */
    private Integer queuingTimeoutMinutes;



    /**
     * get 资源队列ID，决定Notebook运行在哪个资源池。

## 公共资源池
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 使用平台共享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)

## 专享资源池
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 使用平台专享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池
- 队列ID为用户工作空间中的私有队列ID
- 使用用户专属资源，不计费
- 需要指定CPU和内存，可选GPU

     *
     * @return
     */
    public String getQueueId() {
        return queueId;
    }

    /**
     * set 资源队列ID，决定Notebook运行在哪个资源池。

## 公共资源池
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 使用平台共享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)

## 专享资源池
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 使用平台专享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池
- 队列ID为用户工作空间中的私有队列ID
- 使用用户专属资源，不计费
- 需要指定CPU和内存，可选GPU

     *
     * @param queueId
     */
    public void setQueueId(String queueId) {
        this.queueId = queueId;
    }


    /**
     * get 公共/专享资源池的规格ID，定义固定的资源配置。

## 使用说明
- 仅公共/专享资源池需要指定
- 规格ID对应预定义的CPU、内存、GPU配置
- 可通过查询规格列表接口获取可用规格

## 示例
- cpu.2g4: 2核CPU，4GB内存
- gpu.t4.8g32: T4 GPU，8核CPU，32GB内存

     *
     * @return
     */
    public String getFlavorId() {
        return flavorId;
    }

    /**
     * set 公共/专享资源池的规格ID，定义固定的资源配置。

## 使用说明
- 仅公共/专享资源池需要指定
- 规格ID对应预定义的CPU、内存、GPU配置
- 可通过查询规格列表接口获取可用规格

## 示例
- cpu.2g4: 2核CPU，4GB内存
- gpu.t4.8g32: T4 GPU，8核CPU，32GB内存

     *
     * @param flavorId
     */
    public void setFlavorId(String flavorId) {
        this.flavorId = flavorId;
    }


    /**
     * get CPU配置(单位：毫核)，仅私有资源池有效。

## 配置说明
- 1000毫核 &#x3D; 1核
- 示例：2000表示2核CPU

## 建议
- 开发调试：2-4核
- 模型训练：4-8核或更高

     *
     * @return
     */
    public Integer getCpuM() {
        return cpuM;
    }

    /**
     * set CPU配置(单位：毫核)，仅私有资源池有效。

## 配置说明
- 1000毫核 &#x3D; 1核
- 示例：2000表示2核CPU

## 建议
- 开发调试：2-4核
- 模型训练：4-8核或更高

     *
     * @param cpuM
     */
    public void setCpuM(Integer cpuM) {
        this.cpuM = cpuM;
    }


    /**
     * get 内存配置(单位：MiB)，仅私有资源池有效。

## 配置说明
- 1024 MiB &#x3D; 1 GB
- 示例：8192表示8GB内存

## 建议
- 开发调试：4-8GB
- 模型训练：根据模型大小配置

     *
     * @return
     */
    public Integer getMemoryMiB() {
        return memoryMiB;
    }

    /**
     * set 内存配置(单位：MiB)，仅私有资源池有效。

## 配置说明
- 1024 MiB &#x3D; 1 GB
- 示例：8192表示8GB内存

## 建议
- 开发调试：4-8GB
- 模型训练：根据模型大小配置

     *
     * @param memoryMiB
     */
    public void setMemoryMiB(Integer memoryMiB) {
        this.memoryMiB = memoryMiB;
    }


    /**
     * get GPU设备类型，仅私有资源池有效。

## 支持的GPU类型
- NVIDIA_H20-3e: 华为昇腾910B
- NVIDIA_A100: NVIDIA A100
- NVIDIA_A800: NVIDIA A800
- NVIDIA_T4: NVIDIA T4
- 更多型号请咨询平台支持

     *
     * @return
     */
    public String getDeviceModel() {
        return deviceModel;
    }

    /**
     * set GPU设备类型，仅私有资源池有效。

## 支持的GPU类型
- NVIDIA_H20-3e: 华为昇腾910B
- NVIDIA_A100: NVIDIA A100
- NVIDIA_A800: NVIDIA A800
- NVIDIA_T4: NVIDIA T4
- 更多型号请咨询平台支持

     *
     * @param deviceModel
     */
    public void setDeviceModel(String deviceModel) {
        this.deviceModel = deviceModel;
    }


    /**
     * get 虚拟GPU数量，仅私有资源池有效。

## NVIDIA GPU配置
- 支持整数：1、2、4、8(卡)
- 支持小数：0.125、0.25、0.5(卡切分)

## 昇腾GPU配置
- 仅支持整数：1-8(卡)

## 使用说明
- 必须与deviceModel同时使用
- 小数表示GPU切分，共享GPU资源

     *
     * @return
     */
    public String getVcudaCore() {
        return vcudaCore;
    }

    /**
     * set 虚拟GPU数量，仅私有资源池有效。

## NVIDIA GPU配置
- 支持整数：1、2、4、8(卡)
- 支持小数：0.125、0.25、0.5(卡切分)

## 昇腾GPU配置
- 仅支持整数：1-8(卡)

## 使用说明
- 必须与deviceModel同时使用
- 小数表示GPU切分，共享GPU资源

     *
     * @param vcudaCore
     */
    public void setVcudaCore(String vcudaCore) {
        this.vcudaCore = vcudaCore;
    }


    /**
     * get 逻辑可用区编码，指定Notebook实例运行的逻辑可用区。

## 使用说明
- 公共/专享资源池必填，私有资源池无需指定
- 可通过查询可用区列表接口获取可用区编码
- 支持包含&quot;random&quot;关键字实现随机可用区调度

## 示例
- cn-north-1a: 指定可用区
- cn-north-1-random: 随机可用区

     *
     * @return
     */
    public String getLogicAzCode() {
        return logicAzCode;
    }

    /**
     * set 逻辑可用区编码，指定Notebook实例运行的逻辑可用区。

## 使用说明
- 公共/专享资源池必填，私有资源池无需指定
- 可通过查询可用区列表接口获取可用区编码
- 支持包含&quot;random&quot;关键字实现随机可用区调度

## 示例
- cn-north-1a: 指定可用区
- cn-north-1-random: 随机可用区

     *
     * @param logicAzCode
     */
    public void setLogicAzCode(String logicAzCode) {
        this.logicAzCode = logicAzCode;
    }


    /**
     * get 物理集群名称，指定规格对应的异构可用区物理集群名称。

## 使用说明
- 仅公共/专享资源池有效，私有资源池无需指定

     *
     * @return
     */
    public String getHpcClusterName() {
        return hpcClusterName;
    }

    /**
     * set 物理集群名称，指定规格对应的异构可用区物理集群名称。

## 使用说明
- 仅公共/专享资源池有效，私有资源池无需指定

     *
     * @param hpcClusterName
     */
    public void setHpcClusterName(String hpcClusterName) {
        this.hpcClusterName = hpcClusterName;
    }


    /**
     * get 排队超时时间（分钟），Notebook在排队状态的最大等待时间。

## 使用说明
- 仅公共/专享资源池有效，私有资源池不支持此参数
- 不传或传0时默认使用系统配置 5分钟
- 取值范围：5~1440
- 超过排队超时时间后，Notebook将自动停止排队

## 示例
- 30: 排队30分钟后超时
- 60: 排队60分钟后超时

     *
     * @return
     */
    public Integer getQueuingTimeoutMinutes() {
        return queuingTimeoutMinutes;
    }

    /**
     * set 排队超时时间（分钟），Notebook在排队状态的最大等待时间。

## 使用说明
- 仅公共/专享资源池有效，私有资源池不支持此参数
- 不传或传0时默认使用系统配置 5分钟
- 取值范围：5~1440
- 超过排队超时时间后，Notebook将自动停止排队

## 示例
- 30: 排队30分钟后超时
- 60: 排队60分钟后超时

     *
     * @param queuingTimeoutMinutes
     */
    public void setQueuingTimeoutMinutes(Integer queuingTimeoutMinutes) {
        this.queuingTimeoutMinutes = queuingTimeoutMinutes;
    }



    /**
     * set 资源队列ID，决定Notebook运行在哪个资源池。

## 公共资源池
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 使用平台共享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)

## 专享资源池
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 使用平台专享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池
- 队列ID为用户工作空间中的私有队列ID
- 使用用户专属资源，不计费
- 需要指定CPU和内存，可选GPU

     *
     * @param queueId 资源队列ID，决定Notebook运行在哪个资源池。

## 公共资源池
- 队列ID固定为：&#x60;joybuilder-public-queue&#x60;
- 使用平台共享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)

## 专享资源池
- 队列ID固定为：&#x60;joybuilder-exclusive-queue&#x60;
- 使用平台专享资源，按量计费
- 需要指定规格ID(flavorId)
- 需要指定逻辑可用区编码(logicAzCode)
- 必须设置调度优先级(taskPriority，取值1~9)

## 私有资源池
- 队列ID为用户工作空间中的私有队列ID
- 使用用户专属资源，不计费
- 需要指定CPU和内存，可选GPU

     * @return WorkloadSpec
     */
    public WorkloadSpec queueId(String queueId) {
        this.queueId = queueId;
        return this;
    }


    /**
     * set 公共/专享资源池的规格ID，定义固定的资源配置。

## 使用说明
- 仅公共/专享资源池需要指定
- 规格ID对应预定义的CPU、内存、GPU配置
- 可通过查询规格列表接口获取可用规格

## 示例
- cpu.2g4: 2核CPU，4GB内存
- gpu.t4.8g32: T4 GPU，8核CPU，32GB内存

     *
     * @param flavorId 公共/专享资源池的规格ID，定义固定的资源配置。

## 使用说明
- 仅公共/专享资源池需要指定
- 规格ID对应预定义的CPU、内存、GPU配置
- 可通过查询规格列表接口获取可用规格

## 示例
- cpu.2g4: 2核CPU，4GB内存
- gpu.t4.8g32: T4 GPU，8核CPU，32GB内存

     * @return WorkloadSpec
     */
    public WorkloadSpec flavorId(String flavorId) {
        this.flavorId = flavorId;
        return this;
    }


    /**
     * set CPU配置(单位：毫核)，仅私有资源池有效。

## 配置说明
- 1000毫核 &#x3D; 1核
- 示例：2000表示2核CPU

## 建议
- 开发调试：2-4核
- 模型训练：4-8核或更高

     *
     * @param cpuM CPU配置(单位：毫核)，仅私有资源池有效。

## 配置说明
- 1000毫核 &#x3D; 1核
- 示例：2000表示2核CPU

## 建议
- 开发调试：2-4核
- 模型训练：4-8核或更高

     * @return WorkloadSpec
     */
    public WorkloadSpec cpuM(Integer cpuM) {
        this.cpuM = cpuM;
        return this;
    }


    /**
     * set 内存配置(单位：MiB)，仅私有资源池有效。

## 配置说明
- 1024 MiB &#x3D; 1 GB
- 示例：8192表示8GB内存

## 建议
- 开发调试：4-8GB
- 模型训练：根据模型大小配置

     *
     * @param memoryMiB 内存配置(单位：MiB)，仅私有资源池有效。

## 配置说明
- 1024 MiB &#x3D; 1 GB
- 示例：8192表示8GB内存

## 建议
- 开发调试：4-8GB
- 模型训练：根据模型大小配置

     * @return WorkloadSpec
     */
    public WorkloadSpec memoryMiB(Integer memoryMiB) {
        this.memoryMiB = memoryMiB;
        return this;
    }


    /**
     * set GPU设备类型，仅私有资源池有效。

## 支持的GPU类型
- NVIDIA_H20-3e: 华为昇腾910B
- NVIDIA_A100: NVIDIA A100
- NVIDIA_A800: NVIDIA A800
- NVIDIA_T4: NVIDIA T4
- 更多型号请咨询平台支持

     *
     * @param deviceModel GPU设备类型，仅私有资源池有效。

## 支持的GPU类型
- NVIDIA_H20-3e: 华为昇腾910B
- NVIDIA_A100: NVIDIA A100
- NVIDIA_A800: NVIDIA A800
- NVIDIA_T4: NVIDIA T4
- 更多型号请咨询平台支持

     * @return WorkloadSpec
     */
    public WorkloadSpec deviceModel(String deviceModel) {
        this.deviceModel = deviceModel;
        return this;
    }


    /**
     * set 虚拟GPU数量，仅私有资源池有效。

## NVIDIA GPU配置
- 支持整数：1、2、4、8(卡)
- 支持小数：0.125、0.25、0.5(卡切分)

## 昇腾GPU配置
- 仅支持整数：1-8(卡)

## 使用说明
- 必须与deviceModel同时使用
- 小数表示GPU切分，共享GPU资源

     *
     * @param vcudaCore 虚拟GPU数量，仅私有资源池有效。

## NVIDIA GPU配置
- 支持整数：1、2、4、8(卡)
- 支持小数：0.125、0.25、0.5(卡切分)

## 昇腾GPU配置
- 仅支持整数：1-8(卡)

## 使用说明
- 必须与deviceModel同时使用
- 小数表示GPU切分，共享GPU资源

     * @return WorkloadSpec
     */
    public WorkloadSpec vcudaCore(String vcudaCore) {
        this.vcudaCore = vcudaCore;
        return this;
    }


    /**
     * set 逻辑可用区编码，指定Notebook实例运行的逻辑可用区。

## 使用说明
- 公共/专享资源池必填，私有资源池无需指定
- 可通过查询可用区列表接口获取可用区编码
- 支持包含&quot;random&quot;关键字实现随机可用区调度

## 示例
- cn-north-1a: 指定可用区
- cn-north-1-random: 随机可用区

     *
     * @param logicAzCode 逻辑可用区编码，指定Notebook实例运行的逻辑可用区。

## 使用说明
- 公共/专享资源池必填，私有资源池无需指定
- 可通过查询可用区列表接口获取可用区编码
- 支持包含&quot;random&quot;关键字实现随机可用区调度

## 示例
- cn-north-1a: 指定可用区
- cn-north-1-random: 随机可用区

     * @return WorkloadSpec
     */
    public WorkloadSpec logicAzCode(String logicAzCode) {
        this.logicAzCode = logicAzCode;
        return this;
    }


    /**
     * set 物理集群名称，指定规格对应的异构可用区物理集群名称。

## 使用说明
- 仅公共/专享资源池有效，私有资源池无需指定

     *
     * @param hpcClusterName 物理集群名称，指定规格对应的异构可用区物理集群名称。

## 使用说明
- 仅公共/专享资源池有效，私有资源池无需指定

     * @return WorkloadSpec
     */
    public WorkloadSpec hpcClusterName(String hpcClusterName) {
        this.hpcClusterName = hpcClusterName;
        return this;
    }


    /**
     * set 排队超时时间（分钟），Notebook在排队状态的最大等待时间。

## 使用说明
- 仅公共/专享资源池有效，私有资源池不支持此参数
- 不传或传0时默认使用系统配置 5分钟
- 取值范围：5~1440
- 超过排队超时时间后，Notebook将自动停止排队

## 示例
- 30: 排队30分钟后超时
- 60: 排队60分钟后超时

     *
     * @param queuingTimeoutMinutes 排队超时时间（分钟），Notebook在排队状态的最大等待时间。

## 使用说明
- 仅公共/专享资源池有效，私有资源池不支持此参数
- 不传或传0时默认使用系统配置 5分钟
- 取值范围：5~1440
- 超过排队超时时间后，Notebook将自动停止排队

## 示例
- 30: 排队30分钟后超时
- 60: 排队60分钟后超时

     * @return WorkloadSpec
     */
    public WorkloadSpec queuingTimeoutMinutes(Integer queuingTimeoutMinutes) {
        this.queuingTimeoutMinutes = queuingTimeoutMinutes;
        return this;
    }


}